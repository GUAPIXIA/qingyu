# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Electron build output
dist-electron
release
release-v2
.cache

# PoC 临时验证脚本
.poc-tmp-src/

# SillyTavern tools
SillyTavern-Launcher/

# Build artifacts / temp
.workbuddy/
*_spec_*.json
spec_*.json
.trae/
.reasonix/

# Environment files (contain secrets)
.env

# Local workspace files retained beside the repository root
/服务器备忘.md
/token.txt
/conn_pb.bin
/a.txt
/.pi-team/
/合并前备份-20260820/
/狐神抚.json
/启动开发.bat.lnk

# 测试覆盖率报告
coverage/

# 审查临时产物
lint*.json

# QA / Playwright 运行日志
output/
.qa/

# 开发环境本地语义模型（正式版存放在应用安装目录）
/models/

# AI 工具目录（本地工具数据，不入库）
.qoder/
.codebuddy/
.zcode/
.playwright-cli/

# android/ 与 server/ 为本仓库的子项目；各自的 .gitignore 继续排除运行时配置与构建产物。

# === 目录治理补充（2026-08-20 冗余清理） ===
.env.*
!.env.example
*.pem
*.key
*.keystore
*.jks
artifacts/
.gradle/
.kotlin/
.idea/
**/build/
local.properties
*.apk
*.aab
.poc-tmp/
.poc-tmp-src/
piolium/
relay-server/.env
relay-server/dist/

# === 隐私/凭据防误提交（2026-09-13） ===
# 应用运行时凭据与诊断（实际常落在 userData；仓库相对路径一并封死）
data/
**/credentials.json
**/generation-observations.jsonl
**/key-file.txt
# 本地工具 / 会话基线（可能拷贝测试密钥字符串，不入库）
.mimosa/

# === 测试沙盒与临时归档（2026-09-18） ===
# 各测试/演练会在仓库根落下 Electron userData 全量副本（每个约 6 MB，
# 曾累积到 87 个 / 533 MB）。它们**不是**源码、不入库；仓库根的散落副本
# 已统一移入 `.tmp-archive/`，此处一并封死，避免再次污染 git status。
.tmp-drill-*/
.tmp-s204-*/
.tmp-archive/
# 与 .poc-tmp/ / .qa/ 同类的本地 QA 草稿目录（原先漏了忽略，2.6 MB 未跟踪）
.qa-poc/

