# V12-14 联调报告（PC 侧 `Bridge v2` 就绪，安卓侧待升级）

> 时间：2026-08-20  
> 基线：`main@157cad1`（`0.12.0-alpha.1` 已含 `Bridge v2`）  
> 设备：MuMu 12 `V2364A` `Android 15` `127.0.0.1:16416`，包 `com.qingyu.companion` `v0.1.3 (code 4)`

## 1. ADB 探活

```bash
"D:/MuMuPlayer-12.0/nx_main/adb.exe" devices -l
# 127.0.0.1:16416 device product:PD2364 model:V2364A
"D:/MuMuPlayer-12.0/nx_main/adb.exe" -s 127.0.0.1:16416 shell pm list packages | grep qingyu
# package:com.qingyu.companion
"D:/MuMuPlayer-12.0/nx_main/adb.exe" -s 127.0.0.1:16416 shell dumpsys package com.qingyu.companion | grep version
# versionName=0.1.3 versionCode=4
```

* 模拟器可 `ping 10.0.2.2`（宿主回环），`adb forward` 可用，`pm` 可见旧版 `0.1.3` 已安装。

## 2. PC 侧 `Bridge v2` 就绪

* `electron/bridge/taskRoutes.ts` 已挂载 `POST /api/v2/sessions/:id/tasks`（`Idempotency-Key` 202）、`GET /tasks/:id/events?afterSequence=` 补拉、`cancel/retry`
* `taskWsAdapter.ts` 已实现 `task:subscribe {sessionIds,cursors}` 定向推送，不再全量广播
* `taskRoutes.test.ts` 已验证 `Idempotency-Key` 重放同一 `taskId` + `events` 补拉（`116文件/1112用例` 绿）
* `v1` 仍保留，旧安卓 `0.1.3` 可继续走 `api/v1`，`0.12` 保证兼容

## 3. 安卓侧现状与待办

* 当前模拟器安装 `0.1.3`，仅 `api/v1`，未实现 `api/v2` / `task:subscribe` / `afterSequence` 补拉
* 需升级至 `0.1.4+`：`QingyuApi.kt` 新增 `api/v2` 接口，`ChatRepository` 改走 `tasks` + `WS v2`，`CompanionEvent` 新增 `task:*` 类型

## 4. 手动验证步骤（用户在模拟器上执行）

1. PC 启动 `pnpm electron:dev`，确保 `BridgeServer` 监听 `0.0.0.0:8321`（或 `pnpm test -- taskRoutes` 已验证）
2. 模拟器启动 `com.qingyu.companion`，走 `api/v1` 配对并发送一条消息，验证旧链路仍可用
3. 升级安卓至 `0.1.4` 后：
   - 发送消息，`10.0.2.2:8321` 抓包可见 `Idempotency-Key` 头
   - 断网 5s 后重连，`GET /api/v2/tasks/:id/events?afterSequence=N` 补齐 `task:chunk`
   - `WS` 仅收到订阅会话的事件

## 5. 结论

* **PC 侧 `V12-14` 完成**：`Bridge v2` 已具备 `REST v2 + WS v2` 能力，`v1` 兼容保留
* **安卓侧 `V12-14` 阻塞**：需发 `0.1.4` 适配 `v2`（`QingyuApi` + `ChatRepository`），本文档即为联调基线

> 下一步 `V12-15` 进入 `rc.1` 发布检查（`pnpm check/lint/test:coverage` + 升级/回滚冒烟）。
