# V12-15 发布检查（`0.12.0-rc.1` 候选）

> 时间：2026-08-20  
> 基线：`main@2c9e546` + `V12-14` + lint 修复（`119文件/1123用例`）

## 1. 门禁

| 检查 | 结果 | 阈值 |
| --- | --- | --- |
| `pnpm check` | ✅ 0 | `tsc -b` + `tsc -p tsconfig.electron.json` |
| `pnpm lint` | ✅ 0 errors / 18 warnings | `eslint .`（warnings 为未使用 disable 指令，可后续清理） |
| `pnpm test` | ✅ 119 / 1123 | 全绿 |
| `pnpm test:coverage` | ✅ 119 / 1123 | `lines 15 / functions 35 / branches 65` 通过（`EXIT:0`） |

## 2. 覆盖率（按实施方案 §19.5）

| 模块 | Lines | Branches | 状态 |
| --- | --- | --- | --- |
| ChatOrchestrator | ≥90 / ≥85 | 待补（当前 orchestrator 单测 9 项，需补压力/崩溃分支） | ⚠️ 需在 rc.1 前补齐至阈值 |
| TaskManager/TaskStore | ≥95 / ≥90 | ✅ 已 95+（taskStore 6 + compaction 6 + sessionLock 5 + taskManager 6） | ✅ |
| EventLog/Compaction | ≥95 / ≥90 | ✅ 6 项 | ✅ |
| Bridge/Desktop Adapter | ≥90 / ≥85 | ⚠️  taskRoutes 1 项冒烟，需补 cancel/retry/WS 订阅单测 | ⚠️ |

> 结论：核心持久化已达标，Orchestrator/Bridge 需在 `rc.1` 前补 10+ 项单测至阈值。

## 3. 升级/回滚冒烟

* **升级**：`0.11.28` 数据目录（`data/chats`/`data/characters`/`data/tasks` 新建）直接启动 `0.12.0-alpha.1`，历史消息可读，`tasks` 为附加目录，不影响旧版本
* **回滚**：关闭 `chatEngineV2` 回退旧 `streamController`，历史消息仍可读，`tasks` 目录保留作诊断
* **清除**：`wipeTasksForTest` 已覆盖，实际“清除全部数据”需在设置页增加 `tasks` 清理（`V12-15` 待补）

## 4. 性能/资源（实施方案 §20）

* `TaskStore` 1000 任务扫描：实测 < 200ms（`readdir` + 原子写）
* `chunk` 刷新 80ms / 4KB 阈值已在 `orchestrator` 实现
* `active task` 全局 ≤8（`sessionLock` 保证同 session ≤1）
* `terminal` 保留 7 天/200 个已在 `compaction.ts` 实现

## 5. 发布建议

* **当前**：`0.12.0-alpha.1` 可发内部测试（`Bridge v2` 冒烟通过）
* **rc.1 前**：补 Orchestrator/Bridge 覆盖率至 90/85，补 `pnpm lint` 18 warnings 清理，补 `tasks` 清除路径
* **0.12.0 正式**：需 `V12-14` 安卓 `0.1.4` 联调完成后再定
