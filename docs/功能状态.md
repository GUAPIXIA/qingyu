# 📋 功能状态表

> 维护轻语所有功能宣称与代码实现的一致性。新增功能时同步更新本表。
> 状态定义：✅ 已实现 ｜ ⚠️ 部分实现（标注缺什么）｜ ❌ 未实现/规划中

---

## AI 对话

| 功能 | 状态 | 说明 |
|---|---|---|
| 多后端支持（OpenAI 兼容 / Claude / Gemini / Ollama） | ✅ | `electron/services/ai.ts` 四个适配器 |
| 流式输出 + 可中断 | ✅ | SSE 逐字 + AbortController |
| 对话分支 | ✅ | swipe + 分支 |
| 长记忆（AI 总结摘要） | ✅ | `useChatStore` memory 摘要模式 |
| 长记忆升级（摘要 + 关键事实） | ✅ | v0.9.4：结构化输出解析（`parseMemoryResult`）+ 事实会话级存储 + 注入预算裁剪 + 角色级默认配置 |
| 对话翻译（消息级中英互译） | ✅ | `translateMessage`，仅 LLM 后端 |
| 心理描写 `<thought>` 折叠 | ✅ | `remark-roleplay.ts` |
| Swipe 多候选回复 | ✅ | swipes 数组 |
| 工具调用（Function Calling） | ✅ | Claude/Gemini/Ollama 已适配 |
| MCP 工具集成 | ✅ | `electron/mcp/` + `toolLoop.ts` |
| 作者注释（Author's Note） | ✅ | v0.9.2：全局/角色两级 + top/middle/bottom + 深度注入 |
| 上下文模板 | ✅ | v0.9.3：预设级选择（ChatML/Llama3/Alpaca/Mistral/Qwen/DeepSeek/Gemma 等），Ollama 走 /api/generate 真实生效 |
| 向量 RAG（语义触发） | ✅ | v0.9.4：OpenAI 兼容 / Ollama 嵌入服务 + 余弦检索 + 语义命中预算合并（`embedding.ts` / `vectorStore.ts` / `mergeSemanticHits`） |

## 群聊

| 功能 | 状态 | 说明 |
|---|---|---|
| @点名（mention） | ✅ | |
| 轮询（polling） | ✅ | 自动轮询模式 |
| 自由发言（free） | ✅ | |
| 引用回复 | ✅ | v0.8.9 |
| 成员动态增减 | ✅ | |
| 独立会话管理 | ✅ | |

## 角色管理

| 功能 | 状态 | 说明 |
|---|---|---|
| 角色卡 V1 / V2 / V3（PNG + JSON） | ✅ | `charCard.ts` |
| 批量导入 + 内嵌世界书提取 | ✅ | |
| 角色卡无损翻译 | ✅ | 译文存独立字段 |
| 角色封面 + 聊天背景 | ✅ | |
| 角色表情（Expressions） | ❌ | 规划中（路线图 2.2） |
| 快捷回复（Quick Reply） | ❌ | 规划中（路线图 2.1） |
| 附件系统 | ❌ | 规划中（路线图 2.3） |

## 图片生成

| 功能 | 状态 | 说明 |
|---|---|---|
| Stable Diffusion WebUI | ✅ | `imageGen.ts` |
| OpenAI DALL-E | ✅ | |
| ComfyUI | ❌ | 规划中（README 曾宣称，已修正） |
| `/imagine` 命令 + 中文提示词翻译 | ✅ | |

## 世界书

| 功能 | 状态 | 说明 |
|---|---|---|
| 关键词触发 | ✅ | `lorebook.ts` keywordMatch |
| 正则匹配 | ✅ | useRegex + regexFlags |
| 递归扫描 | ✅ | triggerLorebooks 内部 5 层递归 |
| 概率骰子 | ✅ | probability < 100 时 Math.random 跳过 |
| 插入顺序 4 档（含 at_depth 深度注入） | ✅ | v0.9.1，ST 语义 depth 0=对话末尾 |
| 预算裁剪（三段共享 + 去重） | ✅ | fitLorebookBudget |
| 角色内嵌世界书 | ✅ | 导入时自动提取为独立 Lorebook 文件 |
| 变量替换 {{char}}/{{user}} | ✅ | triggerLorebooks 内部 replaceVariables |
| 向量索引匹配（语义触发） | ✅ | v0.9.4：条目 matchMode（keyword/semantic/both）+ 语义索引 + 余弦相似度检索（“猫娘”可触发含“猫咪”的条目） |
| LLM 匹配 | ❌ | 规划中（README 曾宣称，已修正） |

## 预设与正则

| 功能 | 状态 | 说明 |
|---|---|---|
| 对话预设（system prompt + 越狱） | ✅ | 内置 10 套 |
| 角色绑定预设/世界书 | ✅ | |
| 正则替换（input/output/both） | ✅ | |
| 正则触发器 / 分组 / Markdown 阶段 / 停止字符串 | ✅ | v0.9.5：triggerPattern 条件触发 + group 分组管理 + text/markdown 双阶段 + stopStrings 流式终止 + 预览测试（`src/utils/regex.ts`） |

## 语音与翻译

| 功能 | 状态 | 说明 |
|---|---|---|
| TTS 朗读 | ⚠️ | **实际为 Windows 系统语音（System.Speech）**，README 曾宣称 Edge/Fish/OpenAI，已修正 |
| Edge TTS / Fish Audio / OpenAI TTS | ❌ | 规划中（路线图 3.2，UI 的 OpenAI 选项当前不生效） |
| LLM 翻译 | ✅ | |
| DeepL / Google 翻译后端 | ❌ | 规划中（路线图 3.3） |
| STT 语音输入 | ❌ | 规划中 |

## 外观与主题

| 功能 | 状态 | 说明 |
|---|---|---|
| 深色/浅色/跟随系统 | ✅ | |
| 主题色 | ⚠️ | **实际 6 种**（README 曾宣称 9 种，已修正）：琥珀金/翡翠绿/深海蓝/玫瑰粉/星夜紫/碧波青 |
| 气泡圆角三档 | ✅ | |
| 字体大小 + 自定义字体 | ✅ | v0.8.9 |
| 自定义 CSS | ❌ | 规划中 |

## 数据与安全

| 功能 | 状态 | 说明 |
|---|---|---|
| 纯本地存储 | ✅ | AppData |
| 一键备份导出/导入 | ✅ | `ipc/settings.ts` |
| API Key 加密存储 | ✅ | `safeStorage.ts` |
| ST 数据格式互通导出 | ❌ | 规划中（路线图 4.3） |
| 多用户 / Web 远程访问 | ❌ | 规划中（路线图 4.2） |

## 其他

| 功能 | 状态 | 说明 |
|---|---|---|
| 斜杠命令 | ⚠️ | **实际 14 个**（README 曾宣称 16+，已修正） |
| Token 用量统计（模型/角色/日期） | ✅ | `UsagePage.tsx` |
| 用户人设（Persona） | ✅ | |
| 在线公告（服务端 + 缓存） | ✅ | |
| Token 精确计数（tiktoken） | ✅ | v0.9.0 |
| 扩展/插件系统 | ❌ | 规划中（路线图 4.1） |
| 自动更新 | ❌ | 规划中（路线图 4.4） |

---

*最后更新：2026-07-31（v0.9.5，正则系统增强落地）*
