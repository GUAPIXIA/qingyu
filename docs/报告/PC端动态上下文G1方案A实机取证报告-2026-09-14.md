# G1 方案A + chenxi/deepseek-v4.1-flash 取证

> 生成时间：2026-09-14 03:27:59
> 数据源：`prod` ← `.poc-tmp/eval-g1-flash/prod/results.json`；`no-gate` ← `.poc-tmp/eval-g1-flash/no-gate/results.json`
> 口径：主计划 §7.7 / §9.4；只含数值与枚举，不含提示词、正文、URL 或密钥。

## 汇总

| 指标 | prod | no-gate |
|---|---:|---:|
| 用例数 | 144 | 144 |
| 生成调用数（含重试/修复） | 159 | 159 |
| 评审调用数 | 0 | 0 |
| 运行错误用例 | 0 | 0 |
| 其中基础设施错误（限流/超时/5xx，不计入 G1） | 0 | 0 |
| 结构化检查失败 / 警告 | 50 / 164 | 40 / 159 |
| completion token | 2079 | 1822 |
| reasoning token | 0 | 0 |
| 推理占比 | 0.0% | 0.0% |
| finish_reason=length 调用 | 0 | 0 |
| length + 空正文调用（推理挤占） | 0 | 0 |
| 提前中止用例（earlyAbort） | 0 | 0 |
| 其中中止后正文仍为空 | 0 | 0 |
| 空正文用例（用户可见失败） | 0 | 0 |
| **用户可见失败率** | 0.0% | 0.0% |
| 空正文同档重试恢复（transport length+0 / empty_output） | 0/0 | 0/0 |
| 可见正文 < 20 字符用例 | 4（2.8%） | 4（2.8%） |
| 推理挤占可见失败率（空正文/length 调用 ÷ 调用） | 0.0% | 0.0% |
| 延迟 P50 / P95（ms） | 5032 / 9817 | 5057 / 10064 |
| 评审完成 / 未通过 | 0 / 0 | 0 / 0 |

## G1 判定（2026-09-13 修订口径）

| # | 门槛 | 实测 | 判定 |
|---|---|---|---|
| 1 | 推理挤占用户可见失败率 < 0.5% | 0/144 = 0.0%；95% 上界 ≈ 2.1%（零失败需 ≥ 600 例才压到 0.5% 以下，作附注不单独判负） | ✅ 达标（观测） |
| 2 | 空正文降档恢复成功率 ≥ 90% | 方案 A：disableIgnored/off 末级无可降档，空正文同档重试计入恢复；无空正文重试样本（transport 层 length+0 或 empty_output）→ N/A | ✅ N/A（无空正文需恢复；第1条已 0 失败） |
| 3 | 只有正文 < 20 字符才整轮恢复 | 正文 < 20 字符用例 4/144；行为由 gateRecovery / resolveEmptyOutputRecovery 单测锁定（<20 字符才整轮恢复） | ✅ 达标（单测契约） |
| 4 | 只在"继续已不可能产出合格正文"时中止；成对对照不使正文产出率下降 | 提前中止 0 例（未发生中止 → 无收益损失）；对照臂正文产出率 100.0% | ✅ 达标 |
| 5 | 降档额外 completion token 占比 < 3% | 方案 A 主路径为同档重试（非降档）；本取证无降档成对样本 | ✅ N/A（无降档，不触发） |
| 6 | 同请求体条件下成功路径无回归 | 成功路径 P95：主臂 9.8s vs 对照臂 10.1s（-2.5%，n=144/144） | ✅ 无回归 |
| 7 | 全量测试、Bridge 与跨端 fixture | `pnpm test` 与 `--batch verify` 全绿（见交接记录） | ✅ 达标 |

**总判：✅ G1 通过（按本报告口径）**

### 跨臂对比：`prod`（基准）→ `no-gate`

- 推理占比：0.0% → 0.0%（0.0%）
- 空正文（length）调用：0 → 0
- 正文 < 20 字符用例：4 → 4
- P95 延迟：9817 ms → 10064 ms（2.5%）

## 分批次

### 批次 `dialogue`

| 指标 | prod | no-gate |
|---|---:|---:|
| 用例数 | 72 | 72 |
| 推理占比 | 0.0% | 0.0% |
| length + 空正文 | 0 | 0 |
| 提前中止 | 0 | 0 |
| 正文 < 20 字符 | 0 | 0 |
| 延迟 P50 / P95（ms） | 4947 / 5807 | 4955 / 5792 |

### 批次 `directions`

| 指标 | prod | no-gate |
|---|---:|---:|
| 用例数 | 48 | 48 |
| 推理占比 | 0.0% | 0.0% |
| length + 空正文 | 0 | 0 |
| 提前中止 | 0 | 0 |
| 正文 < 20 字符 | 4 | 4 |
| 延迟 P50 / P95（ms） | 5259 / 10181 | 5191 / 10234 |

### 批次 `stream`

| 指标 | prod | no-gate |
|---|---:|---:|
| 用例数 | 24 | 24 |
| 推理占比 | 0.0% | 0.0% |
| length + 空正文 | 0 | 0 |
| 提前中止 | 0 | 0 |
| 正文 < 20 字符 | 0 | 0 |
| 延迟 P50 / P95（ms） | 5032 / 5776 | 5026 / 5825 |

## 本脚本未覆盖的 G1 指标（需应用层灰度补齐）

- 空正文降档恢复成功率：依赖渲染层恢复控制器（`src/store/gateRecovery.ts` + 流控制器），离线夹具只覆盖单次调用；
- 降档额外 completion token 占比：需要"首次失败 + 降档重试"的成对记录，离线两臂只给同题对照；
- 提前中止节省等待时间：本脚本以"同臂未中止用例中位延迟"为基线估算，非生产端到端计时；
- 500 次 unified 有效生成（G2 口径）：需真实用户流量，口径见 `shared/generationBaseline.ts`。
