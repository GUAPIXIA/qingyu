# G2 取证进度（§7.13）

- 观测文件：C:\Users\Administrator\AppData\Roaming\qingyu\data\diagnostics\generation-observations.jsonl
- 生成时间：2026/9/14 13:44:27
- 记录：566 条 → 分析 566 条
- 口径：pass=1 best=unified/gate:none:509/500 providers=3 remaining=0 segments=1 allValid=509

## 分段进度（不得跨段混合过门）

| 分段 | 总记录 | 有效 | 供应商 | 距 500 |
|---|---:|---:|---|---:|
| unified/gate:none | 566 | 509 | ep:2fca3758, ep:7e513190, openai | ✅ |

- 全量有效（跨段总览，**不可**作为过门计数）：509
- 全量供应商：ep:2fca3758, ep:7e513190, openai
- 可过门分段：unified/gate:none

## G2 条款清单（离线可核对 + 人工项）

| # | 状态 | 条款 | 说明 |
|---|---|---|---|
| 1 | ✅ | unified 有效生成 ≥ 500 且 ≥ 2 供应商（同一功能状态分段） | 最佳分段 unified/gate:none：509/500，供应商 3（ep:2fca3758, ep:7e513190, openai）；全量有效 509（不可跨段混合过门） |
| 2 | ✅ | 阶段 7 截断/格式/落盘/跨端 fixture 达标 | 需对照阶段 7 实施报告与 fixture 命令；本脚本不自动判定 |
| 3 | ✅ | G1 达标 | 方案 A + chenxi flash 实机取证已通过（2026-09-14） |
| 4 | ✅ | 动态上下文无 mandatory 丢失 / 记忆删除 / 超限回归 | 依赖 chat-core 回归与灰度观察；观测 JSONL 无该维度 |
| 5 | ⏳ | Android 共享类型与 fixture 通过 | 本期不接入 Android；需单独跑跨端 fixture |
| 6 | ✅ | 至少一个版本保留 legacy 可回退 | W11 前不得删除 legacy；当前代码应仍保留 generationPipeline 分支 |
| 7 | ✅ | 500 次样本按功能状态分段，不混合 gate 前后 | 存在可过门分段：unified/gate:none |

## 说明

- 有效生成口径见 `shared/generationBaseline.ts`（C4 冻结），本脚本不重定义分母。
- G2 第 1/7 条：**同一分段**内 ≥500 有效且 ≥2 供应商；不同 gate/pipeline 状态不得加总过门。
- 其余条款需人工或另包证据；`--g1-passed` 等旗标只标注清单，不自动读取 G1 报告文件。

## 核实记录（2026-09-14）

### 条款 2 — 阶段 7

| 证据 | 结果 |
|---|---|
| 实施报告 7.1–7.3 | [对话输出弹性约束阶段7实施报告](./对话输出弹性约束阶段7实施报告-2026-09-13.md)（7.4 即本 G2 运行期门） |
| 509 条有效观测 | 空正文 **0**；触顶/length **0**；earlyAbort **0**；格式告警 **2**（引号/星号/thought 不平衡，0.4%） |
| PC 契约 fixture | `contractFixtures` + `settingsSnapshot` **27 通过** |
| PC 分块 fixture | `roleplayBlocksFixture` **38 通过** |

### 条款 4 — 动态上下文

| 证据 | 结果 |
|---|---|
| `shared/chat-core` + `contextBuilder` | **169 通过**（含 mandatory 不丢、小窗口分级让位、记忆回滚） |
| 超限 | 影子/分配器在预算可行时 `mandatoryOverBudget=false`；不可行时显式标记不静默丢 |

### 条款 6 — legacy 保留

| 证据 | 结果 |
|---|---|
| `Settings.generationPipeline?: 'unified' \| 'legacy'` | 仍在 `shared/types.ts` |
| 设置 UI | `BehaviorSection.tsx` 仍可切 legacy |
| 生产分支 | `contextBuilder` / `groupStreamController` / Bridge 仍读 pipelineLegacy |

### 条款 5 — Android（仍 ⏳）

| 证据 | 结果 |
|---|---|
| 共享 fixture 权威 | `shared/fixtures/`（settings / pairing / task / roleplay-blocks 等） |
| Android 半边测试 | `ProtocolContractTest` / `RoleplayBlocksFixtureTest` 需 `android` Gradle 单测；**本会话未跑** |
| PC 侧同 fixture | 已绿（见条款 2） |

**结论**：条款 1–4、6、7 可判满足（5 除外）。G2 总门在条款 5（Android fixture 实测）补绿前仍记 **未完全通过**；W11 仍不得启动。

