# G2 取证进度说明（主计划 §7.13）

> 日期：2026-09-14  
> 状态：取证工具已交付；**G2 未通过**（本机有效生成远未达 500，且仅 1 供应商）  
> 工具：`shared/g2Readiness.ts` + `scripts/g2-progress.ts`

## 1. 结论

legacy 退出门（G2）当前**不满足**。离线可核对的第 1/7 条：

| 项 | 本机观测（2026-09-14） |
|---|---|
| 最佳分段 | `unified/gate:none` |
| 有效生成 | **7 / 500**（缺 493） |
| 供应商 | **1**（openai）→ 要求 ≥2 |
| 跨段加总 | 不可作为过门计数 |

W11（删 legacy、8192 复审）按主计划**不得**提前执行。

## 2. 复现

```text
npx tsx scripts/g2-progress.ts --g1-passed --out .poc-tmp/g2-progress.md
# 退出码 0=可过门；2=未达标
npx vitest run shared/__tests__/g2Readiness.test.ts
```

人工项旗标（只改清单标注，不自动读文件）：`--phase7-ok`、`--dynamic-context-ok`、`--android-ok`、`--legacy-kept`。

## 3. 过门口径

- 有效生成：复用 `shared/generationBaseline.computeValidGenerations`（C4 冻结）。
- **同一分段**（`pipeline` × `gate`）内有效 ≥500 且不同 provider ≥2。
- 不同 gate/pipeline 状态的样本**不得加总**过门（G2 第 7 条）。

## 4. 未覆盖

- 阶段 7 指标、Android fixture、动态上下文回归：清单标 ⏳，需另证据。
- 自动读取 G1 报告：本包不做；用 `--g1-passed` 人工标注（G1 已于 2026-09-14 方案 A 实机通过）。
