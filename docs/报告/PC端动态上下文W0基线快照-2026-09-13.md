# PC 端动态上下文 W0 基线快照

> 日期：2026-09-13
> 状态：👀 运行期观察 — W0 冻结基线已固化；阶段 7.4 的 500 次有效生成门槛未达标，本文件不修改该门槛
> 上游：[PC 端动态上下文与生成规划器重构实施方案](../方案/PC端动态上下文与生成规划器重构实施方案-2026-09-13.md)（下称主计划，§2.1 / §7.2 为本文唯一口径来源）
> 用途：主计划 G1/G2 的输入（失败形态证据、有效生成分母口径、分组方式、冻结提交）；**不含正文、提示词、角色卡、世界书、凭据与完整端点**

---

## 1. 冻结基线与提交

W0 开工时的基线为清理期 A/B/C 三线合入后的提交（对应主计划 §16）：

| 提交 | 内容 | 归属 |
|---|---|---|
| `3373d4d` | 阶段 7 弹性约束公共模块、thought 契约、观测基线与群聊提示词 | C 线 |
| `b70f4e3` | 反向依赖下沉 `shared/chat-core/`，Electron/渲染层改引共享实现并加 ESLint 门禁 | B 线 |
| `a7d3d34` | Android 语义分块渲染、观测收尾字段与协议契约同步 | 清理期收尾 |
| `5002eee` | relay 错误响应收敛、备份自检与评测/基线脚本正式化 | A 线 / C4 |
| `04db306`、`54b47cd` | 方案/报告分类归档重构与索引同步 | 文档整理 |
| `0edad70` | W0 实施起点（HEAD，与上述提交共同构成冻结基线） | — |

冻结口径：

- `shared/chat-core/contextBuilder.ts` 是 W7–W9 的改造落点，`src/context/contextBuilder.ts` 仅为过渡垫片；
- settings 迁移链为单一链，`LATEST_VERSION.settings = 3`，W10 的产品语义迁移挂 `v3 → v4`；
- 有效生成口径纯函数：`shared/generationBaseline.ts`（`computeValidGenerations` / `groupKey` / `buildGroupStats`）。

---

## 2. 方向任务失败形态（主计划 §2.1 匿名摘要，未重新统计）

来源：主计划 §2.1 与清理期 §8 接口契约，均为匿名数值统计，无正文：

- `taskType === 'direction'` 观测共 **15 条**；
- 其中 **6 条 `bodyVisibleChars = 0`**，即方向任务空正文失败率 **40%**；
- 这 6 条的 `reasoningTokens` 都等于 `requestedMaxTokens = 1536`；
- 这 6 条均以 `terminationCause = provider_length` 结束（`truncationKind = reasoning_filled`）；
- 15 条方向记录的 `requestedMaxTokens` 全部为 1536（入口硬编码，见 §4）。

W0 核对（仅确认冻结数字与当前本机观测文件一致，不改变上述口径）：6/15 空正文、1536/1536/1536、`length` + `provider_length` 全部对齐。

对照评测（推理随上限弹性增长，证明“调大 1536 不是修复”）：

| 请求上限 | 推理消耗 | 正文可见字符 |
|---|---:|---:|
| 4000 | 3981 | 50 |
| 8000 | 8000 | 0 |

当前唯一直接绕过统一预算的生产入口（主计划 §2.2）：

- `src/store/dialogueDirectionRunner.ts`：`DIALOGUE_DIRECTION_MAX_TOKENS = 1536` + `reasoningMode: 'disabled'`；
- `electron/bridge/dialogueDirections.ts`：同一常量，W4 单聊修复验证后对齐。

---

## 3. 有效生成口径与计数（C4，G2 500 次分母）

口径定义见 `shared/generationBaseline.ts`，脚本 `scripts/generation-baseline.ts`（清理期已正式化）：

- **计入**：`source ∈ {single, group, bridge}` 且 `taskType` 缺省（主对话），`outcome ∈ {completed, truncated, user_cancelled}`；
- **不计入**：`aux`、后台结构化任务（memory / compression / title / direction）、`outcome = error`（失败率单独统计，不进 500 分母）；
- **分段**：gate 开启前后按 `ts` 分段，不得混合（G2 第 7 条）；
- **分组**：`provider / model / taskType`（`groupKey`），G1/G2 的指标不得只看全局均值。

W0 冻结时本机快照（2026-09-13 17:45，观测文件 63 条；命令见附录）：

| 指标 | 数值 |
|---|---:|
| 有效生成 | **7 / 目标 500** |
| completed | 7 |
| truncated | 0 |
| user_cancelled | 0 |
| 排除 aux | 53（含 direction 15、memory 3） |
| 排除后台任务 | 0（已并入 aux 排除） |
| 排除 error | 3 |
| 分组 `openai/deepseek/deepseek-v4-pro/main` | 有效 7 / 总计 10 |

说明：本机快照仅用于记录冻结时刻的进度，不作为“已完成样本量”声明；方向任务失败证据（§2）来自清理期数据集，两者不混用。

---

## 4. W0 冻结的派生值与固定上限（防漂移）

以下数值均由公式派生，**不是独立常量**，W0 已用 `shared/__tests__/modelOutputProfile.test.ts` 的“派生值证明”用例锁死（改系数或改档案会先打红）：

| 用途 | 正文保护线 | 公式结果（deepseek-v4 档案：正文预算 + 3072 推理余量） |
|---|---:|---:|
| 记忆 | 2500 | `resolveRequestBudget(2500)` → 正文 3221 + 推理 3072 = **6293** |
| 压缩 | 600 | `resolveRequestBudget(600)` → 正文 846 + 推理 3072 = **3918** |
| 尾部修复 | 200 | `resolveRequestBudget(200)` → 正文 346 + 推理 3072 = **3418** |

同时冻结、本期不动的边界：

- `MAX_REQUEST_OUTPUT_TOKENS = 8192` 仍为通用安全阀（独立决策门见主计划 §10，W0/W1–W10 不预承诺删除）；
- 长记忆 `min(800, budgetBase*0.1)`、世界书 30% 与 0.4/0.9 分桶仍为现行固定策略，分别由 W8/W9 迁移；
- 方向任务每个逻辑请求当前最多 2 次物理调用（1 次结构修复），W4 起并入统一 `AttemptLedger` 约束。

---

## 5. W0 新增的防漂移测试

只加测试/报告，未改变任何生产请求：

| 文件 | 锁定内容 |
|---|---|
| `shared/__tests__/modelOutputProfile.test.ts` | 2500/600/200 派生值公式复算；非推理模型只叠加协议余量；保护线单调不减；0/负数/NaN/Infinity 与最小正预算；8192 截断；P90 样本边界（全 0、恰好 10 个、全部非法） |
| `shared/__tests__/generationObservation.test.ts` | 单聊/群聊/Bridge 共用 finishReason → outcome / terminationCause / truncationKind 三态闭环；传输中断口径；direction 任务 1536 推理吃满、正文 0、length → `reasoning_filled` |
| `src/store/__tests__/dialogueDirectionRunner.test.ts` | 方向任务推理挤占 fixture：length + 空正文走一次结构修复；两次吃满放弃；第二次非空非法不发起第三次；当前 1536 + `disabled` 直连基线 |
| `src/store/__tests__/groupStreamController.test.ts` | 群聊 stop / length（句界收束 vs 一次补尾）/ cancelled / content_filter 四态契约；异常提示不进入正文 |
| `electron/bridge/__tests__/chatService.finalizeOrder.test.ts` | Bridge `ai:done.finishReason` 与桌面端同口径（stop / length / cancelled）；length 短前缀仅一次非流式补尾 |
| `src/store/__tests__/generationTerminationPhase7.test.ts`（既有） | 单聊终止状态机、迟到事件幂等、length 补尾（清理期已交付，W0 未改动） |

---

## 6. 阶段 7.4 状态声明

- 阶段 7.4（灰度验收与 legacy 退出）**尚未完成**；unified 有效生成 **7/500**，未覆盖两个以上供应商；
- 本文**不修改** 500 次门槛，也不改变其分母口径（口径已由 C4 冻结，见 §3）；
- 在门槛未满足前：不新增 `generationPlannerV2` 开关、不删除 legacy 回滚链路（主计划 §1 执行结论第 4 条）；
- 样本按功能状态分段：`reasoningGateEnabled` 开启前的数据不得与开启后混合计入（G2 第 7 条）。

---

## 7. 附录：复现命令

```text
npx tsx scripts/generation-baseline.ts --file <观测文件路径>     # 有效生成计数 / 分组 / 分位
npx vitest run shared/__tests__/modelOutputProfile.test.ts shared/__tests__/generationObservation.test.ts
npx vitest run src/store/__tests__/dialogueDirectionRunner.test.ts src/store/__tests__/groupStreamController.test.ts
npx vitest run electron/bridge/__tests__/chatService.finalizeOrder.test.ts
```

隐私核对：本文与新增测试均不含提示词、角色卡、世界书、回复正文、完整 URL、凭据与磁盘路径；观测样本只保留数值与枚举。

---

## 8. 风险与下一包依赖

- W1（观测回读与 UsageProfile）依赖本文冻结的分组键与口径，新增端点指纹必须先移除凭据 / query / fragment；
- W2（阶段 8 共享门控）接入后，本文件 §4 的 8192 与 3072 默认余量语义在可靠门控在场时以门控为准；
- W4 会替换方向任务的 1536 直连预算，`dialogueDirectionRunner.test.ts` 中的“当前基线”用例届时必须显式改写；
- 方向任务失败证据的完整样本量仍不足，需在 W4/W5 真实模型矩阵中继续积累（不得用单元测试冒充样本）。
