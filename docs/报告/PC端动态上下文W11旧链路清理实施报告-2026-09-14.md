# PC 端动态上下文 W11 旧链路清理实施报告

> 日期：2026-09-14  
> 对应方案：[主计划 §7.14](../方案/PC端动态上下文与生成规划器重构实施方案-2026-09-13.md)  
> 前置：G2 七条已核实（[G2 取证进度说明](./PC端动态上下文G2取证进度说明-2026-09-14.md)）  
> 状态：已实施；8192 决策门结论为**保留**

## 1. 交付结论

在已并入 W8W9 记忆/世界书接管与方案 A 的分支上完成安全清理：

1. **legacy 退出（一版兼容）**：设置页不再提供「旧版管线」切换；默认 unified。若数据仍为 `generationPipeline: 'legacy'`，显示只读提示并提供「切回新版」按钮。生产 `pipelineLegacy` 代码分支保留一个版本。
2. **方向 1536**：W4 已退出生产调用，本包无残留生产路径。
3. **记忆 800**：生产主路径为候选注入；`fitLayeredMemoryBudget` **保留为异常回滚**（§7.10 第 2 条），不删除实现体。
4. **世界书固定比例**：生产 `lorebookBudget = budgetBase`（W10 迁移已将 `lorebookRatio` 迁出运行时预算）。
5. **shadow 默认关闭**：`BuildOptions.shadow` 缺省不再采集；仅显式 `{ shadow: 'shadow' }` 用于测试/诊断。
6. **`reasoningGateEnabled`**：`isReasoningGateEnabled` **恒为 true**（G1 后门控常开）；设置页移除开关，思考强度档位仍可选。
7. **8192**：按 §10 决策门——证据不足（无「正文需求被 8192 阻止且成功样本」），**继续保留** `MAX_REQUEST_OUTPUT_TOKENS = 8192`。

## 2. 测试

- `shared/chat-core` + `contextBuilder` + `adapterGate` + `gateRecovery` + 设置相关：**190 通过**
- `tsc -b --noEmit`：通过

## 3. 未做 / 边界

- 未物理删除 `fitLayeredMemoryBudget` / runaway guard 实现（回滚与单测仍需要）。
- 未提高或删除 8192。
- 群聊路径未下沉部分仅随共享默认一致，未做独立接管。
- 跨分支 merge 受环境限制：W8W9 以 `git show` 文件集并入本分支（`e552005`）。

## 4. compose

`docs/compose/spec/w11-cleanup.md`
