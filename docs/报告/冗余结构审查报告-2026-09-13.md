# 冗余结构审查报告（三端）

> 日期：2026-09-13  
> **实施状态（同日更新）**：批次①②③ 已实施完成——D1–D8 全部删除；C1–C5 收敛（新增 `shared/thoughtMarkup.stripAllThinking`、`shared/translationPrompt`、`shared/groupChatPrompt`、`shared/defaultMemory` 四个共享模块，`chatService.translateGroup` 取代 routes 内联旧实现，`onDone` 轨道整体删除）；B1 IPC 死写路径与 B3 安卓零读取镜像字段已清理，注释腐化已更正。验证：桌面 vitest 236 文件 / 2475 项通过（4 skip），tsc×2 与 eslint 通过；安卓 gradle 67 套件 / 507 项通过。实施中补充发现并一并删除：`renameGroupSession` 同为全链零调用（报告 D7 只列了 `createGroupSession`）；`lorebook:importJson` 牵连的 `autoPickAmbiguous` 选项参数一并收口。批次④（E1–E3、R1、迁移并链）仍待最低版本决策，保持原计划。  
> 范围：`src/`、`electron/`、`shared/`、`android/`、`server/`、`relay-server/`（排除 node_modules、dist、构建产物与生成代码）
> 口径：以 `main`/生产代码调用点为准，测试与 `__tests__` 引用不算真实消费方；高置信度逐点验证，宁缺毋滥。
> 背景：接续 S8（legacy/unified 双管线，见[阶段0-6剩余问题报告](./阶段0-6与thought修复后剩余问题及实施建议-2026-09-12.md)）的排查——本报告回答"除双管线外还有哪些冗余结构"。双管线本体与 legacy 退出计划不在本报告重复展开。

## 一、结论总览

除双管线外，三端仍存在五类冗余：**纯死代码**、**已发生漂移的双实现**、**只剩旧数据/旧客户端消费的兼容垫片（含已死的写路径）**、**双轨持久化结构**、**跨端手抄镜像表**。合计 20+ 项，其中约 15 项可无风险直接删除。

| 类别 | 编号 | 级别 | 处置 |
|---|---|---|---|
| 死代码 | D1–D8 | 低 | 立即删除（连带各自测试） |
| 复制漂移 | C1–C5 | 中 | 收敛到 shared/单点实现，优先 C1、C2 |
| 兼容垫片 | B1–B3 | 低–中 | 保留读回退，清理死写路径与零读取镜像 |
| 双轨持久化 | E1–E3 | 中 | 计划性收口（随版本观察期） |
| 镜像表 | R1–R3 | 低–中 | R1 下沉 shared；R2/R3 视运维确认 |

已复核项（grep 逐点验证零生产调用方）：D1、D3、D4、D6、D7。其余为深度审计发现，动手前按表中证据行号复核一次即可。

## 二、死代码（零生产调用方，可直接删）

### D1【桌面】`electron/chat/postProcessor.ts` 整模块
- 导出 `postProcessOutput` / `getStopStrings`，是 `src/utils/regex.ts` 的 `applyOutputRegexRules` + `truncateAtStop` + `collectStopStrings` 薄包装。
- 证据：全仓唯一 import 点是自身测试 `electron/chat/__tests__/postProcessor.test.ts:10`；功能已被 `src/store/generatedReplyPipeline.ts` 与 bridge 直接 import `src/utils/regex` 取代（阶段四收口遗留旧壳）。
- 风险：无。纯函数、不落盘。

### D2【桌面】`fitMemoryBudget`（自带待清理标记）
- `src/utils/memory.ts:573-605`；唯一调用方是 `src/utils/__tests__/memory.test.ts:258-287`，已被同文件 `fitLayeredMemoryBudget` 取代。
- 风险：无。

### D3【桌面】`MAIN_CHAT_MAX_TOKENS_LIMIT` 兼容再导出
- `src/store/chatConstants.ts:62`（再导出 `shared/modelOutputProfile.MAX_REQUEST_OUTPUT_TOKENS`），注释称"供兼容引用"，实际仅 `chatConstants.test.ts:4,35` 引用；生产全部走 `resolveRequestBudget`。
- 风险：无。

### D4【桌面】`lorebook:importJson` 旧 IPC 通道
- `electron/ipc/lorebook.ts:191-193` + `electron/preload.ts:188` + `shared/ipc-api.ts:242`。
- 证据：渲染层 `window.api.lorebook.importJson` 调用为 0（`src/pages/LorebookPage.tsx:256` 已全量走 `importJsonDetailed`；`src/lib/preview.ts:27` 只是满足类型契约的 mock）。注释担心的"已有调用方"实际不存在。
- 风险：低；三处条目同步删除即可，不涉及落盘数据。

### D5【桌面】旧 provider 单选体系
- `setActiveProvider`（`src/store/useSettingsStore.ts:53,253-262`）零调用方（已复核），UI 全走 `setActiveProfileId`；`Settings.activeProvider`（`shared/types.ts:736`、`shared/defaults.ts:5`）无任何读取方，可一并删。
- `Settings.providers`（types.ts:737）仅剩旧数据迁移（`useSettingsStore.ts:103-111`）与死方法本身两个读取点——**字段本体保留**至 profile 迁移下线，否则极旧用户升级后不自动生成连接 Profile。

### D6【安卓】`OutboxDao` 9 个死方法 + `TaskCursorDao` 2 个
- 文件：`android/app/src/main/java/com/qingyu/companion/data/CacheEntities.kt`。
- 真实消费仅 `getById / upsert / observeForSession / listPending / countLegacyPending / adoptLegacyPending / clearCompleted`；以下零调用（已复核）：`listForSession`(suspend 版,:109)、`listAll`(:115)、`updateState`(:124，F-04 起状态机改整行 upsert)、`delete(requestId)`(:127)、`clearAllCompleted`(:133)、`deleteOrphan`(:136)、`clear()`(:139，"退出时清除"有意保留 outbox)、`listPendingForDevice`(:151)、`listLegacyPending`(:159)；`TaskCursorDao.get`(:203)、`listForSession`(:209)。
- 唯一"引用"是 `OutboxRestoreTest.kt:40-56` 的 fake 因接口声明被迫 override。
- 风险：低；不动 Room schema、无需迁移。

### D7【安卓】`createGroupSession` 整条死链
- `ChatRepository.kt:187`（接口）→ `OnlineChatRepository.kt:838`（实现）→ `QingyuApi.kt:304-306`（Retrofit POST）→ `ApiDtos.kt:328-342`（`GroupSessionDto`）。
- 证据：全仓调用点 = 0（4 个测试 fake 仅 `throw UnsupportedOperationException`），UI 无入口（已复核）；`GroupSessionDto` 与 `model/Group.kt:26-40` 的 `GroupSession` 字段完全重复。
- 风险：低；PC 端点是否服务其他客户端需 PC 侧确认，但安卓侧链路可整删。

### D8【安卓】零引用资源与废弃字段
- `strings.xml` 18 条无引用（`R.string.` 与 `@string/` 双口径 0 命中）：`action_back/action_clear/action_confirm/action_send/cd_add_image/chat_attachment_voice/chat_directions_failed/chat_error_ai_failed/chat_tools_collapse/chat_tools_expand/chat_tools_title/msg_edit_clear_confirm/msg_intranet_desc/msg_version_current/msg_version_latest/title_intranet_guide/title_memory_settings/title_wipe_on_exit`——多为旧输入工具面板、旧内网引导、旧更新对话框遗留。
- `imageGenSize`（`ApiDtos.kt:107-108`，@Deprecated）：main 零读取，`SettingsOwnership.kt:97` 注明已退出同步集；"仅用于解码旧版 PC 快照"的保留理由被 `NetworkModule.kt:175 ignoreUnknownKeys=true` 证伪。钉它的测试 `SettingsSyncSerializationTest.kt:67-78` 属为已删功能保留的用例，可删；同文件 81-89"自动生图字段已下线"负向断言是有效守卫，**保留**。
- 风险：极低。

## 三、复制漂移中的双实现（已经开始不一致）

### C1【桌面】`<thought>` 剥离内联正则 ×22 处，与共享 helper 并存且已漂移
- 权威实现：`shared/thoughtMarkup.ts:5-9`（`stripVendorThinking`，覆盖 `<think>/<thinking>`）+ `src/utils/messagePostProcess.ts:294-313`（`extractThought/stripThought/stripThoughtTags`）。
- 内联复制（非测试代码 `replace(/<thought` 22 处）：`useChatStore.ts:912`、`useGroupChatStore.ts:403`、`streamController.ts:385,480`、`groupStreamController.ts:486`、`chatUtils.ts:260`、`useCharacterCreatorStore.ts:123,190`、`memory.ts:132`、`presetGen.ts:24`、`electron/bridge/ttsHandler.ts:33`、`LorebookPage.tsx:393,403`、`CharacterCard.tsx:103`、`CharacterEditor.tsx:328` 等。
- 漂移实证：内联版只删 `<thought>`，不处理 `<think>/<thinking>` 与流式未闭合；`LorebookPage.tsx:393` 已手工追加 `<thinking>` 处理；TTS 朗读路径与气泡渲染路径口径不一致。
- 处置：统一改调 `stripThought`/`stripVendorThinking`；纯行为收敛，无数据兼容问题。

### C2【桌面】翻译链路 5 份 prompt 复制，Bridge 群聊翻译是未迁移的旧内联实现
- 复制点：`useChatStore.ts:962`、`useGroupChatStore.ts:455`、`LorebookPage.tsx:426`、`electron/bridge/chatService.ts:518`、`electron/bridge/routes.ts:1770`。
- 关键：单聊 bridge 翻译已走 `MobileFacade`（`routes.ts:1197-1207` → `mobileFacade.ts:118` → `chatService.ts`），群聊翻译（`routes.ts:1748` 起）仍是旧内联 handler——预算用旧硬编码 `Math.max(256, Math.min(content.length, 4000))`（:1780）vs `translationMaxTokens`（chatService.ts:527）；取消传 `new AbortController().signal`（:1785，即不可取消）vs 注册进 generations（chatService.ts:511）。注释自认"对齐单聊"（手工对齐 = 漂移）。
- 处置：并入 facade；协议 URL 不变，风险低。这是阶段四"各端不再复制实现"漏掉的实例。

### C3【桌面】群聊【对话规则】prompt 逐字双份
- `electron/bridge/groupContext.ts:36-44` vs `src/store/groupChatContext.ts:121-129`（点名/轮询两段逐字相同；自由发言段有意差异——bridge 精简版）。
- 处置：抽共享规则字符串，保留 bridge 精简差异；风险低-中。

### C4【桌面】"新会话默认长记忆"三份实现
- `src/store/chatUtils.ts:61-85`（renderer）、`electron/ipc/chat.ts:62-80`、`electron/ipc/group.ts:60-67`（两处注释互称"对齐渲染层"）；`autoMemoryInterval: 10` 魔数散在三处 + `bridge/routes.ts:959`。
- 处置：决策表下沉 shared 单点；纯初始化参数，风险低。

### C5【桌面】`aiDone` 事件双回调轨道（onDone/onComplete）
- `electron/preload.ts:46-57` 同一事件挂两种回调；`onDone` 仍有 ~10 个真实消费方（CharacterCard、CharacterEditor、aiInputHelper、LorebookPage、PresetsPage、UsagePage、useCharacterCreatorStore、useGroupChatStore、useChatStore）——**不是死代码**，但辅助链路只用 requestId，可统一收口 `onComplete` 后解构，删一条轨道。
- 与 S8 联动：legacy 管线退役时牵连的兼容代码（`buildLegacyBodyFormatPrompt`（contextBuilder.ts:96-139）、legacy 预算分支 contextBuilder.ts:153-169 与 `groupRequestPlan.ts:53-71` 逐行雷同、`normalizeRoleplayDialoguePrefixes` 仅剩 legacy 分支调用）一并退役，见 S8 退场计划，此处不重复。

## 四、兼容垫片：本体保留，死写路径先收

### B1 `gameMasterMode`（方向开关旧名）
- 定义：`shared/types.ts:374,660`（@deprecated）。读回退真实且广泛（`shared/dialogueDirections.ts:41-45` → MessageBubble/ChatPage/GroupChatPage/dialogueDirectionRunner 等）——旧会话数据仍靠它判断开关；bridge DTO 镜像写（`routes.ts:92-95`）与接受旧 Android PATCH（:744-747,1595-1599）也是活的兼容。
- **已死部分**：`electron/ipc/chat.ts:43,134-135`、`electron/ipc/group.ts:39,148-149` 的 IPC 写白名单——渲染层零写方，可删。
- 当前安卓客户端已从不发送 `gameMasterMode`，PC 镜像的"旧客户端"窗口实际只剩历史版本；待最低版本抬升后随 S8 一并退出。

### B2 只读兼容字段（保留读、无写方）
- `Character.lorebookId`（`shared/types.ts:127` @deprecated）：`getEffectiveLorebookIds`（`src/utils/lorebook.ts:35`）为旧角色卡 JSON 真实兜底，合理保留；字段已无写方。
- `OutboxMessage.error` v4 遗留列（`CacheEntities.kt:87-88`）：唯一非空写者是死方法 `updateState`（D6），现役路径只写 `error = null`（OutboxStateMachine.kt:121），main 无真值读取。删除需 Room v8 迁移，收益小——列候选，不推荐现在动。

### B3【安卓】model/ 零读取 DTO 字段（`ignoreUnknownKeys=true` 下删除不影响解码）
- `gameMasterMode`（Session.kt:26、Group.kt:37、ApiDtos.kt:339）、`memoryCurrentState`（Session.kt:28、Group.kt:38、ApiDtos.kt:211,340）、`personaId`（Group.kt:39、ApiDtos.kt:341）、`generationKind`（Message.kt:51、Group.kt:58，仅 ProtocolContractTest:197-199 钉住）、`mentionedCharacterIds`（Group.kt:55,78，请求构造从不传、无提及 UI）、GroupChat 的 `chatMode/autoMode/maxRounds/defaultNarrativeMode/createdAt/updatedAt`（Group.kt:15-21，UI 只用 id/name/memberIds）、GroupSession 的 `createdAt/updatedAt/narrativeMode`（Group.kt:30-33）、SessionPreview.narrativeMode（Session.kt:22）、Character.creator（Character.kt:26）。
- 处置：随删随清对应契约测试断言（ProtocolContractTest、WsEventSerializationTest:110-145）；风险低。注意这些字段镜像自 PC 契约——删除属安卓侧消费面收缩，不动协议。

## 五、双轨持久化结构

### E1 Persona 文本在 Settings 存派生副本
- `syncPersonaToSettings`（`src/store/chatUtils.ts:44-54`）把 persona 的 `name/description/persona` 抄进 `Settings.userName/userDescription/userPersona`；contextBuilder（:214,260-261）与 `MobileSafeSettings` 白名单（settingsSync.ts:24-40）读的都是副本，Android PATCH 还可直改副本（settingsSync.ts:206 区段）。
- 唯一事实源是 persona 存储（`electron/ipc/persona.ts`）；编辑 persona 后不重新"选中"则副本陈旧——双写单同步的典型漂移面。
- 收口前提：bridge/Android 改读 persona 聚合视图；风险中。

### E2 settings.json 两代互不知晓的迁移系统
- 主进程版本链 `electron/services/migration.ts:21-62`（settings LATEST=2，读时升）+ 渲染层无版本启动探测链 `src/store/useSettingsStore.ts:91-217`（provider→profiles、tts/imageGen/vision 单字段→数组、authorNote 删除），两条路径都会 `settings.save` 回写（:128,216）。
- 同名字段历史上两侧都碰（`imageGenSize` 主进程删 / `imageGenModel` 渲染层删）；renderer 迁移每次启动全量探测（稳态 no-op）。
- 收敛方向：renderer 三段迁移搬进 `MIGRATIONS.settings` 链（v2→v3），renderer 只留兜底读取；删除前需确认最低支持版本。

### E3 Bridge 设置端点新旧双轨（有意保留，列此供决策）
- `settingsSync.ts:5` 注释明示保留旧 `GET/PATCH /api/v1/settings`；`routes.ts:264-360` 仍注册旧端点并**直写文件**（:359 注释"不经 settings:save IPC"）——同一份文件两条写路径 + `settingsChangeBus` 防回环。
- 旧轨道服务未升级 Android；待最低客户端版本 ≥ 快照 v2 可整体下线。QR v2/legacy 双格式（PhoneConnectionSection.tsx:129-385、bridge/index.ts:246,291,387）属"活的兼容"，现在不动。

## 六、服务端与跨端镜像

### R1 relay-server ↔ PC 路由白名单同表两份手抄
- `relay-server/src/routing/routeAllowlist.ts:3-11`（8 条 method+path+cacheable+queueWhenOffline）vs `electron/relay/relayRpcDispatcher.ts:15-28`（matchRelayRpc 手工镜像）。
- 两端都已 import `shared/relayProtocol.ts`，路由表完全可下沉 shared；当前靠注释约定同步，漂移会静默 404/403。风险中（需 shared 模块同时供两侧 TS 消费，可行）。

### R2 `GET /relay/v1/server/info` 无调用方
- `relay-server/src/app.ts:32-36`；android/electron/src/tests 全量 grep 零调用，capabilities 字面量零命中，安卓探测走 `/relay/v1/health/ready`（RelayConnectionProbe.kt:8）。允许匿名访问，更像预留诊断面。
- 删除前与运维确认是否被 Prometheus/人工探测使用。

### R3【relay-server】错误响应 helper 三份小复制
- `http/pairingRoutes.ts:107-109`、`http/spaceRoutes.ts:71`（另一份 rateLimited）、`app.ts:38` 与 `http/bridgeRoutes.ts:18` 逐字相同的 404 字面量。可收敛单模块，风险≈0。

### R4 server/ 与 relay-server/ 之间：无复制（如实报告）
- server/（Express CJS，8 源文件）无任何 WS/配对/消息转发/AI 代码；两服务鉴权（HS256+bcrypt vs EdDSA 设备令牌）、限流、错误信封、CORS 完全不同。无"本可下沉 shared 却各存一份"的项。server/ 亦未发现旧协议兼容分支（`version.js:22-32` PC+安卓双版本字段是现役设计）。

## 七、已移除功能残留核查（干净，仅迁移器合理存续）

- **自动生图/旧生图配置**：UI 与类型层已净（`shared/types.ts:770` 只剩新结构 `imageGenModels`）；实体残留仅 `electron/services/migration.ts:93-152`（v1→v2 把全局 `imageGenSize` 转 ComfyUI overrides，真实旧数据兼容，保留至存量清零）与 renderer 侧平行清理（归 E2）。`MobileSafeSettings.schemaVersion=4` 是协议版本占位，非冗余。
- **族裔/ethnicity**：三端 + shared/electron/src 全仓 0 命中，已净删。
- **【判定】/【可选行动】游戏主持格式**：仅剩解释性注释（groupStreamController.ts:919）与防回归断言；干净。
- **注释腐化**：`android/.../model/Message.kt:58-63`、`Group.kt:65` 仍写"移动端统一走 Markdown 兼容渲染、字段先落库"，与 RoleplayBlockList 已在用（MessageBubble.kt:429-439）的事实矛盾——随 B3 清理时更正。
- **AppContainer/装配层**：安卓所有 `val` 成员逐一 grep 均有消费，无死装配；`PairingQrV2`/`ServerConnectionCompatTest`/`OutboxSchemaV*Test` 属有意的协议/落盘兼容基线，不算冗余。

## 八、仓库层面

- `dist`、`dist-electron`、`release-v2`、`output`、`合并前备份-20260820`、`models` 均已被 `.gitignore` 覆盖，不属仓库内容量问题；`png/` 截图被 README.md:33-35 引用，保留。
- 未跟踪的本地脚本 `scripts/decrypt-api-key.cjs`、`evaluate-generation.ts`、`generation-baseline.ts` 不在 git 内（S9 报告点名的 decrypt-api-key.cjs 仍在工作区），需决定归档/正式化/移除，但不阻塞仓库。

## 九、建议实施顺序

| 批次 | 内容 | 前提 |
|---|---|---|
| ① 无风险删除 | D1–D8（桌面四件套 + 安卓死 DAO/死链/零引用资源），每项连带其钉住测试 | 无 |
| ② 行为收敛 | C1 thought 剥离统一 → C2 bridge 群聊翻译并入 facade → C3/C4 共享规则与决策表下沉 | 无数据兼容问题，纯重构；每步补对照测试 |
| ③ 死写路径清理 | B1 IPC 写白名单、B3 安卓零读取字段（随删随清契约测试）、更正 §七注释腐化 | 保留全部读回退 |
| ④ 计划性收口 | E2 迁移并链、E1 persona 副本、E3 bridge 旧端点、R1 路由表下沉 shared、C5 onDone 轨道 | 最低支持版本 / 协议窗口决策 |

不动清单（活的兼容）：S8 legacy 管线本体（按其既定三步退场）、QR v2/legacy 双格式、PairingQrV2、生图 settings 迁移器本体、`OutboxMessage.error` 列（需 Room v8 才划算）、`Settings.providers`（待 profile 迁移下线）、`lorebookId` 读兜底。
