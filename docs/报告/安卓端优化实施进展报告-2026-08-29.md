# 安卓端优化实施进展报告

> 报告日期：2026-08-29
> 关联文档：`docs/已移除/安卓端优化实施文档-2026-08-29.md`（实施依据；文档已归档，阶段 A—G 已全部收口）
> 实施范围：`android/`、`electron/bridge/`、`shared/`、`scripts/`
> 实施方式：主代理统筹 + 子代理按文件所有权并行实施，逐阶段门禁验证

---

## 1. 总体进度速览

| 阶段 | 内容 | 状态 |
| --- | --- | --- |
| 基线勘察 | 核实文档 3.3 节六项"当前事实"、修复构建门禁环境 | ✅ 完成 |
| 阶段 A | P0 基线收口（A01–A06） | ✅ 完成 + 模拟器实机验收通过 |
| 阶段 B | 连接协调与延迟优化（B01–B07） | ✅ 代码与单测完成，诊断 UI 待接线 |
| 阶段 C | 设置同步 v2（PC + Android 双侧） | ✅ 代码与单测完成，集成缺口待补 |
| 阶段 D | 配对 QR v2（D01/D02/D04 PC 侧、D03 解析器） | ✅ 完成（D03 解析器未接 PairingViewModel） |
| 集成收口 | WS 分发 / QR 接线 / DI 共享实例 / 诊断 UI | ⏳ 进行中（本次会话在此处暂停） |
| 阶段 E | MainShell 与 UI 统一状态 | ⬜ 未开始 |
| 阶段 F | 事件一致性 / Outbox v5 / 可靠发送 | ⬜ 未开始 |
| 阶段 G | 性能 / Release / CI 收口 | ⬜ 未开始 |

版本已对齐：PC `package.json` = Android `0.16.6 / versionCode 7` = README/CHANGELOG 版本行，`node scripts/version.mjs status` 门禁通过并已挂入 CI。

---

## 2. 环境关键结论（后续会话必读）

1. **Gradle 测试环境故障已定位并绕过**：项目路径含中文"酒馆"导致 Gradle 测试工作进程 classpath argfile 编码错乱（守护进程 UTF-8 写、JDK 17 启动器按 GBK 读），表现为 `ClassNotFoundException` 全量假失败。**所有 gradle 命令必须追加**：
   ```
   -Dorg.gradle.jvmargs="-Xmx2048m -Dfile.encoding=GBK"
   ```
   这是文档 A-06 预警的环境故障，不是项目测试失败。
2. **测试模拟器**：MuMu 12，`adb connect 127.0.0.1:5557`（详见工作区根目录《模拟器环境-备忘录.md》）。新 APK（0.16.6/build7）已覆盖安装旧版 0.1.5/build6 成功，证明版本单调递增有效。
3. **adb 截图注意**：Git Bash 下 `pull /sdcard/...` 路径会被转换出错，用 `exec-out screencap -p > "$TEMP/x.png"`；uiautomator dump 需 `MSYS_NO_PATHCONV=1`。
4. 本机 JDK 17、Node 24、pnpm 11.6、Android SDK（`D:\Android\Sdk`）齐备；仓库是 git repo，**全程未 commit/push**（按约束留给用户）。

---

## 3. 阶段 A 详情（已完成 + 实机验收）

| 任务 | 交付 | 验证 |
| --- | --- | --- |
| A01 版本单源 | `scripts/version.mjs` 扩展 `status/android/release`；TOML 同步 versionName+versionCode（0.16.5/6→0.16.6/7）；README/CHANGELOG 版本行对齐；ci.yml 加版本一致性门禁 | `version:status` exit 0；19 项构造性断言（no-op 逐字节不变、code 严格递增、漂移 exit1） |
| A02 主题选择 | 新建 `ui/components/SettingsChoiceSheet.kt`（`SettingsChoiceRow<T>` + ModalBottomSheet），删除失效的 `OptionPickerRow`；主题/字体/间距三处切换；选当前项不重复写入 | **模拟器实测**：面板弹出三选项、选"浅色"整页变浅、force-stop 冷启动后仍为浅色 |
| A03 启动直达 | `data/StartupRepository.kt`（只读快照，不碰网络）、`ui/startup/`（四态决策+设备选择页）、`CompanionNavHost` startDestination 改 STARTUP；active 丢失→选择器不自动选；token 损坏→"需要修复连接"标记 | 9 项纯函数单测；**模拟器实测**：已配对冷启动直达会话列表，无配对页闪烁 |
| A04 端点规范化 | `network/connection/ConnectionEndpoint.kt` + `EndpointNormalizer.kt`：IPv4 私网/100.64/10/localhost、IPv6 loopback/ULA/link-local（zone id 编码）、`.local`；公网+LOCAL_CLEARTEXT 构造抛异常；IPv6 URL 加方括号 | 15 项单测覆盖文档测试表全部 10 行 + 清洗用例 |
| A05 明文策略 | `network_security_config.xml` base-config 临时放行（注释说明阶段 D TLS pinning 后收紧）；`RedirectPolicyInterceptor` 拒绝跨安全级重定向、跨主机剥离 Bearer | 11 项单测（含 MockWebServer 端到端） |
| A06 通知权限 | 删 MainActivity 启动即申请；设置页开启通知→用途说明→系统请求；永久拒绝→"前往系统设置"；首次后台生成非阻塞提示；渠道与 App 开关统一门控 | 7 项纯函数单测；**模拟器实测**：冷启动无权限框，`POST_NOTIFICATIONS granted=false` |

**阶段 A 门禁**：25 套件 / 220 测试 0 失败；`lintDebug` 通过；`assembleDebug` 成功；APK 覆盖安装 + 核心路径模拟器走查通过。

**已知遗留（A 阶段）**：
- CHANGELOG 0.16.6 条目正文仍是 0.1.5 内容，发布前需人工补写。
- OkHttp 4.12 不支持 link-local zone id URL（实测），阶段 D 升级 OkHttp 5 或剥 zone。
- `ui/components/ServerImage.kt:25` Coil 仍硬编码 `http://` 拼串，未收敛到 `httpBaseUrl(endpointOf(conn))`。
- 通知 deep link（进会话）本就不存在，未实施。

---

## 4. 阶段 B 详情（代码完成，UI 接线待做）

新建 `network/connection/`：`ConnectionState.kt`（10 态 + 8 FailureReason + 4 RepairReason）、`ConnectionCoordinator.kt`（状态收敛 + ReconnectBackoff 0/1/2/4/8/15/30s ±20% jitter）、`ConnectionAttempt.kt`（ConnectionLease generation）、`ConnectivityObserver.kt`（registerDefaultNetworkCallback，250ms 去抖）、`EndpointProbe.kt`（候选去重+上次成功立即探+其余 150ms 并发≤3+2s 超时+serverId 匹配胜出）、`ConnectionMetrics.kt`（环形 100 条 JSON-lines，不含敏感数据，`snapshotForDiagnostics()` 待 UI 消费）。

改造：`NetworkModule` NetworkStack 化（共享 Dispatcher/ConnectionPool，API 缓存键 `deviceId|endpointUrl|tokenHash`，公开签名全兼容）；`ConnectionManagerImpl` 变纯委托 facade；`WsClientImpl` 的 `connection === target` 全部替换为 generation+deviceId 语义；`ServerConnection` 增 6 字段全带默认值（旧 JSON 无损兼容解码）；`AppContainer`/`CompanionApp` 装配 coordinator、Coil token 按 URL host:port 归属。

**新增 7 套件（47 用例），全量 34 套件 / 301 测试 0 失败。**

**待接线**：① 设置页"连接诊断"卡片消费 `snapshotForDiagnostics()`；② PairingViewModel 一行接 `markAwaitingApproval()`；③ 真机验证清单（Wi-Fi 关开 ≤300ms 探测、A/B/A 快切不串台、20 次断连无重复 socket）。

---

## 5. 阶段 C + D 详情（双侧完成，集成缺口待补）

### PC 侧
- `electron/bridge/settingsSync.ts`：snapshot 15 字段白名单（钉死排除 apiKey/凭据/PC 显示偏好）、revision = sha256(stableStringify(MobileSafeSettings))、字段类型+范围验证表。
- `routes.ts`：`GET/PATCH /api/v1/settings/snapshot` 已挂载（409 `{error:'settings_conflict',current}`、appliedFields/rejectedFields、旧 `/settings` 逐字节不变）；`/server/info` 增量 serverId + capabilities。
- `electron/services/settingsChangeBus.ts` + `ipc/settings.ts` 统一出口发事件，BridgeService 订阅→WS `settings:updated`，Android 来源防回环。
- `electron/bridge/identity.ts`：`bridgeIdentity.json` 稳定 serverId（D01）。
- QR v2（D02）：`shared/pairingQr.ts` 载荷构造/解析（version:2/scheme/serverId/capabilities/pairingCode/expiresAt+5min/endpoints/certificatePin:null），UI 默认 v2 + "旧版兼容"切换输出 legacy；pairingCode 一次性+过期轮换。
- mDNS TXT（D04）：serverId/apiVersion/pairVersion/tls/displayName。
- **验证**：`pnpm check` exit 0、`pnpm test` 191 文件 / 1790 用例全过（新增 47）；lint 仅剩 1 个先前已存在的错误（`embeddingSemanticSearch.test.ts` THEME_DIMS，非本轮引入）。

### Android 侧
- `model/SettingsSync.kt` DTO（全带默认值兼容缺字段）；`QingyuApi` 两新端点；`CompanionError.Conflict`（仅 `settings_conflict` 映射，保留 current）；`WsEvent.SETTINGS_UPDATED` + `CompanionEvent.SettingsUpdated`。
- `data/settings/`：SettingsSyncRepository（六态 status、capability 门控 v2/legacy、deviceId 隔离缓存、断线 Failed 不排队、切 PC 清内存再加载、Mutex 串行防串台）；SettingsCacheStore（filesDir 纯文件，未动 Room）；SettingsOwnership（白名单强制过滤，本机项绝不进 PATCH）。
- UI（C-08）：每行四态（Saving 进度/Synced 绿勾 3s/Failed 红重试/Conflict 底部面板 PC 值 vs 本次修改）；滑块 300ms debounce 合并；"仅本机"/"同步到当前 PC"分组标签；legacy 提示"旧版 PC：设置仅支持直接保存"。
- D03：`model/PairingQrV2.kt` 双格式纯解析（逐项校验，失败收敛 Invalid(reason)）。
- **新增 33 用例全过**；与 PC 契约已逐字段比对一致。

### 集成缺口（本次会话正在处理，未完成）
1. `WsClientImpl.handleFrame` 未把 `settings:updated` 帧映射为 `CompanionEvent.SettingsUpdated`（文件归阶段 B 战场，已确认缺）。
2. `PairingViewModel` 仍用旧 `PairingQrPayload` 解码，未接 `parsePairingQr`（QR v2 扫码链路未通）。
3. `AppContainer` 未接线共享 `settingsSyncRepository`（现默认工厂自建同构仓库，功能可用但跨页状态不连续；接线 5 步清单已写在 `SettingsSyncDependencies.kt` KDoc）。
4. `ServerConnection.endpoints` ← QR v2 endpoints 写入衔接未做。
5. rejectedFields 行级"部分成功"反馈未细化。

---

## 6. 当前代码状态与验证快照

- Android：34 套件 / 301 测试 0 失败；lintDebug 通过；assembleDebug 成功；APK 0.16.6/build7 已装 MuMu。
- PC：pnpm check/test 全绿（1790 用例）；lint 1 个存量错误待清（非本轮）。
- 版本门禁：`node scripts/version.mjs status` exit 0。
- 全部改动**未 commit**（工作树含本轮 28+ 文件新增/修改 + 其他并行工作流的存量改动）。

## 7. 下一步计划（按依赖顺序）

1. **集成收口**（当前中断点）：补 WS 分发、PairingViewModel 接 QR v2、AppContainer 五步接线、诊断 UI 卡片、Coil URL 收敛 → 全量门禁（Android test/lint/assemble + pnpm check/lint/test/build）→ 模拟器端到端验收（新 PC 配对 QR v2 → 设置修改双端同步 → 409 冲突 UI → 断网恢复）。
2. **阶段 E**：MainShell（底部三栏/平板 Rail）、LoadState 统一异步组件、设置页 sections 拆分、输入区布局、滚动锚点与搜索匹配列表、360dp/字体 200%/TalkBack 适配。
3. **阶段 F**：task v2 cursor（Room 新表）、`fallbackToDestructiveMigration` 改显式 migration、Outbox schema v5（deviceId/状态机 8 态）、可靠发送 UseCase 拆分、强杀/断网/切 PC 矩阵测试。
4. **阶段 G**：benchmark module、Baseline Profile、R8+签名+SHA-256 发布流水线、契约测试（PC fixture ↔ Android DTO 双向）。
5. 发布建议节奏（文档第 4 节）：0.16.6-p1（A）→ 0.17.0（B+D）→ 0.17.1（C）→ 0.18.0（E）→ 0.18.1（F+G）。

## 8. 风险与待决事项

- **TLS/明文**：当前 base-config 临时放行明文是短期方案，阶段 D 的 TLS 证书+指纹 pinning 未实施前，公网误配风险由应用层 EndpointPolicy 兜底。
- **OkHttp 4.12 link-local**：fe80 带 zone id 的 URL 实测无法连接，需升级 OkHttp 5 或剥 zone（影响 IPv6 link-local 场景）。
- **revision 哈希口径**：PC 实现按"MobileSafeSettings 子集"哈希（文档公式写 toApiSettings 全量，全量会导致 PC 改外观令手机 revision 作废造成假 409，与 C-03 意图矛盾）——已按文档正文意图实现，需评审确认。
- **Room 破坏性迁移**：阶段 F 前 `fallbackToDestructiveMigration()` 仍在，Outbox 扩充必须先改显式 migration（文档 F-05 红线）。
- CHANGELOG 正文、deep link、异步配对协议（202 轮询）为已记录的顺延项。

---

*本报告由实施会话自动生成，对应实施文档第 5–8 节（阶段 A/B/C/D）执行结果。*

---

## 9. 续报（2026-08-31 会话收口）

> 本续报记录 2026-08-30 中断点之后的完整收口：集成缺口清零、阶段 E/F/G 剩余项落地、模拟器端到端验收通过。

### 9.1 集成缺口清零（前报告第 5 节 5 项全部关闭）

| 前报告缺口 | 关闭方式 |
| --- | --- |
| ① WS settings:updated 未映射 | 已映射 `WsClientImpl.handleFrame → CompanionEvent.SettingsUpdated`（WsClientImpl.kt:323-331） |
| ② PairingViewModel 未接 QR v2 | 已接 `parsePairingQr` 双格式解析 + expiresAt/pairingCode 校验 + endpoints 写入 ServerConnection（PairingViewModel.kt:231-253） |
| ③ AppContainer 共享 SettingsSyncRepository 未接 | 按 SettingsSyncDependencies KDoc 五步接线完成（AppContainer.kt:130-143），bind 随 activeFlow 切换 |
| ④ endpoints ← QR v2 写入未做 | 与 ② 一并完成 |
| ⑤ rejectedFields 行级反馈未细化 | 经 RejectionCapturingApi + 行级 `failureDetail` 投影完成（SettingsViewModel.kt:116-162 / PcConversationSection.kt） |

另：连接诊断卡片已接线（ConnectionDataSection.kt:137-220 消费 `snapshotForDiagnostics()`）；ServerImage 已收敛到 `httpBaseUrl(endpointOf(conn))`；PairingViewModel 已调 `markAwaitingApproval()`；通知 deep link 已实现（MainActivity → NotificationDeepLink holder → 启动决策完成后消费，含冷启动与 onNewIntent 两路径）。

### 9.2 阶段 E 剩余项（代理并行完成）

- **聊天输入区（E-04）**：附件菜单（ModalBottomSheet：续写/润色/图片）、失败动作（重试发送/仅重试 AI/离线等待可取消）接入 PendingBubble。
- **滚动与搜索（E-05）**：`ChatScrollState.kt` 纯函数跟随状态机（上滑 3 项阈值脱离、chunk 新增计数、回到底部清零）+ "回到最新"计数浮层；搜索改为匹配 messageId 列表 + 上一项/下一项环形导航 + 高亮（`SearchUtils.matchingMessageIds/navigateMatch`）。
- **文案抽取（E-06）**：聊天侧 13 文件硬编码清零（新增 92 条 strings，复用既有 30+ 条）；全仓 UI 层用户可见硬编码为 0。
- 非聊天页 AsyncStates 迁移（上一波）：Usage/Announcements/Characters/Groups/CharacterSessions/Pairing 全部五态化 + 新增 5 个投影测试类。
- 门禁：476 tests 0 失败；lintDebug 0 Error（告警 103 条均为存量）；assembleDebug 通过。

### 9.3 阶段 F 全部落地（含测试修复）

- **Room v6**：`task_cursors` 表（deviceId+taskId 复合主键）+ MIGRATION_5_6 显式迁移 + schemas/6.json 导出；cursor 三规则（顺序应用/重复忽略/缺口补拉）纯函数化（TaskCursorLogic.kt）。
- **Task v2 客户端**：TaskEventEnvelopeDto 等契约 DTO 对齐 PC taskRoutes/taskWsAdapter；`task:subscribe` 能力门控 + onOpen 自动重发 + task:* 帧分发；v1 ai:* 兼容链路保留，同一 requestId 单链路不双发。
- **F-03 事件丢弃**：`ConnectionMetrics.increment` 计数器 + `ws_event_drop` 计数 + `reconciliationRequests` 对账流（chunk 可丢、done/error/task 生命周期触发对账，AppContainer 接线到 refreshSession）。修复测试：容量 0 时 tryEmit 语义实测（零订阅者快速通道成功；有订阅者必然失败），测试改为挂起订阅者模式。
- **Outbox 八态状态机**：queued/sending/user_committed/awaiting_ai/completed/failed_send/failed_generation/cancelled；发送时序修正（awaiting_ai 不再前置）；v5 列全部接线（taskId/remoteUserMessageId/退避/错误码）；legacy 归属修复（repairLegacyOutbox 启动一次性回填 + pendingLegacyOutbox 提示）；UseCase 拆分（SendMessage/RetrySend/RetryGeneration/CancelQueued/ObserveOutbox）；restoreOutbox 恢复矩阵（sending→queued 同键重发、user_committed/awaiting_ai 不重发用户消息、task 快照终态落库）。
- 修复 19 个失败测试：OutboxRestoreTest Fake 失效跟踪（Room 构造期急切创建，返回真实 InvalidationTracker）；ChatViewModelTest Fake 补齐发件箱建模（与生产语义一致）；OutboxTest 恢复矩阵断言与生产一致；状态机收紧"成功转换清除错误字段"不变量。
- **模拟器端到端验证**：发送消息 → Outbox 行 `completed | dev-a2f0... | task-L1SLFbav`（v2 链路全流程）。

### 9.4 阶段 G 落地（代理交付）

- **契约测试双向 fixture**：`shared/fixtures/` 单处权威（settings_snapshot/settings_patch_request/pairing_qr_v2/task_event_envelope）；PC 侧 `contractFixtures.test.ts`（13 用例）+ Android 侧 `ProtocolContractTest.kt`（8 用例，副本字节级比对一致）。
- **R8/Release**：proguard 补齐 Room/Coil/Media3/Retrofit keep；release `isMinifyEnabled+shrinkResources`；signingConfig 从环境变量读（缺失回退 debug 签名仅限本机冒烟）；`assembleRelease` 成功（APK 4.0M）。
- **CI**：ci.yml 新增 `android` job（版本门禁→单测→lint→assembleDebug→artifact）；release.yml 新增 `android-release` job（secrets 显式校验→assembleRelease→SHA-256→artifact）。
- **Benchmark 骨架**：`android/benchmark/` 模块（com.android.test + macrobenchmark 1.2.4），Startup/SessionOpen/LongChatScroll 三个基准类；插件兼容性结论：androidx.benchmark gradle 插件不支持 com.android.test（AGP 8.5.2 钉版），按官方模板用运行时依赖；Baseline Profile 接入步骤已注释，待真机收集。
- 修复 `chatHistory.test.ts` 整仓并发 5s 超时 flaky（动态 import bridge 全链路，放宽至 20s；单跑 2.3s）。

### 9.5 最终门禁快照（2026-08-31）

| 门禁 | 结果 |
| --- | --- |
| Android testDebugUnitTest | 476 tests / 0 失败 |
| Android lintDebug | 0 Error（103 条存量 Warning） |
| Android assembleDebug / assembleRelease | BUILD SUCCESSFUL |
| pnpm check | exit 0 |
| pnpm test | 192 文件 / 1803 用例全过 |
| version:status | ✔ 0.16.6 / build 7 四源一致 |
| 模拟器冒烟 | 启动直达 / 三栏 Shell / 设置 sections + 连接诊断 / 通知权限延迟 / 端到端发送（Outbox completed + taskId） |

### 9.6 已知遗留（顺延项，非本轮范围）

- 异步配对协议（POST /auth/pair-requests 202 轮询）未实施（文档标注非 QR v2 首发阻塞项）。
- Baseline Profile 真机收集、CI secrets（ANDROID_KEYSTORE_*）、版本接口 downloadUrl/checksum 发布。
- PC 侧 session:updated 未带 revision（Android 侧已留 TODO 注释，待 PC 补充后做 revision 跳变补拉）。
- OkHttp 4.12 link-local zone id URL 不支持（需 OkHttp 5 或剥 zone，影响 IPv6 link-local 场景）。
- 全部改动仍未 commit/push（按约束留给用户）。
