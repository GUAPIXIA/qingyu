# PC 端动态上下文与生成规划器重构实施主计划（修订版）

> 日期：2026-09-13（2026-09-13 晚修订：清理期 A/B/C 三线已完成，见 §1.1 与 §16）
> 状态：待实施；用于任务拆分、代理分派和验收，不作为另一套架构另行实现  
> 本期范围：PC 单聊、PC 群聊、PC 辅助生成、Bridge 兼容、模型运行档案、上下文分配、PC 设置迁移  
> 已知非本期范围：Android UI 与 Android 生成入口接入；但共享类型、协议与构建不得回归  
> 上游依据：[对话输出弹性约束与稳定收尾实施方案](./归档/对话输出弹性约束与稳定收尾实施方案-2026-09-12.md)、[阶段 7 异常链路与跨端收口方案](./对话输出弹性约束阶段7异常链路与跨端收口方案-2026-09-12.md)、[阶段 8 推理门控与降级闭环方案](./对话输出弹性约束阶段8推理门控与降级闭环方案-2026-09-13.md)  
> 前置清理：[生成链路重构实施前遗留清理实施方案](./生成链路重构实施前遗留清理实施方案-2026-09-13.md)（A/B 线实施报告：[A/B 线实施报告](../报告/生成链路重构实施前遗留清理-A_B线实施报告-2026-09-13.md)）  
> 冲突优先级：推理控制、空正文恢复、重试次数、会话熔断以阶段 8 为唯一事实来源；终止状态机、补尾和 legacy 退出以阶段 7 为唯一事实来源；本文负责把后续动态上下文工作接到现有实现上并给出执行顺序。

---

## 1. 执行结论

本次工作不是“再造生成规划器 V2”，而是完成现有规划器的闭环并逐步扩展其职责。

必须按以下顺序执行：

1. 先完成阶段 8：推理门控、运行时探测、流式提前中止、空正文至多一次降级重试和会话熔断。
2. 同批打通生成观测的回读，让现有 `recentReasoningTokens` 真正进入 `resolveRequestBudget`；统计值只帮助无可靠门控的路径估计和止损，不取代服务端门控。
3. 让剧情方向入口停止直接传 `1536`，收编到现有后台任务档案、统一预算和阶段 8 的 `off` 门控。
4. 完成阶段 7.4 的真实样本灰度；在 legacy 退出门槛未满足前，不新增 `generationPlannerV2` 开关，也不删除旧回滚链路。
5. 阶段 8 稳定后，再扩展现有 `ModelOutputProfile` 的能力元数据，并让上下文候选分配器先影子运行、后分层切换。
6. `8192` 通用安全上限在门控和实测完成前保留。是否提高或删除必须通过独立决策门，不属于本方案的预设结论。
7. 动态记忆、世界书与历史分配按“记忆 → 世界书 → 历史压缩”的顺序迁移；每一步都复用同一个输出预留值并可单独验收。
8. 最后迁移 PC 设置与预设。只有 500 次 unified 有效生成、阶段 8 指标和跨端 fixture 同时达标，才允许退出 legacy 和删除兼容代码。

按 W0–W11 的区间求和，预计工作量约 **39–61 工程人日**，另加真实模型灰度所需的自然样本周期。2–3 名实现者并行时，受 W3→W5、W7→W9 等串行关键路径约束，预计 5–8 周；任何排期都不得把“收集 500 次有效生成”伪装成可在单元测试中完成的开发任务。

### 1.1 前置清理已完成（2026-09-13 更新）

前置清理方案（[链接](./生成链路重构实施前遗留清理实施方案-2026-09-13.md)）的 A/B/C 三线已实施完成，本文开工不再有前置欠账：

**A 线（安全与发布门槛）**：P0-A ZIP 恢复目录穿越、P0-B 备份泄露凭据均已关闭并有服务级回归测试；
`safePath()` 已在生产路径真实调用；`electron/services/backup.ts`、`electron/utils/pathGuard.ts`、
`electron/bridge/auth.ts` 有按路径覆盖率门禁；A4 四项发布门槛（备份原子性、设置保存状态、MCP 弹窗可达性、
侧栏底部可达性）已关闭。详见 [A/B 线实施报告](../报告/生成链路重构实施前遗留清理-A_B线实施报告-2026-09-13.md)。

**B 线（架构解耦与迁移收敛）——直接影响本文的工作方式**：

1. **`electron/**` → `src/**` 生产反向依赖已清零**，并有 `eslint.config.js` 的 `no-restricted-imports` 门禁防回归。
   主进程/Bridge 需要的上下文、正则、记忆、世界书、收尾、识图等实现已下沉到 `shared/chat-core/`。
2. **`shared/chat-core/contextBuilder.ts` 是本文 W7–W9 的改造落点**；`src/context/contextBuilder.ts` 与其余
   约 19 个 `src/**` 文件仅为过渡 re-export 垫片（既有渲染层引用零改动兼容）。**对 contextBuilder 的任何改动必须落到
   `shared/chat-core/contextBuilder.ts`**，不要改垫片；垫片可用一次性 codemod 在 W7 前收敛。
3. **settings 迁移已收敛为单一链**：`LATEST_VERSION.settings = 3`，原渲染层三段启动探测迁移已并入
   `MIGRATIONS.settings`，renderer 只保留只读兜底。**W10 的产品语义迁移挂 `v3 → v4`**，不得新增第二条链。
4. `electron/utils/variables.ts` 手抄副本已删除；relay-server 错误响应已收敛到
   `relay-server/src/http/relayErrors.ts`（与本文无关，仅记录）。

**C 线（提示词与观测基线）**：

1. C1 连续性约束（"不得与最近对话的既有事实、已完成动作或已说过的信息矛盾或重复"）已落在
   `shared/chat-core/contextBuilder.ts` 的正文协议构造点（`buildMainChatOutputPrompt`）。
   C2 群聊人称表述（点名/轮询）已收敛到 `shared/groupChatPrompt.ts` **单一来源**，
   `src/store/groupChatContext.ts` 与 `electron/bridge/groupContext.ts` 改为共用同一构造，
   `shared/narrativeMode.ts` 的群聊叙事边界同步；W4/W5 改动同一批提示词时在其基础上叠加，不推翻。
2. C3 `scripts/evaluate-generation.ts` 的 thought 判定口径已与 `shared/thoughtContract.ts` 强契约（每轮必须且只输出
   一组 `<thought>`）一致；**真实模型出现率/合规率复测数字仍缺**（需模型额度），是 G2 第 2 条的证据项之一。
3. C4 有效生成口径已冻结为可复用纯函数：`shared/generationBaseline.ts`（`computeValidGenerations`、
   `groupKey`/`buildGroupStats`），`scripts/generation-baseline.ts` 已正式化并输出「有效生成口径与计数」一节；
   **G1/G2 直接复用该口径，不再重新定义 500 次分母**。

**仍然挂起（不阻塞本文 W0）**：最低支持版本决策（决定 B3 的 E1/E3 与 renderer 兜底移除）、
R2 运维确认、P1-03/P1-04 实机键盘与窗口高度复验。这些项已在清理期报告 §5.3 登记。

---

## 2. 已确认事实与故障边界

### 2.1 真实故障证据

本地 `generation-observations.jsonl` 中 `taskType === 'direction'` 共 15 条：

- 6 条 `bodyVisibleChars = 0`；
- 这 6 条的 `reasoningTokens` 都等于 `requestedMaxTokens = 1536`；
- 这 6 条均以 `terminationCause = provider_length` 结束；
- 即方向任务当前样本中的空正文失败率为 40%。

本地对照评测还显示：

- `max_tokens = 4000` 时，推理消耗 3981，正文仅 50 字符；
- `max_tokens = 8000` 时，推理消耗 8000，正文为 0。

这证明共享窗口模型的推理会随上限弹性增长。单纯把 `1536` 改成 `4096`、`8192` 或更大值，不构成修复。

### 2.2 当前唯一直接绕过统一预算的已知入口

`src/store/dialogueDirectionRunner.ts` 直接使用 `DIALOGUE_DIRECTION_MAX_TOKENS = 1536`，并传 `reasoningMode: 'disabled'`。聚合端若静默忽略关闭推理参数，该请求仍可能让推理吃满全部输出。

Bridge 方向入口 `electron/bridge/dialogueDirections.ts` 使用同一常量，也必须在 PC 单聊修复验证后对齐；不能只修桌面渲染进程而留下 Bridge 漂移。

### 2.3 当前限制的性质必须分清

| 项目 | 当前来源 | 性质 | 本期处置 |
|---|---|---|---|
| `MAX_REQUEST_OUTPUT_TOKENS = 8192` | `shared/modelOutputProfile.ts` | 通用运行安全阀 | 暂保；阶段 8 和能力验证后单独决策 |
| 模型档案 `outputLimit = 8192` | `shared/modelOutputProfile.ts` | 内置能力回退/保守上限 | 改为带来源与置信度的能力值，但不在首批盲目放大 |
| DeepSeek 推理默认 3072、最高 4096 | 同上 | 无门控时的估计与钳制 | 接入实测样本；可靠门控在场时以门控为准 |
| 长记忆 `min(800, budgetBase*0.1)` | `shared/chat-core/contextBuilder.ts` | 固定注入上限 | 在上下文分配阶段移除 |
| 世界书 30%、0.4/0.9 分桶 | `shared/chat-core/chatConstants.ts` | 固定分配策略 | 影子对比后迁移为统一候选分配 |
| 方向 1536 | `shared/dialogueDirections.ts` | 入口硬编码请求上限 | 第一批收编到统一预算与 `off` 门控 |
| 记忆约 6293 | `resolveRequestBudget(2500)` 的派生结果 | 动态派生值 | 不作为“待删常量” |
| 压缩约 3918 | `resolveRequestBudget(600)` 的派生结果 | 动态派生值 | 不作为“待删常量” |
| 尾部修复约 3418 | `resolveRequestBudget(200)` 的派生结果 | 动态派生值 | 不作为“待删常量” |
| 标题、结构最小长度 | 后台任务档案/解析器 | 有效性下限或软目标 | 可保留；不能混称模型硬上限 |

### 2.4 已知平台债务

Android 暂不接入本期 PC 的门控和动态上下文，因此 Android 主对话在共享推理窗口模型上的空正文风险不会因本期自动消失。验收描述必须写成：

- Android 编译、协议解析和现有行为未被破坏；
- PC 新字段对旧 Android 可选且向后兼容；
- Android 仍有同类生成风险，进入下一期明确待办；
- 不得写成“Android 问题已解决”或“多端已统一”。

---

## 3. 现有基础设施盘点

### 3.1 已完成，必须复用

| 目标概念 | 现有实现 | 已有能力 | 实施约束 |
|---|---|---|---|
| 主对话任务意图 | `shared/responsePolicy.ts` 的 `ResponsePolicy` | 简短/适中/展开/自动、用户显式意图、场景系数、近期正文节奏 | 不新建同义 `GenerationIntent` |
| 后台任务意图 | `shared/backgroundGeneration.ts` 的 `BackgroundGenerationProfile` | memory/compression/title/direction 的任务差异与推理策略 | 扩展任务档案，不复制一套枚举 |
| 输出规划 | `shared/modelOutputProfile.ts` 的 `resolveRequestBudget` | 正文预算、推理余量、模型上限、用户硬上限、`riskNotice` | 它是唯一输出预算计算入口 |
| 推理模式分类 | `ModelOutputProfile.reasoningMode` | none/separate/shared-known/shared-unknown | 阶段 8 在其旁增加 gate，不另建平行 planner |
| 预算同源 | `resolveChatRequestPlan`、`groupRequestPlan` | 上下文输出预留与实际 `maxTokens` 共用一个值 | 新上下文分配器不得二次推导 |
| 正常/异常收尾 | `generatedReplyPipeline`、阶段 7 latch | finish reason、稳定边界、补尾、迟到事件幂等 | 恢复逻辑接入调用侧，不复制 finalizer |
| 生成观测 | `shared/generationObservation.ts`、`electron/services/generationObservation.ts` | provider、model、请求上限、正文字符、completion/reasoning token、终止原因、attempts | 增加回读和聚合，不新建第二份日志 |
| 基线分析 | `shared/generationBaseline.ts`（口径纯函数）+ `scripts/generation-baseline.ts` | 读取 JSONL 并生成基线；有效生成口径（G2 500 次分母）与 provider/model/task 分组已冻结（C4）；旧记录字段缺失兼容 | W1 只做键扩展与读取接线，不重定义分母 |
| 灰度回滚 | `Settings.generationPipeline` | unified/legacy 双轨 | 不新增 `generationPlannerV2` |

### 3.2 部分完成，必须补闭环

1. `resolveReasoningReserve` 已支持 `recentReasoningTokens` 和 P90×1.2，但生产调用点没有传样本。
2. 观测文件只有追加与轮转，没有读取、索引、聚合或 IPC 查询。
3. `reasoningMode: 'disabled'` 只是意图；端点可能拒绝或静默忽略，没有可信度记忆。
4. OpenAI、Claude、Gemini 的推理参数仍有适配器私有策略，语义不统一。
5. 方向任务有结构修复重试，但没有“推理挤占”恢复；两种重试必须共享总次数规则，避免一轮产生无限组合。
6. 上下文已有世界书评分、语义事实、分层记忆与历史裁剪，但各层预算仍由分散比例决定。
7. `generationPipeline` 的 legacy/unified 灰度尚未完成 500 次有效生成退出门槛。

### 3.3 尚未实现

- 阶段 8 的 `ReasoningGate`、适配器统一门控、静默忽略探测、提前中止、一次降档重试、会话熔断；
- 按端点隔离的观测回读与 `UsageProfile`；
- 能力来源、置信度、用户覆盖和运行时纠正；
- 统一 `ContextCandidate` 和确定性分配器；
- 序列化后最终输入审计；
- PC 动态上下文诊断视图；
- 旧 maxTokens/快捷设置的产品语义迁移。

---

## 4. 不变量与禁止事项

### 4.1 唯一事实来源

- 正文期望：`ResponsePolicy` 或 `BackgroundGenerationProfile`；
- 输出预算：`resolveRequestBudget`；
- 推理约束：阶段 8 的 `resolveReasoningGate`；
- 最终正文处理：`generatedReplyPipeline` / 统一终止协调入口；
- 观测记录：`GenerationObservation`；
- 回滚：现有 `generationPipeline`，以及阶段 8 临时的 `reasoningGateEnabled` kill switch。

不得增加 `GenerationIntentV2`、`OutputPlannerV2`、`generationPlannerV2` 或第二份观测文件。

### 4.2 预算同源

一次请求必须满足：

```text
上下文构建使用的 reservedOutput
  === 最终 ChatParams.maxTokens
  === 观测中的 requestedMaxTokens
```

重试时允许因门控降档重新计算输出预算，但必须复用同一份业务上下文候选快照；不得悄悄改变用户消息、世界书命中或记忆检索结果。

### 4.3 重试上限

- 推理挤占：同一生成轮最多一次自动降档重试；
- 已有正文的 provider length：走既有稳定边界/补尾，不能触发整条重生成；
- 结构修复：方向等后台任务已有一次结构修复时，必须由统一 attempt ledger 决定是否还允许请求；
- 参数被 400 明确拒绝时的“去参重发”是传输层协商，不得无限递归；每个 knob 每个请求至多尝试一次；
- 会话内连续两次推理降档仍失败时熔断，后续从更低档开始，并且提示只展示一次。

建议统一记录：

```ts
interface AttemptLedger {
  logicalRequestId: string
  transportAttempts: number
  reasoningRecoveryAttempts: number
  structureRepairAttempts: number
  tailRepairAttempts: number
}
```

该结构用于观测和约束，不要求替换现有 requestId；每次物理请求仍应有可关联的子 requestId。

### 4.4 数据与隐私

- 默认不保存提示词、角色卡、世界书、回复正文或完整 URL；
- 端点键使用标准化地址的不可逆短哈希，先移除凭据、query 和 fragment；
- renderer 只取得聚合后的样本或分位数，不取得诊断文件路径与原始 JSONL；
- 旧日志读取失败、单行损坏或字段缺失不得阻塞生成；
- `tailSample` 属现有诊断遗留字段，不得被学习闭环读取或展示为模型档案内容。

### 4.5 安全上限

在完成第 10 节决策门前：

- 保留 `MAX_REQUEST_OUTPUT_TOKENS = 8192`；
- 不因模型宣传值自动申请超长输出；
- 不把端点一次成功响应推断成永久物理能力；
- 不把“模型输出上限”和“本轮需要的输出量”混为一谈。

---

## 5. 目标架构：扩展现有链路，而不是替换

```text
ResponsePolicy / BackgroundGenerationProfile
                    │
                    ├── ModelOutputProfile（现有，逐步补能力来源）
                    ├── UsageProfile（从现有观测日志回读）
                    └── ReasoningGate（阶段 8，服务端约束 + 探测）
                    │
                    ▼
resolveRequestBudget（现有唯一输出预算入口）
                    │
                    ├── requestMaxTokens
                    ├── reasoningReserve / gateTokens
                    └── riskNotice
                    │
                    ▼
ContextBuilder（现有）→ ContextCandidate/Allocator（渐进接入）
                    │
                    ▼
ChatParams + adapter gate mapping
                    │
                    ▼
流式审计 → 阶段 8 恢复 → 阶段 7 finalizer
                    │
                    ▼
GenerationObservation（现有）→ UsageProfile 回灌
```

### 5.1 `ResponsePolicy` 与后台任务档案

主对话继续由 `ResponsePolicy.hardMaxChars` 表达正文保护线。后台任务不强行套用主对话篇幅档位，而由 `BackgroundGenerationProfile` 给出 taskType、期望正文/结构最小空间、默认 gate、允许的修复类型和解析成功判据。

方向任务应声明“短结构化正文、默认 off、允许一次结构修复”，而不是声明“1536 Token”。

### 5.2 `ModelOutputProfile` 的渐进扩展

首批不重命名模块。需要时在现有类型上增加输入/combined limit、token accounting、capability source、confidence 和 updatedAt。若后期认为名称已不准确，可以在所有调用统一后做纯机械重命名；重命名不能和能力语义改造放在同一提交。

能力优先级：用户明确覆盖 > 供应商可验证元数据 > 精确内置规则 > 运行时保守纠正 > fallback。运行时错误只允许降低当前端点的可用能力或置信度，不能污染同名模型的其他端点。

### 5.3 `UsageProfile`：让已采数据参与规划

建议主进程维护只读聚合：

```ts
interface UsageProfileKey {
  provider: string
  endpointFingerprint: string
  model: string
  taskType: string
  reasoningModeOrGate: string
}

interface UsageProfile {
  sampleCount: number
  recentReasoningTokens: number[]
  reasoningP90?: number
  bodyVisibleCharsP95?: number
  reasoningFilledRate: number
  lastUpdatedAt: number
}
```

实现规则：

1. 首次查询时懒加载当前 JSONL；如发生轮转，可读取 `.old`，但总读取量必须有界。
2. 单行 JSON 损坏时跳过并计诊断，不让整个档案失效。
3. 每次新观测写入后同步更新内存索引，避免每轮扫盘。
4. 只返回最近有限个数值样本或聚合分位数；建议每键最多 32 条、全局 LRU 最多 256 个键。
5. 样本不足 5 条时标记低置信度；不足 10 条时沿用当前 `percentile90` 的保守最大值语义。
6. `reasoningTokens === 'unknown'` 不转成 0，也不参与分位数。
7. 成功、推理触顶、参数拒绝分别计数，避免只学习成功样本造成幸存者偏差。

IPC 只暴露聚合查询，例如 `ai:getGenerationUsageProfile`；preload 返回数值结构。发送编排在构建上下文前异步预取并缓存，随后把样本传入现有 `resolveChatRequestPlan` / `resolveRequestBudget`。不得让纯函数内部直接访问文件或 Electron。

### 5.4 推理门控与预算关系

以阶段 8 为准：

```text
可靠 gate：requestMaxTokens = bodyReserve + gateTokens
不可靠/无 gate：requestMaxTokens = bodyReserve + max(档案余量, 近期 P90 估计)
```

- `recentReasoningTokens` 是估计和提前中止阈值，不是推理硬约束；
- 方向任务的正确修复是 `off` 门控 + 静默忽略探测 + 提前中止 + 一次降级恢复，不是把 P90 算得更大。

### 5.5 上下文候选与分配器

```ts
interface ContextCandidate {
  id: string
  kind: 'protocol' | 'character' | 'current-state' | 'memory' |
        'worldbook' | 'history' | 'example' | 'group-state'
  estimatedTokens: number
  mandatory: boolean
  stablePrefix: boolean
  relevance: number
  recency: number
  importance: number
  continuity: number
  dedupeKey?: string
  originalOrder: number
}
```

相同输入必须得到相同顺序与选择；评分相同时按 `originalOrder`、再按稳定 `id` 排序，避免缓存前缀抖动。

分配顺序：

1. 协议、当前用户消息、角色核心与当前场景等 mandatory 块；
2. 最近连续对话和当前群聊参与者状态；
3. 当前状态、相关长期事实与高相关世界书；
4. 示例、低相关记忆、较早原始历史；
5. 若仍超限，先去重，再降低相关块，再用既有摘要替换旧原文；
6. 最后才移除次要设定；持久化记忆永不因本轮分配被删除。

分配器首轮只做影子计算，记录“现有选择 token / 新选择 token / 各类数量 / 是否超预算”，不得记录内容。影子结果稳定后按类别逐步接管，不能一次替换整个 `contextBuilder`（落点：`shared/chat-core/contextBuilder.ts`）。

### 5.6 序列化后最终审计

- 先用现有估算器做候选选择；
- 适配器请求体准备逻辑应可在发送前返回可计数的序列化视图；
- 有供应商 tokenizer 时使用精确计数；没有时使用带 `accountingConfidence: estimated` 的保守估算；
- 最终输入预算必须扣除同源 `requestMaxTokens` 和协议安全余量；
- 审计超限时只回到 `ContextAllocator` 降级一次，不在适配器里私自截消息。

本期验收不能把所有供应商都宣称为“精确 Token”。应分别记录 `exact / provider-reported / estimated`。

---

## 6. 恢复状态机

### 6.1 推理挤占

严格复用阶段 8：

```text
无正文 + reasoning_gate_exceeded
或 length + 正文 < 20 + reasoning/completion ≥ 0.9
或无 usage 时 length + 正文为空
        │
        ▼
降低一档 gate，复用同一上下文快照，至多重试一次
        │
        ├── 成功：进入阶段 7 finalizer，用户只看到最终消息
        └── 再失败：记录并熔断，展示一次可操作错误
```

不得加入“按实际推理消耗再扩大输出”的恢复步骤。

### 6.2 有正文的长度截断

- 不整条重生成；
- 使用既有稳定句界、结构闭合与一次短补尾；
- 补尾和推理降档触发条件互斥；
- UI 保持同一条消息，落盘仍由阶段 7 latch 保证至多一次。

### 6.3 上下文超限

1. 解析供应商返回的限制信息，只记录结构化数值；
2. 降低当前端点能力置信度；
3. 用同一候选快照重新分配；
4. 自动重试一次；
5. 不删除持久化记忆；
6. 与推理恢复共享 `AttemptLedger`，防止两类恢复交叉放大请求数。

### 6.4 方向任务的双重修复约束

- 推理挤占发生在第一物理请求：允许一次降档重试；若第二次得到合法结构则结束；
- 第二次得到非空但结构不合法：不再进行第三次请求，记录 `structure_invalid_after_reasoning_recovery`；
- 第一请求得到非空但结构非法：允许现有一次结构修复；结构修复请求若再推理挤占，不再额外降档第三次；
- 方向任务每个逻辑请求最多 2 次模型调用。

---

## 7. 分阶段实施与依赖关系

### 7.1 总体依赖

```text
W0 基线冻结
 ├── W1 观测回读 ─────┐
 └── W2 阶段8纯函数 ──┼→ W3 适配器门控 → W4 单聊/方向恢复 → W5 群聊/Bridge
                       │                                      │
                       └──────────────────────────────────────┘
                                                              ▼
                                                    G1 阶段8灰度门
                                                              │
                         W6 能力档案扩展 ──────────────────────┤
                                                              ▼
                                      W7 上下文影子 → W8 记忆 → W9 世界书/历史
                                                              │
                                                              ▼
                                           W10 UI/预设 → G2 legacy 退出门 → W11 清理
```

W1 与 W2 可并行。W6 可在 W3–W5 测试期间设计，但不得抢先改变生产上限。W7 之后的生产切换必须等待 G1。

### 7.2 W0：冻结基线与防漂移测试（1–2 人日，部分已由清理期预交付）

任务：

1. 把 §2.1 的匿名观测摘要与 C4 口径（`shared/generationBaseline.ts`）固化为 W0 基线快照文件
   （含有效生成计数、分母口径、分组方式），不复制原始正文。**不再重新统计**，直接引用清理期产出。
2. 为 `resolveRequestBudget` 增加“派生值证明”测试：2500/600/200 字保护线的结果来自公式。
   （清理期已在 `shared/__tests__/modelOutputProfile.test.ts` 覆盖 600/2500 与 P90 路径，W0 补齐 200/边界并确认无遗漏。）
3. 为方向任务增加推理吃满、正文为 0、length fixture。
4. 锁定单聊/群聊/Bridge 的 finishReason 与 terminationCause 契约。
5. 写明阶段 7.4 尚未完成，不修改其 500 次门槛；记录冻结基线提交（清理期 A/B/C 合入后的提交，见 §16）。

建议文件：`shared/__tests__/modelOutputProfile.test.ts`、`shared/__tests__/generationObservation.test.ts`、
`src/store/__tests__/dialogueDirectionRunner.test.ts`、现有 Bridge 方向测试。

验收：只加测试/报告，不改变生产请求；定向测试全绿。

### 7.3 W1：观测回读与 UsageProfile（3–4 人日，可与 W2 并行）

任务：

1. 在现有观测服务增加有界读取与内存聚合；不新建第二份持久化文件。
2. 增加端点指纹纯函数与测试，验证凭据/query/fragment 不进入键。
3. 增加按 provider + endpoint + model + task + gate/reasoningMode 查询。
4. 暴露只读 IPC/preload 方法，返回有限数值样本或聚合结果。
5. 在生成前异步预取到 renderer 缓存，并把样本传到生产调用点。
6. 更新基线脚本，允许按新键分组；旧记录字段缺失时兼容。

最低接线范围：主对话 request plan、群聊 request plan、memory/group-memory、tail repair/continue、direction（待 W4 切换）。

验收：

- 损坏行跳过、`.old` 合并、有界样本、unknown 不当 0；
- 至少一个生产单聊测试证明样本进入 `resolveRequestBudget`；
- 读取失败回退静态档案，生成不失败；
- IPC 不返回正文、完整 URL 或磁盘路径。

### 7.4 W2：阶段 8 共享门控域与预算接线（3–4 人日，可与 W1 并行）

任务：

1. 新增 `shared/reasoningGate.ts`；
2. 定义 gate level、knob、probe、降档链和可信度；
3. 扩展 `ChatParams`、termination cause 与观测字段；
4. 扩展 `resolveRequestBudget`，可靠 gate 使用 gateTokens，不可靠 gate 使用档案/P90；
5. 保留现有 `riskNotice`；
6. 增加 `reasoningGateEnabled` 临时 kill switch，默认关闭；不得增加 planner V2 开关。

验收采用阶段 8 §七：门控可信/不可信/无门控三态、档位链、正文最小空间、用户硬上限、未知模型退化均有纯函数测试。

### 7.5 W3：适配器统一门控与探测（4–6 人日）

顺序：OpenAI 兼容 → Claude → Gemini → Ollama 明确标记 none。

任务：

1. 适配器只消费统一 gate，不自行决定产品策略；
2. 删除 OpenAI 固定 `medium`、Claude `maxTok/3` 等私有猜测；
3. Gemini 增加支持范围内的 thinking 映射；
4. 把 thinking-400 特例收编为字段级降级表；
5. 只有明确字段拒绝才写 `knobAccepted:false`；网络错误不污染 probe；
6. off 请求仍出现 reasoning delta 时写 `disableIgnored:true`；
7. 参数去除后每个物理请求最多重发一次。

验收：fake-fetch 请求体快照覆盖每个 knob；400、假接受、无 usage、用户取消、网络失败均有测试。

### 7.6 W4：单聊、方向任务、提前中止与一次恢复（4–6 人日）

任务：

1. 流式计数 reasoning/body，按阶段 8 条件提前中止；
2. 返回结构化 `reasoning_gate_exceeded`，不依赖中文错误文本驱动状态机；
3. 在单聊终止协调入口之前增加一次降档恢复；
4. 会话内连续失败熔断；
5. 将 direction 的 1536 替换为后台任务档案 + `resolveRequestBudget` + `off` gate；
6. direction 使用统一最多 2 次物理调用规则；
7. 所有生产调用移除后再删除 `DIALOGUE_DIRECTION_MAX_TOKENS`，解析器条数/字段长度限制保留。

验收：

- “忽略 disabled”的方向 fixture 首次提前中止、第二次恢复成功；
- 两次均失败只展示一次最终错误且触发熔断；
- 有正文 length 不整条重生成；
- 用户停止不恢复；
- 方向总调用永不超过 2；
- 成功方向仍通过原解析与持久化规则。

### 7.7 W5：群聊、Bridge 与阶段 8 灰度（3–5 人日）

任务：

1. 群聊点名、轮询、自由发言接入同一 gate 与恢复控制器；
2. Bridge 主对话和 Bridge 方向对齐；
3. 单角色失败不得重复其他角色已完成回合；
4. 扩展观测和基线脚本；
5. 运行 DeepSeek 聚合端/官方端、Claude、Gemini、非推理对照矩阵；
6. 先内部启用 `reasoningGateEnabled`，达标后扩大。

G1 灰度门：

- 推理挤占用户可见失败率 < 0.5%；
- 空正文降档恢复成功率 ≥ 90%；
- 只有正文 < 20 字符才整轮恢复；
- **提前中止只在"继续已不可能产出合格正文"时触发**（观测线 = `requestMaxTokens − MIN_USABLE_BODY_TOKENS`，2026-09-13 修订），
  并在**成对对照**（同用例、同请求体、门控臂 vs 对照臂）下不使正文产出率下降；
- 降档额外 completion token 占比 < 3%；
- **同请求体条件下成功路径无回归**（替代原"P95 延迟增幅 < 5%"：门控臂与对照臂请求体逐字段相同，
  但 n≈10–18 时长尾分布的 P95 不稳定，不足以支撑 ±5% 判定）；
- 全量测试、Bridge 测试和 Android 协议 fixture 通过。

> **2026-09-13 口径修订依据**：原第 4 条"提前中止平均节省等待时间 ≥ 50%"与 §4.4 的观测线数学上互斥——
> 观测线取上限的 85% 时，最多只能比"跑满上限"提前约 15% 结束；而实测 3/3 次中止全部空正文、且比配对对照更慢
> （[G1 报告](../报告/PC端动态上下文G1灰度取证实机评测报告-2026-09-13.md) §4 F3）。若产品确实需要 ≥50% 的等待节省，
> 应把它做成**显式档位**（用户选择"快速模式" + 明确告知可能空正文），而不是下移默认止损线——下移会把"模型本会给出正文"
> 的样本主动掐掉（对照臂推理仅用 47–66% 预算即收手并产出正文，n=44 对照 0 空正文）。

未通过 G1：停止后续上下文生产切换，不得用提高 8192 掩盖失败。

> **G1 实测取证（2026-09-13，CommandCode 聚合端 + `deepseek/deepseek-v4-pro`，n=82 例 / 80 次调用）**：
> ❌ **未通过**。推理相关空正文失败率 4/82 = 4.9%（门控臂内 10.5%），远超 < 0.5%；3 次提前中止全部以空正文结束且不省时。
> 两条结构性阻塞：(1) 该端点接受 `thinking:{"type":"disabled"}` 但仍产出 89.9%–96.3% 的推理，且**完全不传该参数时推理占比仍为 90.8%**（参数为 no-op → 应标记 `disableIgnored`）；
> (2) `off` 已是降档链末级（`nextLowerGateLevel('off') === null`），"空正文降档恢复成功率"结构上不可达。
> 另：观测线取上限 85%（§4.4）使提前中止的等待时间理论上限 ≈ 15%，与本门"≥ 50%"矛盾，需二选一改口径。
> 详见 [G1 灰度取证实机评测报告](../报告/PC端动态上下文G1灰度取证实机评测报告-2026-09-13.md)（含 4 项评测器缺陷修复：流式缓冲导致提前中止漏测、SSE usage 未解析、`--reps` 缺 stream/directions、陈旧断言）。
>
> **扩样复测（2026-09-14，n=240）——结论比首轮更差，且推翻了小样本的"归零"**：
> 推理相关空正文 **14/240 = 5.8%**（95% 上界 7.1%）；**8 次提前中止全部无正文（8/8）**，
> 且 8 次集中在同一时间窗（分四段 `3/2/9/0`）→ 该端点的推理长度会**突发漂移**，
> 观测线在估算 token 口径下落于其真实推理分布中段，会周期性误杀"本会产出短正文"的请求。
> 同请求体对照臂 **96/96 = 429**（`weekly usage limit … resets at 2026-09-16T06:36:25Z`，北京时间 09-16 14:36），
> 第 6 条本次无法验证；第 4 条已由 8/8 独立判负；方向任务 53 例成功 0 空正文（W4 成立）。
> **建议**：对 `disableIgnored` 端点默认停用提前中止（改由 P90 余量 + 零输出一次重试兜底），
> 或把观测线按估算偏差校正后再启用；该决策待产品/架构确认后落 W3/W4 收尾。

### 7.8 W6：模型能力档案扩展（3–5 人日）

任务：

1. 扩展现有 `ModelOutputProfile`，不创建第二能力中心；
2. 精确匹配优先，模型族通配只作低置信回退；
3. 支持用户对私有端点明确覆盖；
4. 运行时上下文错误只修正当前端点；
5. UI 暂只提供高级只读来源/置信度；
6. 替换 `getDefaultMaxContext` 中过宽的 DeepSeek 统一 64K 推断，未知型号保守回退。

验收：同名模型不同端点隔离；用户覆盖优先；错误只能收紧不能擅自放大；无数据时保持当前保守行为。

### 7.9 W7：ContextAllocator 影子运行（4–6 人日）

> 实施报告：[PC 端动态上下文 W7 影子分配器实施报告](../报告/PC端动态上下文W7影子分配器实施报告-2026-09-13.md)（2026-09-13 交付；生产注入未改变，等待 G1）

任务：

1. 从 `shared/chat-core/contextBuilder.ts` 提取候选描述（**不要改 `src/context/contextBuilder.ts` 垫片**），先不移动持久化或提示拼装；
2. 实现确定性评分、去重、稳定排序和预算选择纯函数；
3. 影子运行，只记录分类 token/数量差异；
4. 增加序列化后审计接口骨架；
5. 为 64K/128K/1M、小窗口、群聊、图片/工具建立 fixture；
6. 影子结果不得改变 `messages` 或 `maxTokens`。

验收：不超预算、mandatory 可行时不丢、结果稳定、增大预算不删除高优先块、不记录正文、差异可解释。

### 7.10 W8：动态记忆注入（3–5 人日）

> 实施报告：[PC 端动态上下文 W8 动态记忆注入实施报告](../报告/PC端动态上下文W8动态记忆注入实施报告-2026-09-13.md)（2026-09-13 交付；记忆注入未改变，等待 G1 后按 §7.10 接管）

任务：

1. current state、语义 facts、timeline 映射为候选；
2. 保留 `fitLayeredMemoryBudget` 作为对照与回滚；
3. 按剩余输入空间和相关性动态分配，不设新的全局上限；
4. 语义检索失败保留 importance+recency fallback；
5. 切换模型只改变本轮视图，不修改存储；
6. 做真实长会话记忆召回、矛盾、token、延迟对照。

验收：小模型安全降级；大模型可超过 800；简单话题可少注入；失败可退原选择；任何失败不删除记忆。

### 7.11 W9：世界书、历史迁移与最终输入审计（5–8 人日）

任务：

1. 复用世界书已有关键词/语义/实体/近因分数；
2. always 映射为 mandatory 或高优先策略；
3. 世界书、记忆、示例、历史在统一剩余预算选择；
4. 旧历史先用已有摘要替代，再删除原文；
5. 接入序列化后审计；超限只允许一次重新分配；
6. 等分项保持原顺序，稳定前缀不抖动；
7. 删除固定比例前保留影子差异与回归报告。

验收：空间足够时完整注入；接近上限时按定义降级；关键块不被低价值历史挤出；适配器不私自截消息；计数精度口径可见。

### 7.12 W10：PC 设置、预设迁移与诊断（4–6 人日）

> 实施报告：[PC 端动态上下文 W10 设置、预设迁移与诊断实施报告](../报告/PC端动态上下文W10设置预设迁移与诊断实施报告-2026-09-14.md)（2026-09-14 交付；保留 legacy 与 8192 安全阀）

默认界面：回复长度、思考强度、连续性、自动续写和费用提醒；思考强度可先只展示自动，阶段 8 稳定后开放更多档。

高级界面：用户输出硬上限、自定义输入/输出能力、能力来源与置信度、端点门控探测和重置、本轮分类 token、计划/实际 reasoning/body、恢复次数。不得展示 API Key、完整端点或原始正文。

迁移基线（清理期结论）：settings 已收敛为单一链——`LATEST_VERSION.settings = 3`，原渲染层三段启动探测迁移
已并入 `MIGRATIONS.settings`，renderer 只保留只读兜底。本工作包的产品语义迁移挂到 **`v3 → v4`**，
不新增第二条迁移链、不在 renderer 侧写回。

迁移规则：

1. `maxTokens = 0` 继续表示自动；
2. 用户明确自定义正数保留为硬上限；
3. 内置预设旧快捷值可映射为篇幅偏好，但必须区分内置默认与用户保存值；
4. `maxContext` 只有明确启用能力覆盖时才高于内置/供应商值；
5. 旧世界书比例仅保留迁移记录；
6. migration 幂等，可重复启动。

验收：旧设置 round-trip、不丢自定义上限、重复迁移一致、关闭新能力仍可读旧数据。

### 7.13 G2：阶段 7.4 与 legacy 退出门

> 取证工具（2026-09-14）：`shared/g2Readiness.ts` + `scripts/g2-progress.ts` —
> 按 `pipeline × gate` 分段统计有效生成与供应商；**单段** ≥500 且 ≥2 供应商才判可过门。
> 进度说明：[G2 取证进度说明](../报告/PC端动态上下文G2取证进度说明-2026-09-14.md)。
> 本机观测（同日）：最佳分段有效 7/500、供应商 1 → **G2 未通过**；W11 不得提前。

必须同时满足：

1. unified 至少 500 次有效生成且覆盖两个以上供应商；
2. 阶段 7 的截断、格式、落盘、跨端 fixture 指标达标；
3. G1 达标；
4. 动态上下文无 mandatory 丢失、记忆删除或超限回归；
5. Android 共享类型与 fixture 通过；
6. 至少一个版本保留 legacy 可回退；
7. 500 次样本按功能状态分段，不能混合 gate 开启前后数据。

### 7.14 W11：旧链路清理与上限复审（2–4 人日）

只有 G2 通过后执行：

1. 按阶段 7 三步规则退出 legacy；
2. 删除无生产调用的方向 1536、记忆 800、世界书固定比例路径；
3. 删除 shadow 记录和完成使命的 `reasoningGateEnabled`；
4. 旧消息兼容渲染长期保留；
5. 独立执行第 10 节 8192 决策门；
6. 更新 README、变更日志、方案状态和实施报告。

---

## 8. 代理工作包规范

### 8.1 领取前必读

1. 本文第 1–7 节；
2. 与任务相关的上游阶段文档；
3. 目标文件及同目录测试；
4. `git status --short`，确认已有改动；
5. 其他代理最新交接记录；
6. 清理期结论（§1.1 + [A/B 线实施报告](../报告/生成链路重构实施前遗留清理-A_B线实施报告-2026-09-13.md)）：
   确认落点已下沉到 `shared/chat-core/`、settings 链版本为 3、`electron/**` 反向依赖门禁生效。

### 8.2 分派模板

```text
工作包：W?.? 名称
目标：一句话可验收结果
前置：依赖的工作包/提交
允许修改：明确文件或模块
禁止修改：并行代理所有权、公共契约之外区域
必须复用：现有类型/函数/状态机
行为约束：重试次数、回滚、隐私、不变量
测试：定向测试和新增 fixture
交付：代码、测试、观测样例、风险/未完成项
```

### 8.3 推荐并行拆分

| 工作包 | 可分派子任务 | 文件所有权建议 | 合并顺序 |
|---|---|---|---|
| W1 | JSONL reader/index；IPC/preload；renderer cache/接线 | 主进程服务 / IPC / store 可分，但共享类型单一 owner | reader → IPC → 接线 |
| W2 | gate 纯函数；预算扩展；观测类型 | `shared/` 单一 owner | 一次合并 |
| W3 | OpenAI；Claude；Gemini/Ollama | 每个适配器可独立，`adapters/types.ts` 单一 owner | contract → 各家 |
| W4 | early abort；recovery controller；direction | 状态机与 direction 可并行，单一 owner 最终接线 | controller → direction |
| W5 | group；Bridge；评测脚本 | 三块可并行 | group/Bridge → 评测 |
| W7 | allocator；shadow instrumentation；fixtures | 纯函数与 `shared/chat-core/contextBuilder.ts` 单一 owner | pure → shadow |
| W8/W9 | memory；worldbook；history/audit | 不建议同时改 `shared/chat-core/contextBuilder.ts` | memory → worldbook → history |
| W10 | migration；settings UI；diagnostics | 数据迁移先合并（挂 `v3 → v4`） | migration → UI |

同一时间只能有一名代理拥有这些高冲突文件：`shared/types.ts`、`shared/modelOutputProfile.ts`、
`shared/chat-core/contextBuilder.ts`、`src/store/streamController.ts`、`src/store/groupStreamController.ts`、
`electron/services/adapters/types.ts`。
`src/**` 下的过渡垫片（`src/context/contextBuilder.ts`、`src/utils/*`、`src/store/chatConstants.ts` 等）
不是所有权目标：改动一律落到 `shared/chat-core/` 对应实现。

### 8.4 每个工作包的完成定义

交付必须包含：

- 修改文件和测试清单；
- 实际运行的命令与结果；
- 行为前后对比；
- 是否影响协议、设置或持久化；
- 匿名数值观测示例；
- 已知风险和下一工作包依赖；
- 未经验证的供应商行为标为“假设”。

### 8.5 建议提交边界

1. `test(generation): freeze reasoning-filled and derived-budget baselines`
2. `feat(observation): add bounded usage-profile reader and endpoint key`
3. `feat(observation): expose aggregate usage profile and wire samples`
4. `feat(reasoning): add shared gate domain and budget integration`
5. `feat(adapters): map reasoning gates and persist capability probes`
6. `feat(generation): abort runaway reasoning and recover once`
7. `fix(direction): use background profile, gate and unified budget`
8. `feat(group-bridge): align reasoning recovery and observations`
9. `feat(models): extend output profiles with scoped capability metadata`
10. `feat(context): add deterministic allocator in shadow mode`
11. `feat(memory): migrate layered memory to dynamic allocation`
12. `feat(context): migrate lorebook and history with final audit`
13. `feat(settings): migrate preferences and expose diagnostics`
14. `chore(generation): retire legacy path after measured gates`
15. `docs(generation): publish rollout evidence and remaining Android debt`

每个提交必须编译并通过定向测试。不要在同一提交中同时做模块重命名、行为改变和 UI 迁移。

---

## 9. 测试与验证矩阵

### 9.1 纯函数与属性不变量

`resolveRequestBudget`：

- 永不超过模型能力、通用安全阀和用户硬上限；
- 可靠 gate 时为正文保留最小空间；
- P90 样本缺失、unknown、异常值稳定回退；
- 增加 `hardMaxChars` 时预算不反向减少，除非命中硬上限；
- 2500/600/200 等结果是公式派生。

`ContextAllocator`：

- 总量不超过预算；
- 可行时 mandatory 全保留；
- 选择确定，输入对象不变；
- 同一 `dedupeKey` 最多一项；
- 增大预算通常保持单调包含；
- NaN、负分、空候选不崩溃。

`UsageProfile`：

- unknown 不转 0；
- 隔离键完整；
- 损坏行不污染其他记录；
- 样本与键数量有界；
- 旧 schema 可读。

### 9.2 状态机

| 场景 | 期望 |
|---|---|
| stop + 完整正文 | 正常收尾，无重试 |
| length + 有稳定正文 | finalizer/补尾，绝不整条重试 |
| length + 空正文 + reasoning 证据 | 降档一次 |
| gate early abort + 空正文 | 降档一次 |
| 第二次仍推理挤占 | 最终错误 + 熔断 |
| 用户 cancel | 不重试、不计失败熔断 |
| timeout/network | 按阶段 7 分类并保留允许保存的正文 |
| content filter | 不重试 |
| 400 拒绝 gate | 去参一次，写 probe |
| 200 接受 off 但仍有 reasoning | disableIgnored + early abort |
| 方向结构非法 | 最多一次结构修复，总调用 ≤ 2 |
| 迟到 done/error | latch 幂等，只落盘一次 |

### 9.3 固定上下文场景

1. 8K 小窗口 + 长角色卡；
2. 64K + 100 轮普通单聊；
3. 128K + 大世界书 + 长记忆；
4. 1M + 100 轮，验证不无条件塞满低价值内容；
5. 多角色群聊；
6. 图片与工具定义；
7. 语义检索不可用 fallback；
8. 切换大/小模型后存储不变、视图改变；
9. 供应商报告窗口小于内置档案；
10. 用户硬上限低于最小可用输出。

### 9.4 真实模型矩阵

至少包含 DeepSeek 支持门控端点、静默忽略 off 的聚合端、OpenAI 推理模型、Claude 扩展思考、Gemini thinking、非推理模型和 Ollama/本地模型。

每个组合跑：短方向、普通单聊首轮、长上下文、多角色、已有正文触顶、用户取消。记录 gate、reasoning/body、attempt、earlyAbort、延迟和结局，不记录原文。

### 9.5 每阶段命令

```text
pnpm check
pnpm test -- <定向测试文件>
pnpm test
pnpm test -- --coverage        # 安全敏感文件（backup/pathGuard/auth）按路径阈值判定
pnpm build
```

基线与自检脚本（清理期已正式化，可直接复用）：

```text
npx tsx scripts/generation-baseline.ts [--file <jsonl>] [--out report.md] [--days N]   # 有效生成计数/分组/分位
npx tsx scripts/evaluate-generation.ts                                                 # thought 口径与生成质量评测
node scripts/verify-backup.mjs --self-test                                             # 备份真实实现往返与安全断言
```

涉及 Electron 时覆盖对应 services/bridge 测试；涉及共享协议时运行 Android 协议/fixture 测试。若全量测试受既有失败影响，交接必须区分本提交与既有失败。

---

## 10. `8192` 安全阀的独立决策门

### 10.1 当前决定

W1–W10 保留 `MAX_REQUEST_OUTPUT_TOKENS = 8192`。本方案不预承诺删除，也不把“模型支持 384K 输出”等价为“应用应允许普通单轮申请 384K”。

### 10.2 允许评估变化的前置条件

1. G1 达标，推理不再依赖扩大输出止损；
2. 能力档案能区分模型输出、combined window 和端点覆盖；
3. 序列化后输入审计可用；
4. 用户硬上限、费用保护与取消生效；
5. 存在明确正文需求被 8192 阻止，而不是推理无界吃满；
6. 超过 8192 的成功样本证明正文收益、成本和延迟可接受；
7. legacy 退出门未被绕过。

ADR 可得出：保留 8192、按任务开放更高有限档、按高置信能力动态设置、或只对显式长篇任务开放。证据不足时默认保留。

---

## 11. 上线观测与退出标准

### 11.1 指标分组

至少按 provider + endpoint fingerprint + model、task/generation type、gate/knob/probe confidence、generationPipeline、rollout 版本、上下文区间分组。不能只看全局均值。

### 11.2 核心指标

- 推理挤占最终可见失败率；
- early abort 次数与节省时长；
- 降档率、成功率、额外 token 和延迟；
- 正文截断率与补尾成功率；
- 上下文超限与重分配成功率；
- memory/worldbook/history 各类注入 token；
- mandatory 丢失计数（必须为 0）；
- 记忆召回与矛盾人工抽样；
- 首 token / 总耗时 P50、P95；
- 缓存命中率与估算费用；
- 重复落盘、格式破损、跨端失败（必须为 0）。

### 11.3 停止上线条件

出现正文丢失/重复落盘、恢复超次数、取消后重试、协议无法解析、删除持久化记忆、mandatory 丢失、敏感信息进日志，或成本/延迟显著倒退时，立即停止扩大灰度。回滚只切执行逻辑，不清空用户数据或观测。

---

## 12. 风险登记

| 风险 | 早期信号 | 控制 | 工作包 |
|---|---|---|---|
| P90 被误当硬限制 | reasoning 超过 reserve | 区分 estimate 与 enforced gate | W1/W2 |
| 聚合端假接受 off | 200 但仍有 reasoning | disableIgnored + early abort | W3/W4 |
| 两类方向修复叠加 | 一逻辑请求 ≥3 次调用 | AttemptLedger + 总调用 ≤2 | W4 |
| 读盘影响首轮延迟 | 首次发送卡顿 | 懒加载、索引、有界读取、预热 | W1 |
| 端点档案污染 | 换代理沿用错误 probe | endpoint fingerprint | W1/W3 |
| 能力表盲目放大 | 普通回复申请巨大输出 | 能力/需求分离，8192 暂保 | W6/W11 |
| always 世界设定丢失 | 关键设定缺失 | mandatory 契约与 fixture | W7/W9 |
| 缓存前缀抖动 | 相同上下文顺序变化 | 稳定 tie-breaker | W7/W9 |
| 开关爆炸 | 出现三套回滚 | 禁止 plannerV2，gate 开关临时 | 全程 |
| legacy 永不退出 | 7.4 无负责人 | G2 门槛与版本责任人 | W10/W11 |
| Android 风险被掩盖 | PC 达标、Android 仍失败 | 明确下一期债务 | W5/W11 |

---

## 13. 本期完成定义

- 阶段 8 在 PC 单聊/群聊/Bridge 生效；
- 方向任务不再生产使用 1536；
- `recentReasoningTokens` 有生产回读并按端点/模型/任务隔离；
- 没有新增平行 intent/planner 或 plannerV2；
- `resolveRequestBudget` 仍是唯一输出预算入口；
- 记忆不再受 800 全局上限；
- 世界书不再以 30% 和 0.4/0.9 为生产硬上限；
- 上下文选择确定、可解释，审计精度口径明确；
- 设置迁移幂等且不丢用户硬上限；
- 阶段 7.4 的 500 次样本与 G1/G2 达标；
- legacy 按阶段 7 退出，旧消息兼容渲染保留；
- 8192 经独立 ADR 决定，不因本文直接删除；
- 日志不新增敏感正文、凭据或完整端点；
- PC、Electron/Bridge、Android 协议 fixture 与构建验证通过；
- 实施报告明确 Android 剩余风险和下一期接入点。

---

## 14. 下一期边界

Android 与其他桥接 AI 辅助任务下一期复用 ReasoningGate 语义、GateProbe/UsageProfile 协议、能力来源、ContextCandidate 和统一恢复观测。开始前需确认共享结构不依赖 Electron 文件路径或 renderer store——**该项已由清理期 B1 满足**：`shared/chat-core/` 不依赖 `electron/**` 或 `src/**`（`src/store/useSettingsStore.ts` 与 `src/lib/logger.ts` 仍属渲染层，仅通过 sink 注入影响共享层），并由 ESLint 门禁防回归。Android 是本地执行纯函数还是接收最终执行计划，应另行决策。

---

## 15. 最终执行指令

从 W0 开始，W1/W2 并行，随后严格按 W3 → W4 → W5 完成阶段 8。G1 未通过前不得切换动态上下文生产行为；G2 未通过前不得删除 legacy。动态上下文从 W7 影子运行开始，按记忆、世界书、历史逐层接管。

遇到冲突时，优先“扩展现有唯一入口、保留阶段 7/8 契约、缩小提交面”。如果实现需要第二套 planner、第二份观测或第三个长期回滚开关，应停止编码并重新拆分。

---

## 16. 修订记录

| 日期 | 修订 | 说明 |
|---|---|---|
| 2026-09-13 | 初版 | 主计划立项 |
| 2026-09-13 | 清理期完成同步（本次） | 新增 §1.1「前置清理已完成」；W7–W9 落点改为 `shared/chat-core/contextBuilder.ts`；§2.3/§3.1 路径与基线表格对齐 C4；W0 标注清理期预交付项；W10 明确挂 `v3 → v4`；§8.1/§8.3 增补门禁与垫片约束；§14 标注前置已满足。冻结基线提交为清理期 A/B/C 三线合入后的提交（`b70f4e3` 反向依赖下沉、`5002eee` 脚本/自检正式化、`3373d4d` 共享模块与提示词）。 |
| 2026-09-13 | W7 实施（影子运行） | §7.9 挂实施报告链接：`contextCandidates` / `contextShadow` / `inputAudit` 三个共享模块 + `contextBuilder` 影子接线 + 7 个固定场景 fixture + 75 例定向测试；**未改变 `messages` / `maxTokens`**，未新增 planner/观测/开关；群聊生产路径（`src/store/groupChatContext.ts`）待其下沉后接入。G1 仍未达标，W8 起按序等待。 |
| 2026-09-13 | W8 实施（动态记忆注入，影子） | §7.10 挂实施报告链接：新增 `memoryCandidates`（时间线切块、三层候选化、复用 `scoreAndRankFacts` 官方评分、与全部非记忆块同预算竞争）+ `contextShadow` 候选访问器 + `BuildResult.memoryShadow`；注入仍由 `fitLayeredMemoryBudget` 决定，接管为 G1 后的单点替换，**未新增开关**。实测：200K 窗口下既有 584 → 候选 1751（突破 800 口径），8K 超量记忆下分级让位且 mandatory 不丢。遗留：真实长会话召回/矛盾/延迟对照（需模型额度）、极端样本下记忆占用 88% 的复核项、时间线截断方向由"保留最旧"改为"保留最新"。 |
| 2026-09-14 | W10 实施（设置、迁移与诊断） | §7.12 挂实施报告链接：settings 唯一链升至 `v3 → v4`；新增生成规划默认/高级界面、端点能力显式覆盖与脱敏诊断；预设保持 `0=自动` 和正数硬上限，只有内置旧快捷值映射篇幅；旧世界书比例仅留迁移记录；legacy 与 8192 安全阀保留。 |
| 2026-09-13 | G1 实机取证（未通过） | 活跃 profile `commandcode` + `deepseek/deepseek-v4-pro` 实机评测 n=82 例：**G1 未通过**（空正文失败率 4.9%；提前中止 3/3 空正文且不省时；off 档无降档目标）。新增 `scripts/g1-report.ts` 取证汇总器与 [G1 报告](../报告/PC端动态上下文G1灰度取证实机评测报告-2026-09-13.md)；修复取证工具 5 处缺陷（流式缓冲屏蔽提前中止、SSE usage 未解析、`--reps` 缺 stream/directions、`legacy-preset-maxTokens` 陈旧断言、基线脚本 userData 目录写死）。复核 G2 分母仍为 7/500（评测样本不计入）。§7.7 已就地记录结论与两条结构性阻塞；生产接管继续封停。 |
| 2026-09-13 | G1 阻断项修复（第一轮：零输出恢复） | [修复报告](../报告/PC端动态上下文G1阻断项修复报告-2026-09-13.md) §2.1–§2.2：`ai:error` 透传 `classifyFailureOutcome` 分类（`AIErrorPayload.errorKind`，可选字段）；新增 `resolveEmptyOutputRecovery` + `streamController.runRecoveryRetry` —— 零输出（`empty_output` / `reasoning_budget_exhausted`）时**有更低档位则降档、`off` 已末级则同档重试**，与降档恢复共用同一 attempt 位（一轮 ≤2 次调用），同档失败不计入熔断、不写 `downgradeRetry`；已有正文/用户停止/审核/传输失败行为不变。 |
| 2026-09-13 | G1 阻断项修复（第二轮：口径与余量） | [修复报告](../报告/PC端动态上下文G1阻断项修复报告-2026-09-13.md) §2.3–§2.5：**R3** 提前中止观测线由 `max(gateTokens, 0.85×上限)` 改为"可证明的徒劳点"`requestMaxTokens − MIN_USABLE_BODY_TOKENS`（阶段8 方案 §4.4 同步修订；§7.7 第 4/6 条随之改写：不再要求 ≥50% 等待节省，改为"只在徒劳时止损 + 成对对照不使正文产出率下降 + 同请求体无回归"）；**R4** P90 用量样本接入不可信门控预算（`resolveRequestBudget` 取 `max(承诺值, 保守余量)`、`resolveReasoningGate` 支持 `recentReasoningTokens`、`withReasoningGate` 带缓存样本）；**R5** 全零样本不再把余量塌到协议下限（回退档案默认余量）。同端点小样本复测 n=46：守卫期空正文 2/10 → 0/24（**该结论已被下一行的扩样推翻**）；意外三档 A/B：余量 192 → 6/6 空正文、3072 → 1/18、4096 → 0/12。`pnpm test` 2818 全绿（+18 例）。 |
| 2026-09-14 | G1 扩样复测（n=240，推翻小样本结论） | 同端点同模型扩到 **240 例**（dialogue/stream/directions，并发 3，P90 余量）：**推理相关空正文 14/240 = 5.8%**（95% 上界 7.1%），其中 **8 次提前中止全部无正文（8/8）**，且 8 次集中在同一时间窗（按完成顺序分四段 `3/2/9/0`）→ 观测线在估算 token 口径下落于真实推理分布中段，会周期性误杀；**§5.2 的"0/24 归零"是小样本假象，已在两处报告更正**。紧随其后的同请求体对照臂 **96/96 = 429**：`weekly usage limit … resets at 2026-09-16T06:36:25Z`（北京时间 09-16 14:36）→ 第 6 条本次无法验证；第 4 条已由 8/8 独立判负。方向任务 53 例成功 0 空正文（W4 成立）。§7.7 第 1/4 条仍 ❌，G1 未通过。评测器补齐真实 `--concurrency`（供扩样）。 |
