# 正则表达式审查报告

> 涉及文件：`dialogue-parser.ts`、`MessageBubble.tsx`、`GroupChatMessage.tsx`、`ChatInput.tsx`、`StatusBar.tsx`、`variables.ts`

---

## 目录

1. [总览 —— 全部 27 条正则](#1-总览)
2. [按文件逐一分析](#2-按文件逐一分析)
   - [dialogue-parser.ts](#21-dialogue-parserts)
   - [MessageBubble.tsx](#22-messagebubbletsx)
   - [GroupChatMessage.tsx](#23-groupchatmessagetsx)
   - [ChatInput.tsx](#24-chatinputtsx)
   - [StatusBar.tsx](#25-statusbartsx)
   - [variables.ts](#26-variablests)
3. [问题汇总](#3-问题汇总)
4. [修复建议](#4-修复建议)

---

## 1. 总览

| # | 位置 | 正则 | 用途 | 风险 |
|---|------|------|------|------|
| 1 | `dialogue-parser.ts:45` | `/[“”„‟＂「『‹«]/g` | 开引号归一化 | ✅ 低 |
| 2 | `dialogue-parser.ts:46` | `/[」』›»]/g` | 闭引号归一化 | ✅ 低 |
| 3 | `dialogue-parser.ts:48` | `` /`([^`\n]+)`/g `` | 反引号→双引号 | 🔴 高 |
| 4 | `dialogue-parser.ts:52` | `/\*{3}[^*]+\*{3}/g` | 粗斜体 `***...***` 保护 | 🟡 中 |
| 5 | `dialogue-parser.ts:53` | `/_{3}[^_]+_{3}/g` | 粗斜体 `___...___` 保护 | 🟢 低 |
| 6 | `dialogue-parser.ts:54` | `/\*\*[^*]+\*\*/g` | 粗体 `**...**` 保护 | 🟡 中 |
| 7 | `dialogue-parser.ts:55` | `/__[^_]+__/g` | 粗体 `__...__` 保护 | 🟢 低 |
| 8 | `dialogue-parser.ts:57` | `/~~[^~]+~~/g` | 删除线 `~~...~~` 保护 | 🟢 低 |
| 9 | `dialogue-parser.ts:58` | `/<\/?[a-zA-Z][^>]*\/?>/g` | HTML 标签保护 | 🟡 中 |
| 10 | `dialogue-parser.ts:59` | `/!\[[^\]]*\]\([^)]*\)/g` | 图片 `![alt](url)` 保护 | 🟡 中 |
| 11 | `dialogue-parser.ts:60` | `/\[[^\]]*\]\([^)]*\)/g` | 链接 `[text](url)` 保护 | 🟡 中 |
| 12 | `dialogue-parser.ts:61` | `` /`[^`\n]+`/g `` | 行内代码保护 | 🔴 死代码 |
| 13 | `dialogue-parser.ts:71` | `/(\*[^*]+\*)\|(speaker:"…")\|("…")/g` | 核心解析 | 🔴 高 |
| 14 | `dialogue-parser.ts:65` | `PH_MARKER(\d+)PH_MARKER` | 占位符还原 | 🟢 低 |
| 15 | `dialogue-parser.ts:95` | `/^"\|"$/g` | 剥离对话引号 | 🟢 低 |
| 16 | `MessageBubble.tsx:70` | `/<thinking([\s>])/gi` | `<thinking>` → `<thought>` | 🟡 中 |
| 17 | `MessageBubble.tsx:70` | `/<\/thinking>/gi` | `</thinking>` → `</thought>` | 🟢 低 |
| 18 | `MessageBubble.tsx:71` | `/<thought>([\s\S]*?)<\/thought>/gi` | 提取 thought 内容 | 🟢 低 |
| 19 | `MessageBubble.tsx:77` | `/<thought>[\s\S]*?<\/thought>/gi` | 剥离 thought 标签 | 🟢 低 |
| 20 | `MessageBubble.tsx:92-94` | 同 #16-#19 | 翻译文本二次剥离 | 🟡 冗余 |
| 21 | `GroupChatMessage.tsx:59-63` | 同 #16-#19 | 群聊 thought 处理 | 🟡 中 |
| 22 | `ChatInput.tsx:434` | `/<thought>[\s\S]*?<\/thought>/gi` | AI 助手结果剥离 | 🟢 低 |
| 23 | `ChatInput.tsx:492` | 动态 Regex — 角色名前缀匹配 | AI 续写后处理 | 🟢 低 |
| 24 | `ChatInput.tsx:495` | 动态 Regex — 用户名行匹配 | AI 续写后处理 | 🟢 低 |
| 25 | `StatusBar.tsx:30` | `/(?:\[Status:\|【状态:) key=value/gi` | 状态解析 | 🟡 中 |
| 26 | `variables.ts:10` | `/\{\{user\}\}/gi` | `{{user}}` 变量替换 | 🟢 低 |
| 27 | `variables.ts:11` | `/\{\{char\}\}/gi` | `{{char}}` 变量替换 | 🟢 低 |

---

## 2. 按文件逐一分析

### 2.1 dialogue-parser.ts

#### 整体处理流水线

```
原始文本
  │
  ├─ #1-#2  引号归一化（Unicode → ASCII）
  ├─ #3     反引号 → 双引号
  ├─ #4-#8  Markdown 粗体/斜体/删除线 → 占位符保护
  ├─ #9     HTML 标签 → 占位符保护
  ├─ #10    图片 ![...](...) → 占位符保护
  ├─ #11    链接 [...](...) → 占位符保护
  ├─ #12    行内代码 `...` → 占位符保护 ⚠️ 死代码
  │
  ├─ #13    核心解析（动作/对话/旁白分段）
  │
  └─ #14    还原占位符 → 输出 Segment[]
```

#### #1 — 开引号归一化 ✅

```js
.replace(/[""„‟＂「『‹«]/g, '"')
```

**分析**：将各种语言的左引号统一为 ASCII `"`，包括弯引号 `""`、德语引号 `„‟`、全角引号 `＂`、直角引号 `「『`、单书名号 `‹«`。

**问题**：无。注意第一个字符 `"` 是 U+201C（左双弯引号），与替换目标 `"` (U+0022) 不同。

---

#### #2 — 闭引号归一化 ✅

```js
.replace(/[」』›»]/g, '"')
```

**分析**：将各种语言的右引号统一为 ASCII `"`。

**问题**：无。

---

#### #3 — 反引号→双引号 🔴 严重

```js
.replace(/`([^`\n]+)`/g, '"$1"')
```

**分析**：将 `` `...` `` 转换为 `"..."`，意图是让 AI 模型中用反引号表示的对话能被后续正则识别。

**问题**：

1. **与 #12 冲突导致 #12 变成死代码**：此转换在 #12（行内代码保护）之前执行，等跑到 #12 时所有反引号已变成双引号，`#12` 永远不会匹配到任何内容。

2. **破坏真正的行内代码**：Markdown 行内代码 `` `code` `` 被错误转为 `"code"`，进而被核心正则误匹配为"对话"。例如 AI 输出：
   ```
   请使用 `console.log()` 来调试
   ```
   会被转换为：
   ```
   请使用 "console.log()" 来调试
   ```
   `"console.log()"` 会被 #13 匹配为 dialogue，导致行内代码被渲染成对话样式。

3. **跨行反引号**：`[^`\n]+` 中的 `\n` 排除了换行，但 `\r`（回车）没被排除。在 Windows 风格的 `\r\n` 换行中，`\r` 会留在匹配内容中。不过实际场景中消息内容通常只有 `\n`，影响不大。

---

#### #4 — 粗斜体保护 🟡

```js
.replace(/\*{3}[^*]+\*{3}/g, protect)
```

**分析**：保护 `***text***` 不被核心正则误匹配。先于 #6 执行（粗体 `**...**`），否则 `***text***` 会被先匹配为 `*text*`（动作）再残留两个 `*`。

**问题**：

- `[^*]+` 要求内容不包含 `*`。如果粗斜体内部还有 `*`（如 `***text*more***`），不会被保护，后续会被错误解析。不过 markdown 规范中 `***` 内部极少再嵌套 `*`，实际影响小。

---

#### #5 — 下划线粗斜体保护 🟢

```js
.replace(/_{3}[^_]+_{3}/g, protect)
```

**分析**：保护 `___text___`。下划线语法在中文 AI 角色扮演场景中极少使用。

---

#### #6 — 粗体保护 🟡

```js
.replace(/\*\*[^*]+\*\*/g, protect)
```

**分析**：保护 `**text**`。这是本次 B-06 修复的核心 —— 防止 `**text**` 被核心正则误匹配为 `*text*`（动作），残留两个 `*` 在 CJK 字体中渲染为黑色实心点。

**问题**：同 #4，`[^*]+` 限制了内容不能含 `*`。

---

#### #7 — 下划线粗体保护 🟢

```js
.replace(/__[^_]+__/g, protect)
```

**分析**：保护 `__text__`，与 #6 同理。下划线语法在中文场景中极少使用。

---

#### #8 — 删除线保护 🟢

```js
.replace(/~~[^~]+~~/g, protect)
```

**分析**：保护 `~~text~~`。删除线语法 `~~` 与动作 `*...*` 无冲突（字符不同），但保护起来可避免 `~` 被其他逻辑误处理。

---

#### #9 — HTML 标签保护 🟡

```js
.replace(/<\/?[a-zA-Z][^>]*\/?>/g, protect)
```

**分析**：保护 HTML 标签，避免标签属性值中的引号和 `*` 被核心正则误匹配。

**问题**：

1. **`[^>]*` 贪婪匹配无法处理属性值中含 `>` 的情况**：
   ```html
   <a title="a > b">link</a>
   ```
   这个 `<a title="a > b">` 会被 `[^>]*` 在第一个 `>` 处截断，匹配为 `<a title="a ` 然后 `>` 被 `\/?>` 消费... 实际上 `[^>]*` 匹配 `a title="a ` 后在 `>` 处停止，然后 `\/?>` 匹配 `>`，所以整体匹配 `<a title="a >`。剩余的 ` b">link</a>` 会留在文本中，`b"` 中的 `"` 可能被核心正则误匹配。

   不过 HTML 属性值中出现裸 `>` 极为罕见（应使用 `&gt;`），实际影响很小。

2. **自闭合标签**：`<br/>`、`<img src="x" />` 中的 `\/?>` 能正确匹配末尾。但如果自闭合标签前无空格 `<br/>`，`[^>]*` 匹配 `br`，`\/?` 匹配 `/`，`>` 匹配 `>`，正确。

---

#### #10 — 图片保护 🟡

```js
.replace(/!\[[^\]]*\]\([^)]*\)/g, protect)
```

**分析**：保护 `![alt](url)`。

**问题**：

1. **alt 文本含 `]`**：`![text [with brackets]](url)` 会被 `[^\]]*` 在第一个 `]` 处截断，匹配失败。实际中 alt 文本很少含 `]`，影响小。

2. **URL 含 `)`**：`![alt](url with ) paren)` 的 URL 在第一个 `)` 处截断。URL 中裸 `)` 不常见但可能出现（如 Wikipedia URL），会导致匹配不完整。

---

#### #11 — 链接保护 🟡

```js
.replace(/\[[^\]]*\]\([^)]*\)/g, protect)
```

**分析**：保护 `[text](url)`。

**问题**：同 #10 —— `]` 在链接文本中、`)` 在 URL 中都会导致匹配失败。对链接来说影响稍大，因为链接文本可能包含 `[nested]` 引用，URL 更可能包含 `)`（如 Wikipedia 的 `https://en.wikipedia.org/wiki/C_(programming_language)`）。

**实际测试**：Wikipedia 链接 `[C language](https://en.wikipedia.org/wiki/C_(programming_language))` 中，`[^)]*` 匹配 `https://en.wikipedia.org/wiki/C_(programming_language` 然后期待 `\)` 但遇到 `)` → 匹配失败。整个链接不会被保护，`C language` 和 URL 中的字符暴露给核心正则。

---

#### #12 — 行内代码保护 🔴 死代码

```js
.replace(/`[^`\n]+`/g, protect)
```

**分析**：意图保护 `` `code` ``，但由于 #3 已将所有反引号转为双引号，此正则**永远不会匹配到任何内容**。

**建议**：如果 #3 确实需要保留，则删除 #12 避免混淆。如果希望保护真正的行内代码，需要调整顺序或重构 #3。

---

#### #13 — 核心解析正则 🔴 关键

```js
/(\*[^*]+\*)|([A-Za-z\u4e00-\u9fff\u3040-\u30ff\uac00-\ud7af]+:\s*"[^"]*")|("[^"]*")/g
```

这是整个解析器的核心，分为三个捕获组：

##### 捕获组 1：`(\*[^*]+\*)` — 匹配 `*动作*`

**问题**：

| 输入 | 预期 | 实际 | 残留 |
|------|------|------|------|
| `*smiles*` | action: `smiles` | ✅ action: `smiles` | — |
| `*text with * inside*` | action: `text with * inside` | ❌ action: `text with ` | ` inside*`（`*` 变成黑点❗） |
| `*very long action spanning multiple lines*` | action | ❌ 不匹配（`[^*]+` 不含换行，但文本可能被 `.` 不匹配换行影响） | 整段变 plain |

**核心局限**：`[^*]+` 不允许动作内容中出现 `*`。在角色扮演场景中，动作偶尔会包含 `*`（如 `*says "hello" with a grin*` 这种情况没问题，但 `*does *this* and that*` 的嵌套会出问题）。

##### 捕获组 2：`([A-Za-z\u4e00-\u9fff\u3040-\u30ff\uac00-\ud7af]+:\s*"[^"]*")` — 匹配 `Speaker: "..."`

**问题**：

1. **说话人名 Unicode 覆盖不全**：
   - ❌ 缺少 CJK Extension A `\u3400-\u4DBF`（生僻汉字）
   - ❌ 缺少西里尔字母 `\u0400-\u04FF`（俄语等）
   - ❌ 缺少拉丁扩展 `\u00C0-\u024F`（如 `é`, `ñ`, `ü`）
   - ❌ 缺少数字 `0-9`（如 `Player1: "..."`
   - ❌ 缺少连字符/下划线（如 `AI-chan: "..."` 或 `Master_Chief: "..."`）

2. **对话内容 `[^"]*` 不允许嵌套引号**：
   ```
   John: "He said "hello" to me"
   ```
   匹配为 `John: "He said "`（在第二个 `"` 处截断），剩下 `hello" to me"` 会被捕获组 3 误匹配为独立对话。

3. **冒号前的空格**：`\s*` 允许冒号前后有空格，但说话人名部分不允许空格。所以 `John Smith: "hello"` 不会匹配（`Smith` 后的空格不在 `[A-Za-z...]+` 中）。

##### 捕获组 3：`("[^"]*")` — 匹配 `"对话"`

**问题**：同组 2 的嵌套引号问题。`"He said "hello""` 会被错误拆分为 `"He said "`（对话 1）+ `hello`（plain）+ `""`（空对话）。

---

#### #14 — 占位符还原 🟢

```js
new RegExp(`${PH_MARKER}(\\d+)${PH_MARKER}`, 'g')
```

其中 `PH_MARKER = '\x00PH\x00'`（含 null 字节）。

**分析**：`\x00` 是 ASCII NULL，在 JavaScript 字符串中合法但极少出现在正常文本中，作为占位符标记很安全。每次 `protect()` 调用会 `push` 到数组并用索引做标记，还原时按索引取回。

**问题**：无严重问题。但如果 placeholder 数量极大（>1000），正则中的 `\d+` 匹配效率略降。

---

#### #15 — 剥离对话引号 🟢

```js
.replace(/^"|"$/g, '')
```

**分析**：去除对话内容首尾的 `"`。

**问题**：

1. **与 #13 组 3 的 slice 方式不一致**：组 3（`"..."`）用 `fullMatch.slice(1, -1)` 去引号；组 2（`Name: "..."`）用此正则。应该统一。
   - slice 方式假设首尾字符一定是 `"`，更简洁
   - regex 方式更健壮（即使首尾不是 `"` 也不会出错）

2. **`g` 标志多余**：首和尾各最多一个匹配，全局标志无意义（但无害）。

---

### 2.2 MessageBubble.tsx

#### #16-#17 — `<thinking>` 标签归一化 🟡

```js
// #16  opening tag
.replace(/<thinking([\s>])/gi, '<thought$1')

// #17  closing tag
.replace(/<\/thinking>/gi, '</thought>')
```

**分析**：部分 AI 模型（如 DeepSeek）使用 `<thinking>` 而非 `<thought>`，统一转为 `<thought>`。

**问题**：

1. **#16 不匹配自闭合 `<thinking/>`**：`[\s>]` 字符类是空格或 `>`，不包含 `/`。如果模型输出 `<thinking/>`，它不会被转换。不过自闭合的 thinking 标签在现实中几乎不会出现。

2. **#16 不匹配标签末尾无闭合的情况**：如果流式输出的最后一帧是 `<thinking`（还没输出 `>`），不会匹配。但这不影响最终渲染（流结束后会有完整的 `>`）。

3. **大小写**：`gi` 标志正确处理了大小写变体。

---

#### #18 — 提取 thought 内容 🟢

```js
/<thought>([\s\S]*?)<\/thought>/gi
```

**分析**：使用 `[\s\S]*?`（非贪婪的"任意字符含换行"）匹配 thought 标签内的全部内容。`while` 循环配合 `exec` 提取所有匹配项。

**正确**：非贪婪 `*?` 确保多个 thought 块各自独立匹配。

---

#### #19 — 剥离 thought 标签 🟢

```js
/<thought>[\s\S]*?<\/thought>/gi
```

**分析**：与 #18 基本相同，只是不捕获内部内容。

---

#### #20 — 翻译文本二次剥离 🟡 冗余

```js
const displayRaw = (rawDisplay || '')
  .replace(/<thinking([\s>])/gi, '<thought$1')   // #16 重复
  .replace(/<\/thinking>/gi, '</thought>')        // #17 重复
  .replace(/<thought>[\s\S]*?<\/thought>/gi, '')  // #19 重复
  .trim()
```

**分析**：当显示翻译结果时，对翻译文本再做一次 thought 剥离。

**问题**：`rawDisplay` 在非翻译场景下等于 `originalDisplay`（已经剥离过 thought 标签），这里又做了一次。虽然结果正确（已剥离的文本再剥离不会变化），但属于**冗余计算**。

**优化**：可简化为仅在 `showTranslation` 时执行此逻辑。

---

### 2.3 GroupChatMessage.tsx

#### #21 — 群聊 thought 处理 🟡

```js
const thoughtMatch = normalizedContent.match(/<thought>([\s\S]*?)<\/thought>/i)
```

**分析**：与 MessageBubble 的核心区别——
- 使用 `match`（单次匹配）而非 `exec` 循环
- 标志是 `i` 而非 `gi`

**问题**：

1. **只处理第一个 thought 块**：如果消息中有多个 `<thought>...</thought>` 块，只有第一个被提取，其余被视为正文。这可能是故意的简化（群聊通常不需要多 thought），但与 MessageBubble 的行为不一致。

2. **没有 `g` 标志导致 `match` 行为不同**：`/regex/i` 只匹配第一个；`/regex/gi` 匹配所有。如果群聊消息也需要全部 thought，需要改为 `while + exec` 模式。

---

### 2.4 ChatInput.tsx

#### #22 — AI 续写行过滤 🟢

```js
// 角色名前缀检测
new RegExp(`^\\*?${escapedName}\\s*[:：]\\s*`, 'i')

// 用户行提取
new RegExp(`(?:^|\\n)\\*?${escapedUserName}\\s*[:：]\\s*(.*)`, 'i')
```

**分析**：这两个是动态构建的正则，用于 AI 续写功能中过滤角色视角内容、提取用户视角内容。

**优点**：
- 使用 `replace(/[.*+?^${}()|[\]\\]/g, '\\$&')` 正确转义用户名中的特殊字符
- `\\*?` 匹配可选的 `*` 前缀（动作中的角色名如 `*John: hello*`）
- `[:：]` 同时匹配英文和全角冒号

**问题**：无严重问题。`(.*)` 捕获组匹配到行尾的用户内容，在 Windows `\r\n` 场景下可能包含 `\r`，但实际影响很小。

---

### 2.5 StatusBar.tsx

#### #25 — 状态解析 🟡

```js
/(?:\[Status:|【状态:)\s*([^=]+?)\s*=\s*([^\]]+?)\s*(?:\]|】)/gi
```

**分析**：从消息中提取 `[Status: key=value]` 或 `【状态: key=value】` 格式的状态信息。

**问题**：

1. **value 含 `]` 时截断**：
   ```
   [Status: location = Room [A]]
   ```
   value 的 `[^\]]+?` 会在 `A` 后面的第一个 `]` 处停止（非贪婪），导致 value 为 `Room [A` 丢失了闭合 `]`。正确结果应是 `Room [A]`。
   
   修复可改为贪婪匹配 + 回溯：`([^\]]+(?:\][^\]])*[^\]]*)` 但这很复杂。实际场景中 value 很少含 `]`，影响小。

2. **非贪婪 `+?` 的性能**：在长消息中逐条执行 `exec`，非贪婪量词可能导致较多回溯。不过消息数量通常有限，实际影响不大。

---

### 2.6 variables.ts

#### #26-#27 — 变量替换 🟢

```js
.replace(/\{\{user\}\}/gi, userName)
.replace(/\{\{char\}\}/gi, charName)
```

**分析**：将模板中的 `{{user}}` 和 `{{char}}` 替换为实际名称。

**问题**：无。`gi` 标志正确，`\{\{` 正确转义了花括号。唯一小遗憾是没有处理大小写混合以外的变体（如 `{{ User }}` 含空格），但这是规范问题而非正则问题。

---

## 3. 问题汇总

### 🔴 严重

| # | 位置 | 问题 | 影响 |
|---|------|------|------|
| **3** | `dialogue-parser.ts:48` | 反引号→双引号转换破坏行内代码，使 `#12` 变死代码 | 行内代码被误渲染为对话样式 |
| **13-1** | `dialogue-parser.ts:71` | `[^*]+` 禁止动作内容含 `*` | 含 `*` 的动作被截断，残留 `*` 在 CJK 字体下成黑点 |
| **13-2** | `dialogue-parser.ts:71` | 说话人名 Unicode 范围不足 | 带重音的拉丁名、俄语名、含数字/符号名无法识别 |
| **13-2/3** | `dialogue-parser.ts:71` | `[^"]*` 禁止对话内容含 `"` | 嵌套引号导致对话被错误拆分 |

### 🟡 中等

| # | 位置 | 问题 | 影响 |
|---|------|------|------|
| **6** | `dialogue-parser.ts:54` | `[^*]+` 限制粗体内容不能含 `*` | 粗体含 `*` 时保护失败，退化为旧 bug |
| **9** | `dialogue-parser.ts:58` | HTML 属性值含 `>` 时截断 | 非常罕见 |
| **10-11** | `dialogue-parser.ts:59-60` | URL 含 `)` 时链接/图片保护失败 | Wikipedia 等 URL 可能受影响 |
| **16** | `MessageBubble.tsx:70` | `<thinking/>` 自闭合标签不处理 | 极其罕见 |
| **21** | `GroupChatMessage.tsx:61` | 只提取第一个 thought 块 | 多 thought 块时行为不一致 |
| **25** | `StatusBar.tsx:30` | value 含 `]` 时截断 | 状态值很少含 `]` |

### 🟢 低 / 建议优化

| # | 位置 | 问题 | 影响 |
|---|------|------|------|
| **12** | `dialogue-parser.ts:61` | 死代码，永远不会执行 | 混淆维护者 |
| **15** | `dialogue-parser.ts:95` | 去引号方式与组 3 不一致 | 代码风格 |
| **20** | `MessageBubble.tsx:92-94` | 非翻译时冗余二次剥离 thought | 微小的性能浪费 |

---

## 4. 修复建议

### 4.1 立即修复（本次已完成）

✅ **B-06 粗体保护**（`#4-#8`）：在核心正则前保护 `**...**`、`***...***` 等 markdown 语法，已实现。

✅ **移除 `.dialogue-text::before/::after`**（CSS）：弯引号 `"` `"` 在 CJK 字体下渲染为黑点，已移除。

### 4.2 建议后续修复

#### 修复 `#3 + #12`：反引号处理逻辑

**方案 A**（推荐）：将反引号转换移到保护阶段之后，且只转换未被保护的裸反引号。

```js
// 先将所有反引号保护起来
.replace(/`([^`\n]+)`/g, protect)
// ... 核心解析 ...
// 在 restore 阶段判断占位符内容是否像对话再决定是否转双引号
```

但这种方案太复杂。更简单的**方案 B**：删除反引号→双引号转换（#3），让行内代码保持原样。如果确实需要支持反引号对话，在 Markdown 渲染阶段由 `remarkGfm` 处理。

#### 修复 `#13-1`：动作支持嵌套 `*`

```js
// 当前（不允许内容含 *）
/(\*[^*]+\*)/

// 改进（允许内容含 *，用非贪婪 + 排除空匹配）
/(\*(?:(?!\*\*)[\s\S])+?\*)/
```

但非贪婪动作匹配会与 `**粗体**` 冲突，需要 #4-#8 的保护按顺序先执行。保护顺序已正确（粗体保护先于核心正则）。

#### 修复 `#13-2`：说话人名 Unicode 范围

```js
// 当前
[A-Za-z\u4e00-\u9fff\u3040-\u30ff\uac00-\ud7af]+

// 改进（增加拉丁扩展、西里尔、数字、常用符号）
[A-Za-z0-9_\u00C0-\u024F\u0400-\u04FF\u4e00-\u9fff\u3040-\u30ff\uac00-\ud7af][A-Za-z0-9_\-.\u00C0-\u024F\u0400-\u04FF\u4e00-\u9fff\u3040-\u30ff\uac00-\ud7af]*
```

#### 修复 `#13-2/3`：对话内容支持转义引号

在保护阶段增加对 `\"` 的处理：

```js
// 在引号归一化后，保护转义引号
.replace(/\\"/g, protect)
```

这样 `"He said \"hello\" to me"` 中的 `\"` 被保护，核心正则只看到外围的两个 `"`，正确匹配整个对话。

#### 清理 `#12` 死代码

删除或注释掉 `dialogue-parser.ts:61` 的行内代码保护，因为 #3 的反引号转换已经让它永远无法匹配。

#### 统一 `#15` 去引号方式

```js
// 组 2（当前用 regex）
const inner = fullMatch.slice(colonIdx + 1).trim().replace(/^"|"$/g, '')

// 改为与组 3 一致（用 slice）
const inner = fullMatch.slice(colonIdx + 1).trim()
const unquoted = inner.startsWith('"') && inner.endsWith('"') ? inner.slice(1, -1) : inner
```

或者反过来，组 3 也用 regex（更健壮）。

---

*报告生成于 2026-07-28 · 涉及 `d:\Code\酒馆\` 项目*
