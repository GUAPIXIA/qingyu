# 「制作角色卡」功能方案

## 一、功能目标

提供引导式角色卡制作流程，支持：

1. **AI 辅助填文**：用户输入简短概念，AI 自动扩展为完整角色设定（描述/性格/场景/首条消息/对话示例/标签）
2. **单字段 AI 生成**：每个字段旁有「帮我写」按钮，可单独调用 AI 生成
3. **封面制作**：用户上传图片 **或** 调用已有文生图模组（SD WebUI / DALL-E）生成 3:4 封面
4. **封面提示词智能构建**：根据角色设定自动生成画像提示词，用户可编辑后生图
5. **头像自动裁剪**：保存时从封面裁剪 1:1 作为头像（位置可调），无需单独处理
6. **实时预览**：制作过程中可预览角色卡效果

---

## 二、用户流程（四步向导）

```
CharactersPage 点击「制作角色卡」
  │
  ▼
┌─────────────────────────────────────────────────────┐
│ Step 1 · 概念设定                                     │
│                                                       │
│  ┌───────────────────────────────────────────────┐   │
│  │ 输入框：简述你的角色（如"赛博朋克世界的女黑客"）  │   │
│  │ [AI 扩展] 按钮                                  │   │
│  └───────────────────────────────────────────────┘   │
│                                                       │
│  AI 扩展后 → 可编辑的完整字段：                         │
│  · 名称 / 标签                                        │
│  · 描述 / 性格 / 场景                                 │
│  · 首条消息 / 对话示例                                 │
│  每个字段旁有 [帮我写] 按钮可单独 AI 生成               │
├─────────────────────────────────────────────────────┤
│ Step 2 · 封面制作                                     │
│                                                       │
│  ┌──────────┐  ┌─────────────────────────────────┐   │
│  │  3:4 预览 │  │ Tab A: 上传图片                   │   │
│  │  封面区域  │  │   [选择图片] 拖拽/点击上传         │   │
│  │          │  │ Tab B: AI 生成                    │   │
│  │          │  │   提示词编辑框（自动填充，可改）     │   │
│  │          │  │   负面提示词（SD WebUI 专用）       │   │
│  │          │  │   尺寸选择 / 风格预设              │   │
│  │          │  │   [生成封面] [重新生成]            │   │
│  └──────────┘  └─────────────────────────────────┘   │
│  保存时自动从封面裁剪 1:1 头像（上/中/下位置可调）      │
├─────────────────────────────────────────────────────┤
│ Step 3 · 高级设置（可选）                              │
│  · 备选开场白（alternateGreetings）                   │
│  · 默认记忆设置（defaultMemoryEnabled/Mode/Interval） │
│  · 绑定预设 / 世界书                                  │
│  · 角色级系统提示 / postHistoryInstructions           │
│  · 作者注释                                           │
├─────────────────────────────────────────────────────┤
│ Step 4 · 预览与保存                                   │
│  完整角色卡预览（封面 + 五件套 + 标签）                 │
│  [保存角色] → createCharacter → 跳转 /chat            │
└─────────────────────────────────────────────────────┘
```

**设计要点**：
- Step 1 的「AI 扩展」是核心亮点：用户只需一句话，AI 生成全部字段，再逐字段微调
- Step 2 的封面提示词由 Step 1 的角色设定自动构建，用户可编辑
- 每一步均可回退修改，非线性流程
- **步骤跳跃规则**：Step 2/3/4 均可跳过（允许保存无封面角色）；但 AI 生成封面要求 Step 1 至少填写 name + description（`buildCoverPrompt` 返回 null 时引导回 Step 1）；Step 4 不强制任何字段
- 未配置文生图模组时，Step 2 的 AI 生成 Tab 引导去 `/api` 页配置

---

## 三、技术架构

### 3.1 新增文件清单

```
src/
├── pages/
│   └── CharacterCreatePage.tsx              # 新路由页面 /character-create（向导容器）
├── components/
│   └── character-creator/                   # 新组件目录
│       ├── WizardStepper.tsx                # 步骤指示器
│       ├── ConceptStep.tsx                  # Step 1：概念输入 + AI 扩展 + 字段编辑
│       ├── CoverStep.tsx                    # Step 2：封面上传/生成
│       ├── AdvancedStep.tsx                 # Step 3：高级设置
│       ├── ReviewStep.tsx                   # Step 4：预览保存
│       └── CharacterPreview.tsx             # 角色卡预览组件
├── store/
│   └── useCharacterCreatorStore.ts          # 向导状态管理（草稿、当前步骤、生成状态）
└── utils/
    ├── charPrompt.ts                        # 角色画像提示词构建器
    └── avatarCrop.ts                        # 头像裁剪（位置可调 + 降采样，独立便于复用与测试）
```

### 3.2 修改文件清单

| 文件 | 修改内容 |
|---|---|
| `src/App.tsx` | 新增路由 `/character-create` |
| `src/pages/CharactersPage.tsx` | 顶部操作栏新增「制作角色卡」按钮 |

### 3.3 状态管理设计（useCharacterCreatorStore.ts）

```typescript
interface CharacterCreatorState {
  // 流程控制
  step: 0 | 1 | 2 | 3;

  // 草稿角色数据（未保存的角色卡）
  draft: Character;

  // 封面
  coverMode: 'upload' | 'generate';
  coverBase64: string | null;              // 最终封面 base64（3:4）
  coverPrompt: string;                     // AI 生图提示词
  negativePrompt: string;                  // SD WebUI 负面提示词
  coverSize: string;                       // '576x768' | '768x1024' | 自定义
  avatarPosition: 'top' | 'center' | 'bottom'; // 头像裁剪锚点，默认 center

  // AI 生成状态
  isExpanding: boolean;                    // AI 扩展中
  isGeneratingCover: boolean;              // 封面生成中
  generatingField: string | null;          // 单字段 AI 生成中
  expandRequestId: string | null;          // 当前 AI 扩展/字段生成的 requestId（用于取消）
  coverRequestId: string | null;           // 当前封面生成的 requestId

  // 错误
  error: string | null;

  // actions
  setStep(step: number): void;
  updateDraft(partial: Partial<Character>): void;
  aiExpand(concept: string): Promise<void>;        // 核心：一句话扩展为完整角色
  aiGenerateField(field: string): Promise<void>;   // 单字段 AI 生成
  buildCoverPromptFromDraft(): string | null;       // 从 draft 构建画像提示词（信息不足时返回 null）
  generateCover(): Promise<void>;                   // 调用 imageGen
  saveCharacter(): Promise<Character>;              // 保存（含头像裁剪）
  reset(): void;
}
```

### 3.4 数据流总览

```
用户输入概念 → aiExpand() → window.api.ai.chat(stream)
                                    │
                                    ▼
                          AI 返回 JSON（name/description/...）
                                    │
                                    ▼
                          parseExpandResult() → 填充 draft 各字段
                                    │
                                    ▼
                          用户逐字段编辑 / 单字段 aiGenerateField()
                                    │
                          ── Step 2 封面 ──
                                    │
          buildCoverPrompt(draft) → 填入提示词编辑框（用户可改）
                                    │
                                    ▼
          generateCover() → window.api.imageGen.generate(prompt, {size, negativePrompt})
                                    │
                                    ▼
                          返回 images[] → 取第一张 → coverBase64
                                    │
                          ── Step 4 保存 ──
                                    │
          saveCharacter() → cropAvatar(coverBase64, { position })  // 位置可调
                                    │
                                    ▼
          window.api.character.save(character)
                                    │
                                    ▼
          跳转 /chat，选中新角色
```

---

## 四、封面生成方案（核心）

### 4.1 提示词构建器（charPrompt.ts）

```typescript
// 根据角色设定构建 SD WebUI 标签式提示词
// 返回 null 表示缺少最小信息（name 与 description 均为空），UI 应引导用户回 Step 1 补充
export function buildCoverPrompt(draft: Character): string | null {
  if (!draft.name && !draft.description) return null
  const parts: string[] = [
    'best quality, masterpiece, highres,',
    inferGenderTag(draft.description),        // 从描述推断 1girl/1boy
    extractVisualKeywords(draft.description),  // 发色、瞳色等
    draft.description.slice(0, 100),           // 描述前100字
    `background: ${extractSceneKeywords(draft.scenario)}`,
    'portrait, upper body, looking at viewer',
    ...draft.tags,
  ].filter(Boolean);
  return parts.join(', ');
}
```

**提取逻辑**：
- **性别**：从描述中匹配 女/girl/woman → `1girl`，男/boy/man → `1boy`
- **发色**：银发→`silver hair`、金发→`blonde hair`、黑发→`black hair` 等
- **瞳色**：蓝眼→`blue eyes`、红眼→`red eyes`、异色瞳→`heterochromia` 等
- **场景**：城市→`cityscape`、森林→`nature, forest`、太空→`space, stars` 等

> **重要**：提示词末尾固定追加 `portrait, upper body, subject centered, looking at viewer`（见上方代码），保证主体居中——DALL-E 1024×1024 裁 3:4 会损失左右各约 12.5% 内容，主体不居中会导致头像/封面裁掉人脸。

### 4.2 生图调用流程

```
用户点击 [生成封面]
  │
  ├── buildCoverPrompt(draft)
  │   ├── 返回 null（未填 name/description）→ 引导卡片「请先完成概念设定」+ [回 Step 1]
  │   └── 正常 → 填入提示词编辑框（用户可编辑）
  │
  ├── 用户确认提示词 → 点击 [生成]
  │
  ├── 检查 imageGen 配置
  │   └── 无配置 → 提示"请先在 API 页面配置生图模组" + 跳转链接
  │
  ├── window.api.imageGen.generate({
  │     prompt: userEditedPrompt,
  │     negativePrompt: negativePrompt,   // SD WebUI 专用
  │     size: coverSize,                  // 默认 '576x768'（3:4 竖图）
  │   })
  │   └─ 主进程 imageGen.ts → SD WebUI / DALL-E
  │
  ├── 返回 images[] (base64 data URL)
  │   ├── 单张 → 直接设为 coverBase64
  │   └── 多张 → 展示选择网格，用户选一张
  │
  └── 更新预览
```

### 4.3 尺寸与比例

| 用途 | 比例 | SD WebUI 尺寸 | DALL-E 尺寸 |
|---|---|---|---|
| 封面 cover | 3:4 | 576×768（或 768×1024） | 1024×1024（生成后前端裁剪为 3:4） |
| 头像 avatar | 1:1 | 从封面裁剪 | 从封面裁剪 |

> **注意**：
> - SD WebUI 的 **512×768 是 2:3 不是 3:4**，不可用作封面尺寸；3:4 用 576×768 / 768×1024
> - DALL-E 3 仅支持 1024×1024 / 1024×1792 / 1792×1024：生成 1024×1024 后前端裁剪为 3:4，会损失左右各约 12.5% 内容——提示词已强制主体居中（见 4.1）；若生成结果构图不佳，用户可 [重新生成] 或改用上传

### 4.4 风格预设

提供快捷风格标签供用户选择，追加到提示词：

```typescript
const COVER_STYLE_PRESETS = [
  { label: '二次元',   tags: 'anime style, cel shading, vibrant colors, detailed eyes' },
  { label: '写实',     tags: 'photorealistic, detailed, cinematic lighting, 8k uhd' },
  { label: '油画',     tags: 'oil painting, classical art style, rich textures, dramatic lighting' },
  { label: '水彩',     tags: 'watercolor, soft colors, artistic, delicate brushwork' },
  { label: '赛博朋克', tags: 'cyberpunk, neon lights, futuristic, dark atmosphere, glowing' },
  { label: '古风',     tags: 'chinese traditional art, hanfu, ink wash painting, elegant' },
];
```

### 4.5 头像自动裁剪（utils/avatarCrop.ts）

保存时从封面裁剪 1:1 头像，**位置可调（默认中心）**——全身图/构图偏上时中心裁剪可能裁掉脸，提供上/中/下三档，后续可升级为拖动微调：

```typescript
export interface CropAvatarOptions {
  /** 裁剪锚点：top 保留头部区域，center 居中，bottom 保留下半身 */
  position?: 'top' | 'center' | 'bottom'
  /** 输出边长，默认 256（头像为圆形小图，256 足够清晰） */
  size?: number
  /** 输出格式，默认 jpeg（体积约为 PNG 的 1/10） */
  format?: 'png' | 'jpeg'
  quality?: number // jpeg 质量，默认 0.9
}

export async function cropAvatar(
  coverBase64: string,
  options: CropAvatarOptions = {},
): Promise<string | null> {
  const { position = 'center', size = 256, format = 'jpeg', quality = 0.9 } = options
  return new Promise((resolve) => {
    const img = new Image()
    img.onload = () => {
      try {
        const side = Math.min(img.width, img.height)
        let offsetY: number
        if (position === 'top') offsetY = 0
        else if (position === 'bottom') offsetY = img.height - side
        else offsetY = (img.height - side) / 2 // center
        const offsetX = (img.width - side) / 2
        const canvas = document.createElement('canvas')
        canvas.width = size
        canvas.height = size
        const ctx = canvas.getContext('2d')!
        ctx.drawImage(img, offsetX, offsetY, side, side, 0, 0, size, size)
        resolve(canvas.toDataURL(format === 'jpeg' ? 'image/jpeg' : 'image/png', quality))
      } catch {
        resolve(null) // 裁剪失败返回 null，调用方头像留空，不阻塞保存
      }
    }
    img.onerror = () => resolve(null)
    img.src = coverBase64
  })
}
```

> 输出 256×256 JPEG 约 30~60KB，远小于 512×512 PNG（500KB~1MB），降低保存与后续草稿持久化的体积压力。

### 4.6 无生图配置时的引导

```
┌─────────────────────────────────┐
│  ⚠️ 尚未配置生图模组              │
│                                  │
│  要使用 AI 生成封面，请先在        │
│  API 页面配置 SD WebUI 或 DALL-E │
│                                  │
│  [前往配置]  [改用上传图片]       │
│  [暂时跳过封面]                   │
└─────────────────────────────────┘
```

---

## 五、AI 辅助内容生成方案

### 5.1 一句话扩展（Step 1 核心）

```
用户输入："赛博朋克世界的女黑客，冷酷但有柔软的一面"
  │
  ▼
const requestId = crypto.randomUUID()   // 必填，与 onChunk/onDone/onError 回调对应
store.expandRequestId = requestId        // 存状态，用于中断/离开时取消
  │
  ▼
调用 window.api.ai.chat({
  requestId,                             // 必填字段
  messages: [
    { role: 'system', content: CHARACTER_EXPAND_PROMPT },
    { role: 'user', content: concept }
  ],
  temperature: 0.8,
  maxTokens: 2048,
  stream: true,
})
  │
  ▼（流式期间 UI 行为）
流式 chunk 仅静默累积，**不展示 JSON 原文**（避免闪烁/误编辑）；
界面显示「AI 正在构思角色…」加载动画 + 已生成字数。
  │
  ▼（onDone 回调）
AI 返回结构化 JSON：
{
  "name": "零",
  "description": "27岁，银色短发植入神经接口...",
  "personality": "外冷内热，逻辑缜密...",
  "scenario": "2087年的新上海...",
  "firstMessage": "*手指在虚拟键盘上飞速跳动*...",
  "exampleDialog": "{{user}}: 你为什么做这一行？\n{{char}}: ...",
  "tags": ["赛博朋克", "黑客", "女性"]
}
  │
  ▼
parseExpandResult() 解析 JSON → 填充 draft 各字段 → 用户可逐字段编辑

**中断与取消**：
- 生成中用户切换步骤 / 离开页面 / 重新点击 [AI 扩展] → `window.api.ai.cancelChat(requestId)`，并清空 `expandRequestId`
- 离开页面时统一在 store 的 reset/cleanup 中取消未完成的生成
- 若流式被中断（用户取消 / 网络断开），文本不完整：`parseExpandResult` 尽力解析出已完成的字段，UI 提示「生成不完整，可重试或手动补全」
```

**System Prompt 设计**（`CHARACTER_EXPAND_PROMPT`）：

```
你是一位专业的角色设计师，擅长创作有深度的 AI 角色扮演角色。
用户会给出一个简短的角色概念，请将其扩展为完整的角色设定。

输出要求：
1. 严格按照 JSON 格式输出，不要输出其他内容
2. JSON 字段：
   - "name": 角色名字（简洁有特色）
   - "description": 角色外貌与背景描述（100-300字）
   - "personality": 性格特征（50-150字）
   - "scenario": 登场场景（50-150字）
   - "firstMessage": 角色开场白（含 *动作描写*，用 {{user}} 指代用户）
   - "exampleDialog": 对话示例（使用 {{user}} 和 {{char}} 占位符）
   - "tags": 3-8个标签数组
3. 内容用中文撰写
4. firstMessage 要生动有趣
5. exampleDialog 用 <START> 分隔多轮对话
```

**exampleDialog 格式约定**：`{{user}}` / `{{char}}` 占位符 + `<START>` 分隔多轮——与现有编辑器 `IdentitySection.tsx` 的占位提示完全一致（`<START>\n{{user}}: 你好\n{{char}}: 你好呀！`），保证 AI 生成的示例与现有导入导出/解析链路兼容。

**JSON 解析容错**（`parseExpandResult`）：
- 去除可能的 ` ```json ... ``` ` 代码块包裹
- 从文本中提取第一个 `{` 到最后一个 `}` 之间的内容
- `JSON.parse` 失败时返回空对象，不崩溃

### 5.2 单字段 AI 生成

每个字段旁的「帮我写」按钮：
- 以当前 draft 的其他字段作为上下文
- System Prompt：「根据以下角色信息，为字段 XXX 生成内容…」
- 返回结果填入对应字段，用户可编辑

```typescript
export function buildFieldGeneratePrompt(field: string, draft: Character) {
  // 构建上下文（排除当前字段）
  const context = []
  if (draft.name && field !== 'name') context.push(`角色名：${draft.name}`)
  if (draft.description && field !== 'description') context.push(`描述：${draft.description}`)
  if (draft.personality && field !== 'personality') context.push(`性格：${draft.personality}`)
  // ...

  return {
    systemPrompt: `你是一位专业的角色设计师。请根据以下角色信息，为指定字段生成内容。
      \n## 已有角色信息\n${context.join('\n')}
      \n要求：只输出字段内容，不要解释，用中文撰写`,
    userContent: `请为字段「${fieldLabel}」生成内容。`,
  }
}
```

**流式与取消**：单字段生成同样使用独立 `requestId`，支持取消与离开页面清理（与 5.1 相同的模式）。

### 5.3 封面提示词翻译

复用主进程 `imageGen.ts` 已有的 `translatePromptToEnglish`：当 provider 是 SD WebUI 且提示词含中文时，自动翻译为英文标签。**无需前端额外处理**，IPC 层已内置。

---

## 六、保存流程

```
useCharacterCreatorStore.saveCharacter()
  │
  ├── 组装完整 Character 对象（draft → Character 类型）
  ├── name 空白时默认「未命名角色」
  │
  ├── coverBase64 存在时：
  │   ├── cropAvatar(coverBase64, { position: avatarPosition }) → 1:1 头像
  │   │   （avatarPosition 默认 center，用户在预览时可切 top/center/bottom）
  │   ├── character.avatar = avatarBase64
  │   └── character.cover = coverBase64
  │
  ├── window.api.character.save(character)
  │   └── 主进程 charCard.ts saveCharacter：
  │       ├── 写 <id>.json（不含 base64）
  │       ├── avatar 落盘 <id>.<ext>
  │       └── cover 落盘 <id>_cover.<ext>
  │
  ├── useCharacterStore.loadCharacters()  // 刷新列表
  ├── selectCharacter(character.id)
  ├── reset()  // 清空向导状态 + 清除 localStorage 草稿
  └── navigate('/chat')
```

**放弃/退出流程**（与保存并列）：

```
用户点击 [← 返回] 且存在未保存草稿
  ├── 弹确认框：「放弃当前草稿？」
  │   ├── [保存草稿并退出]  → 写入 localStorage，下次进入可恢复
  │   ├── [直接放弃]         → 清除 localStorage 草稿，返回列表页
  │   └── [继续编辑]         → 留在向导
  └── 无草稿时直接返回
```

**与现有编辑器的关系**：保存后跳转 /chat；后续如需修改，引导用户走现有角色编辑页（CharacterEditor）。封面/头像落盘均走 `charCard.ts` 同一套逻辑，两处行为保持一致，不做第二套存储路径。

---

## 七、UI 设计要点

### 7.1 页面整体布局

```
┌──────────────────────────────────────────────────────────┐
│  ← 返回          制作角色卡              [跳到概念设定 →]   │
│  ●━━━━━━━━━━━━○━━━━━━━━━━━━○━━━━━━━━━━━━○               │
│  概念设定       封面制作       高级设置      预览保存        │
├──────────────────────────────────────────────────────────┤
│                                                          │
│                   [当前步骤内容区]                         │
│                                                          │
├──────────────────────────────────────────────────────────┤
│  [← 上一步]                              [下一步 →]       │
└──────────────────────────────────────────────────────────┘
```

### 7.2 Step 1 概念设定布局

```
┌─────────────────────────────────────────────────────────┐
│  🪄 AI 一句话扩展                                        │
│  输入简短的角色概念，AI 自动生成完整设定                   │
│  ┌─────────────────────────────────────┐  ┌──────────┐  │
│  │ 赛博朋克世界的女黑客，冷酷但有柔软…  │  │ AI 扩展  │  │
│  └─────────────────────────────────────┘  └──────────┘  │
├─────────────────────────────────────────────────────────┤
│  ┌─ 角色名 ──────────────────────────┐  ┌─ 标签 ─────┐  │
│  │ [输入框]                     [🪺] │  │ [tag] [tag]│  │
│  └────────────────────────────────────┘  │ [输入框]   │  │
│                                          └────────────┘  │
│  ┌─ 角色描述 ────────────┐  ┌─ 性格特征 ──────────────┐  │
│  │ [textarea]        [🪺] │  │ [textarea]         [🪺] │  │
│  └────────────────────────┘  └──────────────────────────┘ │
│  ┌─ 场景设定 ────────────┐  ┌─ 首条消息 ──────────────┐  │
│  │ [textarea]        [🪺] │  │ [textarea]         [🪺] │  │
│  └────────────────────────┘  └──────────────────────────┘ │
│  ┌─ 对话示例 ──────────────────────────────────────────┐  │
│  │ [textarea]                                      [🪺] │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                          │
│  [🪺] = AI 帮我写此字段                                   │
└─────────────────────────────────────────────────────────┘
```

### 7.3 Step 2 封面制作布局

```
┌────────────┐  ┌───────────────────────────────────────┐
│            │  │  [上传图片] [AI 生成]                   │
│            │  │                                        │
│   3:4 封面  │  │  ┌─ AI 生成面板 ──────────────────┐  │
│   预览区   │  │  │ 提示词：                          │  │
│            │  │  │ ┌──────────────────────────────┐ │  │
│            │  │  │ │best quality, 1girl, silver...│ │  │
│            │  │  │ └──────────────────────────────┘ │  │
│            │  │  │ [重新构建提示词]                   │  │
│            │  │  │                                    │  │
│            │  │  │ 风格：[二次元][写实][油画]...      │  │
│            │  │  │ 尺寸：[576×768 ▼]                 │  │
│            │  │  │ 负面提示(SD)：[lowres, bad...]    │  │
│            │  │  │                                    │  │
│            │  │  │ [生成封面]                         │  │
│            │  │  └────────────────────────────────────┘  │
│            │  │                                        │
│            │  │  生成中：●●●○○ (loading 动画)           │
└────────────┘  └───────────────────────────────────────┘
```

### 7.4 步骤指示器（WizardStepper）

- 4 个圆形节点 + 连接线
- 当前步骤：填充强调色 + ring 光晕
- 已完成步骤：填充强调色 + 对勾图标
- 未到达步骤：灰色背景
- 已完成步骤可点击回退

---

## 八、与现有代码的集成点

### 8.1 路由注册

`src/App.tsx` 的 `<Routes>` 中新增：

```tsx
<Route path="character-create" element={<CharacterCreatePage />} />
```

### 8.2 入口按钮

`src/pages/CharactersPage.tsx` 顶部操作栏新增按钮：

```tsx
<button
  onClick={() => navigate('/character-create')}
  className="btn-primary"
>
  <Wand2 className="w-4 h-4" />
  制作角色卡
</button>
```

### 8.3 复用现有 IPC 接口

| 接口 | 用途 | 已有/新增 |
|---|---|---|
| `window.api.ai.chat()` | AI 扩展/字段生成（流式） | 已有 |
| `window.api.ai.onChunk/onDone/onError` | 流式回调 | 已有 |
| `window.api.imageGen.generate()` | 文生图 | 已有 |
| `window.api.file.selectImage()` | 选择上传图片 | 已有 |
| `window.api.file.readImageAsBase64()` | 读取图片为 base64 | 已有 |
| `window.api.character.save()` | 保存角色卡 | 已有 |
| `window.api.settings.get()` | 读取 imageGen 配置 | 已有 |

**无需新增任何 IPC 接口或主进程代码**，全部复用现有能力。

### 8.4 复用现有组件

| 组件 | 来源 | 复用方式 |
|---|---|---|
| `PresetBinding` | `character/editor/Bindings.tsx` | Step 3 高级设置中直接引用 |
| `LorebookBinding` | `character/editor/Bindings.tsx` | Step 3 高级设置中直接引用 |
| `charAssetUrl()` | `utils/asset.ts` | 预览组件中引用 |
| `useSettingsStore.getActiveProfile()` | `store/useSettingsStore.ts` | 获取当前 AI 连接配置 |
| `useSettingsStore.settings.imageGenModels` | 同上 | 获取生图模组配置 |

---

## 九、草稿持久化（防丢失）

向导过程中草稿自动保存到 localStorage（防刷新丢失）。

> **约束**：完整 coverBase64（1~3MB）**不写入 localStorage**——单张即可能逼近 5MB 配额，且 500ms 防抖序列化大字符串会卡顿。

```typescript
const DRAFT_KEY = 'character-creator-draft';

interface DraftSnapshot {
  step: number;
  draft: Omit<Character, 'avatar' | 'cover'>;   // 仅文本字段
  coverThumb?: string;                          // 封面缩略图（192×256 JPEG，约 10~20KB）
  avatarPosition: 'top' | 'center' | 'bottom';
  savedAt: number;
}

// 每次 draft / step / 封面变化时防抖写入（500ms）
useEffect(() => {
  const timer = setTimeout(async () => {
    const snapshot: DraftSnapshot = {
      step,
      draft: stripImages(draft),          // 剥离 avatar/cover 大字段
      coverThumb: coverBase64
        ? await downscaleCover(coverBase64) // Canvas 降采样 192×256 JPEG，失败则 undefined
        : undefined,
      avatarPosition,
      savedAt: Date.now(),
    };
    try {
      localStorage.setItem(DRAFT_KEY, JSON.stringify(snapshot));
    } catch {
      // 配额不足/隐私模式：仅保留内存草稿，console.warn，不阻塞向导流程
    }
  }, 500);
  return () => clearTimeout(timer);
}, [draft, step, coverBase64, avatarPosition]);

// 进入页面时检查并询问是否恢复
const saved = localStorage.getItem(DRAFT_KEY);
if (saved) {
  // 弹确认：「检测到未完成的草稿（保存于 xx），是否恢复？」
  // [恢复] → 填充 draft/step；封面显示缩略图 + 提示「草稿封面为压缩缩略图，保存前请重新上传或生成」
  // [不恢复] → 清除 localStorage 草稿，重新开始
}
```

**清除时机**：保存成功 / 用户主动放弃 / 恢复时选择「不恢复」→ 清除 `DRAFT_KEY`。

---

## 十、实现分期

| 阶段 | 内容 | 配套测试（vitest） | 依赖 |
|---|---|---|---|
| **P1·基础向导** | 路由 + 四步向导容器 + 步骤指示器 + 概念输入 + 手动字段编辑 + 保存（含 `avatarCrop.ts` 头像裁剪） | `avatarCrop` 裁剪比例/位置/坏图返回 null；向导步骤前进/回退/跳跃规则 | 无新依赖 |
| **P2·AI 扩展** | `aiExpand` 一句话扩展 + 单字段「帮我写」+ JSON 解析容错 + 流式取消 | `parseExpandResult`：纯净 JSON / 代码块包裹 / 前后杂文 / 截断 JSON 部分恢复 / 非 JSON 不抛异常 | P1 |
| **P3·封面上传** | 上传 Tab + 预览 + 保存为 cover | 上传 → 预览 → 保存链路；无图保存不崩溃 | P1 |
| **P4·封面生成** | 提示词构建器 + AI 生图 Tab + 风格预设 + 无配置引导 | `charPrompt`：性别/发色/瞳色/场景提取；空 draft 返回 null；标签追加 | P3 + 已有 imageGen |
| **P5·体验优化** | 草稿持久化（缩略图方案）+ 加载动画 + 错误重试 + 保存后跳转 + 放弃确认 | 草稿防抖写入/恢复/清除；超大封面不写入 localStorage（含失败降级） | P4 |

---

## 十一、错误处理

| 场景 | 处理 |
|---|---|
| 未配置 AI 连接 | 显示「请先在 API 页面配置 AI 连接」 |
| AI 扩展返回非 JSON | `parseExpandResult` 容错，返回空对象，不崩溃 |
| AI 扩展超时/失败 | `onError` 回调，显示错误信息，允许重试 |
| AI 扩展被中断/截断（取消/断网） | 尽力解析已完成的字段 + 提示「生成不完整，可重试或手动补全」 |
| 生成中切换步骤/离开页面 | `cancelChat(expandRequestId)` 取消未完成请求，避免泄漏与竞态 |
| 未配置生图模组 | Step 2 AI 生成 Tab 显示引导卡片 + 跳转 `/api` + [暂时跳过封面] |
| 生图失败 | 显示错误信息，允许重试 |
| 点击 [生成封面] 但 name/description 均空 | `buildCoverPrompt` 返回 null → 引导「请先完成概念设定」+ 回 Step 1 |
| 图片上传后读取失败 | 显示错误提示 |
| 保存失败 | 显示错误信息，不跳转 |
| 封面裁剪失败 | 头像留空，不影响保存 |
| 草稿写入 localStorage 失败（配额/隐私模式） | 仅保留内存草稿，console.warn，不阻塞流程 |
| 草稿封面降采样失败 | 草稿不存封面缩略图，恢复时提示重新上传/生成 |

## 十二、测试计划

项目已有 vitest 测试体系（`src/**/__tests__`），本功能纯函数密度高，各阶段配套测试已在分期表中列出，汇总如下：

| 模块 | 关键用例 |
|---|---|
| `utils/avatarCrop.ts` | 中心/顶部/底部裁剪偏移正确；输出尺寸与格式（256 JPEG）；损坏 base64 与 `onerror` 返回 null；jpg 质量参数生效 |
| `utils/charPrompt.ts` | 性别推断（女/girl/woman → 1girl）；发色/瞳色/场景关键词提取；空 draft 返回 null；tags 追加与去重 |
| `parseExpandResult` | 纯净 JSON；`json` 代码块包裹；前后杂文剥离；截断 JSON 部分恢复；完全非 JSON 返回空对象不抛异常 |
| 向导流程（组件级） | 步骤前进/回退/已完成步骤点击跳转；跳跃规则（无 name/description 时 AI 封面引导回 Step 1）；放弃确认三按钮分支 |
| 草稿持久化 | 防抖写入；恢复确认分支；「不恢复」与保存成功均清除；超大封面只存缩略图 |
| 保存链路 | 无封面保存不崩溃；avatarPosition 三档生效；保存失败不跳转 |
