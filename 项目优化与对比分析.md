# 轻语 QingYu — 项目优化与对比分析报告

> 生成时间：2026-08-01
> 分析范围：`d:\Code\酒馆`（轻语 v0.11.9）对照 `d:\Code\SillyTavern-Launcher\SillyTavern`（SillyTavern v1.18.0）
> 方法论：两次 code-explorer 子代理深度扫描（轻语 65 次工具调用 / SillyTavern 104 次）+ 主代理对关键文件的直接行数与代码验证

---

## 目录

- [一、概览与项目信息速查](#一概览与项目信息速查)
- [二、轻语内部架构与代码质量优化点](#二轻语内部架构与代码质量优化点)
  - [2.1 超大文件清单（重构首要目标）](#21-超大文件清单重构首要目标)
  - [2.2 状态管理耦合分析](#22-状态管理耦合分析)
  - [2.3 文档与代码不一致](#23-文档与代码不一致)
  - [2.4 性能隐患](#24-性能隐患)
  - [2.5 错误处理模式](#25-错误处理模式)
  - [2.6 测试覆盖评估](#26-测试覆盖评估)
  - [2.7 工程化与 CI/CD 现状](#27-工程化与-cicd-现状)
- [三、与 SillyTavern 的功能差距对比](#三与-sillytavern-的功能差距对比)
  - [3.1 AI 后端连接器](#31-ai-后端连接器)
  - [3.2 向量检索系统](#32-向量检索系统)
  - [3.3 插件与扩展生态](#33-插件与扩展生态)
  - [3.4 国际化 i18n](#34-国际化-i18n)
  - [3.5 多用户与安全机制](#35-多用户与安全机制)
  - [3.6 角色卡格式覆盖](#36-角色卡格式覆盖)
  - [3.7 图像与 TTS 后端](#37-图像与-tts-后端)
  - [3.8 跨平台与分发形态](#38-跨平台与分发形态)
- [四、具体可执行优化建议（带文件路径）](#四具体可执行优化建议带文件路径)
- [五、结论与优先级路线图](#五结论与优先级路线图)

---

## 一、概览与项目信息速查

| 维度 | 轻语 QingYu | SillyTavern |
| --- | --- | --- |
| 版本 | 0.11.9 | 1.18.0 |
| 定位 | Electron 桌面客户端（Windows） | Web 服务端 + 浏览器 |
| 技术栈 | Electron 32 + React 18 + TS 5 + Vite 6 + Tailwind 3 + Zustand 5 | Express + 原生 JavaScript（jQuery） |
| 构建打包 | Vite + electron-builder（NSIS） | webpack + Node 进程 |
| 源码语言 | TypeScript（强类型） | JavaScript（jsconfig） |
| 状态管理 | Zustand（7 个 store） | 全局变量 + jQuery DOM |
| 路由 | react-router-dom 7（HashRouter） | 无（单页 DOM 切换） |
| 虚拟滚动 | react-virtuoso | 自实现懒加载 |
| 许可证 | MIT | AGPL-3.0 |

> 说明：两者定位本质不同——轻语是"开箱即用的桌面客户端"，SillyTavern 是"自部署的 Web 服务"。因此报告中会明确区分**桌面端无关项**与**真实能力缺失**，避免生搬硬套。

---

## 二、轻语内部架构与代码质量优化点

### 2.1 超大文件清单（重构首要目标）

以下文件均超过 700 行，是可维护性风险的主要来源（行数已实测验证）：

| 文件 | 实测行数 | 角色 | 主要问题 |
| --- | --- | --- | --- |
| `src/store/useChatStore.ts` | **2144** | 单聊状态 | God Store：CRUD + 流式 + 上下文构建 + 世界书触发 + 长记忆 + Token 预算全揉在一起 |
| `src/store/useGroupChatStore.ts` | **2006** | 群聊状态 | 同上，与单聊 store 大量逻辑重复（context 构建、流式、预算计算） |
| `electron/services/ai.ts` | **1197** | AI 适配器 | 所有 provider 适配 + 重试 + 工具循环集中一处，已有 `registerAdapter` 扩展钩子（`ai.ts:1027`）但尚未被外部利用 |
| `src/components/character/CharacterEditor.tsx` | **1102** | 角色编辑器 | 单组件承载表单 + 图片 + 翻译 + 预设联动 |
| `src/pages/SettingsPage.tsx` | **1011** | 全局设置 | 单页装配所有设置分区 |
| `src/components/chat/ChatInput.tsx` | **920** | 聊天输入框 | 输入 + 斜杠命令 + 自动补全 + 发送逻辑 |
| `electron/services/charCard.ts` | 881 | 角色卡解析 | V2/V3 + PNG 读写，可接受但偏大 |
| `src/pages/GroupChatPage.tsx` | 856 | 群聊页 | 与 `useGroupChatStore` 耦合重 |
| `src/pages/LorebookPage.tsx` | 853 | 世界书编辑 | 条目列表 + 编辑器 + 向量索引 |
| `src/components/chat/QuickSettingsPanel.tsx` | 728 | 快捷设置面板 | 浮层装配 |
| `src/components/chat/MessageBubble.tsx` | 700 | 消息气泡 | 渲染 + 操作菜单 + 翻译 + Swipe |
| `src/pages/ApiPage.tsx` | 752 | API 配置页 | 多 provider 表单 |

**建议拆分方向**：
- `useChatStore.ts` → 抽出 `contextBuilder.ts`（上下文构建）、`streamController.ts`（流式状态机）、`lorebookTrigger.ts`（世界书触发）、`memoryManager.ts`（长记忆），store 只保留状态与编排。
- `useGroupChatStore.ts` 与 `useChatStore.ts` 的公共逻辑（`buildContext`、`streamSend`、`estimateBudget`）抽到 `store/_chatShared.ts`，消除约 800 行重复。
- `ai.ts` → 每个 provider 一个文件（`adapters/openai.ts` / `claude.ts` / `gemini.ts` / `ollama.ts`），主文件只做调度与重试。

### 2.2 状态管理耦合分析

实际只有 **7 个 store**（README 宣称 14 个，见 2.3）：

| Store | 大小 | 耦合问题 |
| --- | --- | --- |
| `useChatStore.ts` | 85.6 KB | 在 store 内部直接 import `useSettingsStore` / `usePersonaStore` / `useCharacterStore`（`useChatStore.ts:5-7`），形成 store 间横向依赖，测试时难以隔离 |
| `useGroupChatStore.ts` | 74.2 KB | 同样的跨 store import 模式 |
| `useSettingsStore.ts` | 17.7 KB | 健康 |
| `useCharacterStore.ts` | 10.8 KB | 健康 |
| `useAnnouncementStore.ts` | 1.7 KB | 健康 |
| `usePersonaStore.ts` | 1.2 KB | 健康 |
| `useUIStore.ts` | 0.7 KB | 健康 |

**优化点**：跨 store 的数据读取应通过函数参数显式传入，或在调用处组合，而非在 store 内部 `import` 另一个 store。当前写法导致 `useChatStore` 无法脱离 settings/persona 单测——这也是 store 测试只有 `useGroupChatStore.test.ts` 和 `useSettingsStore.test.ts` 两个的原因。

### 2.3 文档与代码不一致

- `README.md:169` 声称"6 个 Store"，但同文件 `README.md:153` 的结构注释又说 `store/` 含 6 个 store，而实际为 7 个；同时子代理扫描时曾出现"14 个 store"的描述（可能混淆了 store 与 test 文件计数）。**建议**：用脚本统计并回写 README，避免数字打架。
- `README.md` 技术栈表写 "Electron 32"，`package.json:52` 实际依赖 `"electron": "^31.0.0"`——版本不符。

### 2.4 性能隐患

| 位置 | 隐患 | 现状 |
| --- | --- | --- |
| `useChatStore.ts:25` | 流式更新节流 | 已有 `STREAM_THROTTLE_MS = 50ms` 节流，处理得当 |
| 消息列表渲染 | 虚拟滚动 | 已用 `react-virtuoso`，良好 |
| `vectorStore.ts` | 向量检索 | JSON 文件 + 全量内存缓存（`vectorStore.ts:26`），世界书条目上千时余弦计算为 O(n)，且每次检索遍历全部 entries。SillyTavern 用 vectra（本地向量库）做 ANN |
| `electron/services/embedding.ts` | 嵌入计算 | 依赖远程 API，无本地 ONNX 模型，离线不可用 |
| 世界书触发 `src/utils/lorebook.ts` | 关键词扫描 | ~380 行，对每条消息全量扫描所有条目，无索引 |
| 角色卡 PNG 导入 | 同步解析 | `charCard.ts` 中 PNG 元数据提取在主进程同步执行，批量导入大量卡时可能阻塞 UI |

### 2.5 错误处理模式

**优点**：
- `electron/main.ts:129-143` 对 `ipcMain.handle` 做了 monkey-patch，统一捕获异常并记录日志，IPC 层错误处理一致。
- `main.ts:34-46` 注册了 `unhandledRejection` / `uncaughtException` 兜底。
- `useChatStore.ts:57` 的 `friendlyError` 把 API 错误转中文提示，体验友好。

**待改进**：
- 部分前端调用处仅 `console.error` 而无用户可见反馈（需逐一核查 IPC 调用点的 `.catch`）。
- 流式失败时 `isStreaming` 超时兜底为 5 分钟（`useChatStore.ts:51`），偏长，建议降至 60s 或基于无 chunk 间隔判断。

### 2.6 测试覆盖评估

共 **19 个测试文件**，分布如下：

| 层 | 测试数 | 评估 |
| --- | --- | --- |
| `src/utils/__tests__/` | 13 | 工具层覆盖较好（lorebook、regex、macros、memory、messagePostProcess、vector 等） |
| `src/store/__tests__/` | 6 | store 层薄弱，仅 group chat 与 settings 有测试，`useChatStore`（2144 行）无测试 |
| `electron/` | 0 | **主进程与服务层零测试**，AI 适配器、charCard、storage、vectorStore 均无测试 |
| E2E | 0 | 无 Playwright/E2E 测试 |

SillyTavern 有 22 个测试文件含 Playwright E2E。轻语在**主进程服务层零测试**是最大短板，`ai.ts`(1197行)、`charCard.ts`(881行) 这类核心逻辑一旦改动无回归保障。

### 2.7 工程化与 CI/CD 现状

`.github/workflows/ci.yml` 配置了三个 job：

| Job | 内容 | 问题 |
| --- | --- | --- |
| `test` | tsc 类型检查 + vitest + electron 主进程 esbuild 编译 | 健康 |
| `lint` | ESLint | **`continue-on-error: true`（`ci.yml:43`），存量 152 个错误**（注释说明多为 `no-explicit-any`），lint 实际不阻塞 CI |
| `build-windows` | electron-builder NSIS 打包 | 仅 Windows，无 macOS/Linux 产物 |

**优化点**：
- lint 的 `continue-on-error` 使代码规范形同虚设，建议分批清理 152 个 `any`（可先加 `// eslint-disable` 局部豁免 + 设定逐步清零计划），最终移除 `continue-on-error`。
- 无 release 自动化（changelog 生成、自动发版、签名）。
- 无依赖安全审计 step（建议加 `pnpm audit`）。

---

## 三、与 SillyTavern 的功能差距对比

> 标注说明：🟩 = 桌面端不适用，可低优；🟥 = 真实能力缺失，建议补齐；🟨 = 已部分实现/规划中

### 3.1 AI 后端连接器

| 对比项 | 轻语 | SillyTavern | 状态 |
| --- | --- | --- | --- |
| 独立适配器数 | 4（openai/claude/gemini/ollama） | 多后端统一在 `backends/chat-completions.js`(121KB) + `text-completions.js` | 🟨 |
| Provider 类型数 | 8（openai/claude/gemini/ollama/openrouter/vllm/lmstudio/tabby，见 `shared/types.ts:314-319`） | 40+（DeepSeek/Groq/Cohere/MistralAI/xAI/KoboldAI/TextGen WebUI/Horde 等） | 🟥 |
| 扩展机制 | 已有 `registerAdapter` 钩子（`ai.ts:1027`），但无第三方适配器在用 | 端点级路由，社区可贡献 | 🟨 |
| 流式实现 | 各适配器内部实现 | 统一 streaming 管线 | 🟨 |

**结论**：轻语的 `registerAdapter` 设计已为扩展铺路，但**缺少 DeepSeek/Groq 等国产/高性价比后端**，对国内用户价值大。多数 provider 其实都走 OpenAI 兼容协议，补齐成本低（仅新增 provider 类型枚举 + 默认 baseUrl 预设）。

### 3.2 向量检索系统

| 对比项 | 轻语 | SillyTavern | 状态 |
| --- | --- | --- | --- |
| 存储方案 | JSON 文件 + 内存 Map（`vectorStore.ts` 89 行） | vectra 本地向量库 + 9 个向量提供者（`src/vectors/`） | 🟥 |
| 本地嵌入模型 | 无（依赖远程 API，`embedding.ts`） | `sillytavern-transformers`（ONNX 本地模型，`src/transformers.js`） | 🟥 |
| 检索算法 | 全量线性余弦（O(n)） | ANN 近似最近邻 | 🟥 |
| 离线可用 | 否 | 是 | 🟥 |

**结论**：这是**最大技术差距**。轻语世界书的"向量匹配 / LLM 匹配"在 README 标注为"规划中"，而向量基础设施（本地嵌入 + 高效检索）是它的前置依赖。建议引入 `@xenova/transformers`（ONNX runtime web）做本地嵌入，向量存储升级为 SQLite + sqlite-vec 或 hnswlib-wasm。

### 3.3 插件与扩展生态

| 对比项 | 轻语 | SillyTavern | 状态 |
| --- | --- | --- | --- |
| 服务端插件系统 | 无 | `plugins.js` + `src/plugin-loader.js`（动态 import、git pull 自动更新、独立 Router） | 🟩 |
| 前端扩展市场 | 无 | `public/scripts/extensions/` 13 个内置 + third-party | 🟥 |
| 正则扩展 | 有（`src/pages/RegexPage.tsx`） | 有 | 🟩 |
| 斜杠命令 | 14 个内置（`src/commands/builtin/`） | 27 文件的命令引擎（解析器/闭包/调试器） | 🟨 |
| MCP 工具 | 有（`electron/mcp/`） | 无原生 MCP | 🟩（轻语领先） |

**结论**：桌面端无需服务端插件系统，但**前端扩展机制**对长期生态有价值。轻语的 MCP 支持是相对 SillyTavern 的**优势项**，应继续强化。

### 3.4 国际化 i18n

| 对比项 | 轻语 | SillyTavern | 状态 |
| --- | --- | --- | --- |
| i18n 框架 | 无 | `public/locales/` 17 种语言 | 🟥 |
| 硬编码 | 全站中文，`date-fns` 锁定 `zhCN`（`src/utils/format.ts:2`） | 多语言 | 🟥 |

**结论**：轻语定位"本地化开箱即用"，纯中文是产品选择，未必是缺点。但若考虑出海或社区贡献，建议引入 `react-i18next`，至少把 UI 字符串外提为资源文件。**优先级中**。

### 3.5 多用户与安全机制

| 对比项 | 轻语 | SillyTavern | 状态 |
| --- | --- | --- | --- |
| 多用户 | 无（单用户桌面） | `src/users.js`(44KB) 完整多用户 + 数据迁移 | 🟩 |
| SSO | 无 | Authelia/Authentik | 🟩 |
| API Key 加密 | 有（`safeStorage.ts` 系统级） | 配置文件 | 🟩（轻语更好） |
| CSRF/CSP | 无 | CSRF + CSP + host validation | 🟩（桌面端 contextIsolation + sandbox 已足够） |
| Rate limit | 无 | `rate-limiter-flexible` | 🟩 |
| SSRF 防护 | 路径穿越防护有（`main.ts`），出站请求无私有 IP 过滤 | `private-request-filter.js` | 🟨 |
| 请求代理 | 无 | SOCKS5/HTTP `request-proxy.js` | 🟥 |

**结论**：多用户、CSRF、CSP 等对桌面端不适用。但**请求代理（SOCKS5）**对国内用户访问 OpenAI/Claude 有真实价值，建议补齐。SSRF 防护在用户可自定义 baseUrl 的场景下也有必要。

### 3.6 角色卡格式覆盖

| 对比项 | 轻语 | SillyTavern | 状态 |
| --- | --- | --- | --- |
| V1/V2/V3 | 有（`charCard.ts`） | 有 + `validator/TavernCardValidator.js` 规格验证 | 🟨（缺验证器） |
| PNG 元数据 | 有 | 有 | 🟩 |
| BYAF 格式 | 无 | `src/byaf.js` | 🟥（小众，低优） |
| CharX (RisuAI) | 无 | `src/charx.js` | 🟥（小众，低优） |
| 卡片校验 | 无独立校验器 | 有完整 schema 校验 | 🟨 |

**结论**：补一个轻量的 V2/V3 schema 校验函数（基于 `shared/types.ts` 现有类型），导入时拦截非法卡片，成本低收益高。

### 3.7 图像与 TTS 后端

| 对比项 | 轻语 | SillyTavern | 状态 |
| --- | --- | --- | --- |
| 图像后端 | SD WebUI + DALL-E（`imageGen.ts`） | ComfyUI + SD WebUI + Horde + DALL-E + NovAI 等 | 🟥（ComfyUI 已规划） |
| TTS 后端 | Windows 系统语音（`tts.ts` 443行） | 多引擎（Edge TTS / Fish / OpenAI / xTTS 等） | 🟥 |
| STT | 无 | 有 | 🟥 |

**结论**：README 已标注 Edge TTS / Fish Audio / OpenAI TTS "规划中"。建议优先接入 **Edge TTS**（`node-edge-tts` 已在依赖中，`package.json:26`，但 `tts.ts` 似未使用），成本极低。图像侧 ComfyUI 优先级最高（用户基数大）。

### 3.8 跨平台与分发形态

| 对比项 | 轻语 | SillyTavern | 状态 |
| --- | --- | --- | --- |
| 平台 | Windows only | Web 跨平台 | 🟥 |
| macOS/Linux 构建 | 无（`electron-builder` 仅 win target） | N/A | 🟥 |
| 移动端 | 无 | 响应式 Web | 🟩 |
| 数据迁移/升级 | 未见显式版本迁移机制 | `src/users.js` 含数据迁移 | 🟥 |

**结论**：跨平台是 Electron 的天然优势，补 macOS/Linux target 在 `package.json:81-86` 加几行即可，成本低。**数据迁移机制**是隐性风险——当 `shared/types.ts` 的数据结构变更时，旧版本本地数据如何升级？建议引入 schema version + 迁移函数。

---

## 四、具体可执行优化建议（带文件路径）

按优先级 P0（高）/P1（中）/P2（低）分级。

### P0 — 高优先级（影响稳定性与核心体验）

| # | 建议 | 文件路径 | 修改方向 | 预期收益 |
| --- | --- | --- | --- | --- |
| 1 | 拆分 `useChatStore.ts`（2144行） | `src/store/useChatStore.ts` | 抽出 `store/_chatContext.ts`（上下文构建）、`store/_streamCtrl.ts`（流式状态机）、`store/_lorebookTrigger.ts` | 可维护性 + 可测试性 |
| 2 | 消除两 store 重复逻辑 | `src/store/useChatStore.ts`、`src/store/useGroupChatStore.ts` | 公共逻辑提至 `store/_chatShared.ts` | 删减约 800 行重复 |
| 3 | 主进程服务层补测试 | `electron/services/ai.ts`、`charCard.ts`、`storage.ts` | 新增 `electron/services/__tests__/` | 防回归 |
| 4 | 引入数据迁移机制 | `shared/types.ts`（加 `schemaVersion`）、新增 `electron/services/migration.ts` | 读取旧数据时按 version 执行迁移函数 | 避免版本升级数据损坏 |
| 5 | 清理 lint 存量错误 | 全仓 `any`（CI 注释 152 个） | 逐文件补类型，目标移除 `ci.yml:43` 的 `continue-on-error` | 代码规范生效 |

### P1 — 中优先级（功能补齐，用户可感知）

| # | 建议 | 文件路径 | 修改方向 | 预期收益 |
| --- | --- | --- | --- | --- |
| 6 | 接入 Edge TTS | `electron/ipc/tts.ts`、`electron/services/`（新增 edgeTts） | `node-edge-tts` 已在依赖，编写适配器 | 免费高质量语音 |
| 7 | 补 DeepSeek/Groq provider | `shared/types.ts:314`（ProviderType）、`src/pages/ApiPage.tsx` | 新增枚举 + 默认 baseUrl 预设，复用 openaiAdapter | 国产/低价后端 |
| 8 | 接入 ComfyUI 图像后端 | `electron/services/imageGen.ts` | 新增 ComfyUI workflow 适配 | 主流生图生态 |
| 9 | 引入本地嵌入模型 | `electron/services/embedding.ts` | 接入 `@xenova/transformers`（ONNX） | 离线向量检索 |
| 10 | 向量存储升级 | `electron/services/vectorStore.ts`（89行） | 改用 sqlite-vec 或 hnswlib-wasm | 千级条目检索性能 |
| 11 | 添加请求代理（SOCKS5） | `electron/services/ai.ts`（fetch 处） | 支持 `https_proxy`/SOCKS5 环境变量或设置项 | 国内访问 OpenAI |
| 12 | 跨平台构建 | `package.json:81-86` | 加 `mac`/`linux` target | 扩大用户群 |
| 13 | 角色卡 schema 校验 | 新增 `electron/services/charCardValidator.ts` | 基于 `shared/types.ts` 校验 V2/V3 | 拦截非法卡片 |

### P2 — 低优先级（生态与打磨）

| # | 建议 | 文件路径 | 修改方向 | 预期收益 |
| --- | --- | --- | --- | --- |
| 14 | 引入 i18n 框架 | `src/`（新增 `i18n/`） | `react-i18next`，先抽 UI 字符串 | 出海/社区贡献 |
| 15 | 前端扩展机制 | `src/`（新增 extension loader） | 借鉴 SillyTavern `public/scripts/extensions/` | 长期生态 |
| 16 | SSRF 防护 | `electron/services/ai.ts` | baseUrl 自定义时过滤私有 IP | 安全加固 |
| 17 | 自动发版 | `.github/workflows/` | tag 触发 + changelog 自动生成 | 发版效率 |
| 18 | 依赖安全审计 | `.github/workflows/ci.yml` | 加 `pnpm audit` step | 供应链安全 |
| 19 | 修正 README 版本 | `README.md:8`（Electron 32 → 31）、`README.md:169`（store 数量） | 与 `package.json` 对齐 | 文档准确 |
| 20 | 流式超时缩短 | `src/store/useChatStore.ts:51`（`STREAM_TIMEOUT_FALLBACK_MS` 5min → 60s 或基于 chunk 间隔） | | 卡死更快恢复 |

---

## 五、结论与优先级路线图

### 总体评价

轻语在工程素养上**显著优于**原版 SillyTavern：TypeScript 强类型、Zustand 状态管理、Vite 构建、Tailwind 主题系统、IPC 统一异常兜底、`registerAdapter` 扩展钩子、MCP 集成——这些都是 SillyTavern（jQuery + 全局变量）所不具备的现代化优势。

不足集中在**功能广度**（AI 后端少、向量系统弱、无本地嵌入、图像/TTS 后端少）与**代码组织**（超大文件、store 耦合、主进程零测试、lint 失效）。

### 建议路线图

```
阶段一（稳定性筑底，1-2 周）
  └─ P0-1 拆分 useChatStore
  └─ P0-2 消除 store 重复
  └─ P0-4 数据迁移机制
  └─ P0-5 清理 lint（分批）

阶段二（核心功能补齐，2-4 周）
  └─ P1-6 Edge TTS
  └─ P1-7 DeepSeek/Groq
  └─ P1-9 本地嵌入模型
  └─ P1-10 向量存储升级
  └─ P1-8 ComfyUI

阶段三（生态与打磨，4-8 周）
  └─ P1-11 请求代理
  └─ P1-12 跨平台构建
  └─ P0-3 主进程测试补齐
  └─ P2-14 i18n
  └─ P2-15 前端扩展机制
```

### 轻语相对 SillyTavern 的既有优势（应保持）

1. **桌面端开箱即用** — 免去用户部署 Node 服务的门槛
2. **TypeScript 强类型** — `shared/types.ts`(708行) + `ipc-api.ts`(365行) 的契约层，前后端类型安全
3. **MCP 工具集成** — SillyTavern 无原生 MCP，轻语已领先
4. **API Key 系统级加密** — `safeStorage.ts` 优于 ST 的配置文件明文
5. **Zustand + react-virtuoso** — 现代化状态与渲染，ST 的 jQuery DOM 操作在长对话下性能劣势明显
6. **Tailwind 主题系统** — 6 主题色 + 三档圆角，ST 依赖用户自写 CSS

---

> 本报告所有行号与文件路径基于 2026-08-01 工作区状态实测，后续代码变更可能导致引用偏移，建议结合 `git blame` 定位最新位置。
