# 项目审查报告：轻语（QingYu）

> 审查日期：2026-08-03  
> 项目版本：v0.11.10  
> 技术栈：Electron + React + Vite + TypeScript + Zustand

---

## 项目概述

轻语是一款轻量级 AI 角色扮演客户端，采用 Electron + React + Vite 架构，支持多 AI Provider、MCP 集成、世界书（Lorebook）、群聊、TTS、图片生成等功能。整体架构清晰，代码质量较好。

---

## 🔴 严重问题（需立即修复）

### 1. `withCharacterLock` 未定义

**文件**：`electron/ipc/character.ts`（第 53、63、89、127、273 行）

多处调用 `withCharacterLock`，但该函数在整个代码库中未定义也未导入。所有角色操作（保存/删除/导入）运行时会抛出 `ReferenceError`。

```typescript
// 第 53 行示例
await withCharacterLock(id, async () => {
  // ...
})
```

**影响**：角色相关功能全部不可用。

**修复建议**：定义或导入 `withCharacterLock` 函数，可参考 `storage.ts` 中的 `withFileLock` 实现。

---

### 2. 重定向 SSRF 绕过漏洞

**文件**：`electron/services/charCard.ts`（第 346 行）

下载角色卡图片时，重定向目标 URL 未重新经过 SSRF 安全检查。恶意服务器可以重定向到内网地址（如 `http://169.254.169.254/`），绕过初始 SSRF 检查。

```typescript
// 第 346 行
_downloadOne(res.headers.location, proxy, maxRedirects - 1)
// 未对 res.headers.location 做 SSRF 校验
```

**影响**：安全漏洞，可被利用访问内网资源。

**修复建议**：在递归调用 `_downloadOne` 前，对 `location` URL 重新执行 SSRF 检查。

---

### 3. 下载响应无大小限制

**文件**：`electron/services/charCard.ts`（第 361-362 行）

下载文件时无最大响应大小限制，如果服务器返回超大响应（如几 GB），内存会被耗尽。

```typescript
// 第 361-362 行
chunks.push(chunk)
// 无累计大小检查
```

**影响**：内存耗尽导致应用崩溃。

**修复建议**：添加累计大小检查，超过阈值（如 50MB）时中止下载。

---

### 4. 非原子写入导致数据损坏风险

**文件**：
- `electron/ipc/chat.ts`（第 107 行）— `writeMessages` 直接 `writeFileSync`
- `electron/services/safeStorage.ts`（第 73 行）— 凭据写入直接 `writeFileSync`

直接写入目标文件，不使用 temp + rename 的原子写入模式。写入过程中崩溃会导致数据损坏。

```typescript
// chat.ts 第 107 行
writeFileSync(filePath, content, 'utf-8')

// safeStorage.ts 第 73 行
writeFileSync(path, JSON.stringify(data, null, 2), 'utf-8')
```

**影响**：崩溃时数据文件可能损坏，用户丢失聊天记录或凭据。

**修复建议**：统一使用 `storage.ts` 中已有的原子写入模式（先写临时文件再 rename）。

---

## 🟠 中等问题（建议尽快修复）

### 安全类

#### 5. JWT 算法混淆攻击

**文件**：`server/app/middleware/auth.js`（第 21 行）

`jwt.verify(token, JWT_SECRET)` 未指定算法，可能遭受算法混淆攻击（Algorithm Confusion Attack）。

```javascript
// 第 21 行
const decoded = jwt.verify(token, JWT_SECRET)
// 应改为
const decoded = jwt.verify(token, JWT_SECRET, { algorithms: ['HS256'] })
```

#### 6. CORS 配置逻辑错误

**文件**：`server/app/server.js`（第 18 行）

当 `ALLOWED_ORIGINS` 环境变量为空字符串时，`split(',').filter(Boolean)` 返回空数组 `[]`，而空数组是 truthy，导致 `[] || ['http://localhost:3000']` 结果为 `[]`，CORS 完全禁止所有来源。

```javascript
// 第 18 行
const origins = (process.env.ALLOWED_ORIGINS || '').split(',').filter(Boolean)
// origins = [] 时，下面的 || 不会生效
app.use(cors({ origin: origins || ['http://localhost:3000'] }))
```

**修复建议**：改为 `origins.length ? origins : ['http://localhost:3000']`。

#### 7. XSS 风险：公告内容未过滤

**文件**：`server/app/routes/announcements.js`（第 72-75 行）

`title` 和 `content` 未做 XSS 过滤或长度限制。如果管理后台直接渲染这些内容，可能导致存储型 XSS。

#### 8. IPv6 私有地址检查不完整

**文件**：`electron/utils/pathGuard.ts`（第 69 行）

方括号包裹的 IPv6 地址（如 `[::1]`）中，`new URL()` 会保留方括号，导致 `hostname === '::1'` 检查失败，绕过 SSRF 防护。

---

### 数据完整性类

#### 9. `chat:clearChat` 竞态条件

**文件**：`electron/ipc/chat.ts`（第 472-476 行）

清空对话时，先 `readdirSync` 获取文件列表，再逐个加锁和删除。在两步之间，新的写入（如 `saveMessage`）可能创建新文件，这些文件不会被删除，导致数据不一致。

#### 10. `activeRequests` Map 竞态

**文件**：`electron/services/ai.ts`（第 134 行）

前端用同一个 `requestId` 快速连续发起两次请求时，第二次 `set` 覆盖第一次的 controller，第一次请求的 `finally` 中 `delete` 会把第二次的 controller 也删掉，导致第二次请求无法被取消。

#### 11. 向量缓存无上限

**文件**：`electron/services/vectorStore.ts`（第 28 行）

`const cache = new Map<string, VectorIndex>()` 随世界书数量无限增长，每个 VectorIndex 包含大量高维向量数据。

**修复建议**：添加 LRU 淘汰策略或缓存大小上限。

---

### 前端问题

#### 12. 事件监听器内存泄漏

**文件**：`src/store/useChatStore.ts`（第 230-264 行）

`translateMessage` 方法中的 `onChunk`、`onDone`、`onError` 监听器未在组件卸载时清理，导致内存泄漏。

```typescript
// 第 230 行
const unbindChunk = window.api.ai.onChunk((data) => {
  if (data.requestId !== requestId) return
  result += data.text
})
// 缺少：组件卸载时调用 unbindChunk()
```

#### 13. 异步操作期间状态竞态

**文件**：`src/store/useGroupChatStore.ts`（第 118-127 行）

快速切换会话时，旧会话的 `loadMessages` 可能在新的加载完成后才返回，覆盖新会话的消息。

#### 14. MCP 事件监听器泄漏

**文件**：`electron/mcp/manager.ts`（第 75-82 行）

每次 `startServer` 都会注册 `log`、`disconnected`、`error` 监听器。server 被 stop 后又 start 时，旧监听器不会被移除，导致重复注册。

#### 15. 流式响应频繁更新

**文件**：`src/store/streamController.ts`

每个 chunk 都触发完整状态更新和重新渲染，长消息流时性能下降明显。

**修复建议**：增大 flush 间隔或使用 `requestAnimationFrame` 节流。

---

## 🟡 低优先级改进

### 代码质量

| # | 问题 | 文件 | 说明 |
|---|------|------|------|
| 16 | TypeScript 严格模式未启用 | `tsconfig.json:19` | `strict: false` 降低了类型安全性 |
| 17 | `readJson` 静默吞掉异常 | `electron/services/storage.ts:54` | 无法区分"文件不存在"和"文件损坏" |
| 18 | 工具调用无超时控制 | `electron/services/toolLoop.ts:134` | 单个 MCP 工具调用可能永远不返回 |
| 19 | PNG 解析无边界检查 | `electron/services/charCard.ts:64` | 损坏的 PNG 可能导致内存问题 |
| 20 | 嵌入结果维度不一致 | `electron/services/embedding.ts:99` | 补零向量 `[]` 维度与正常向量不一致 |

### 输入验证

| # | 问题 | 文件 | 说明 |
|---|------|------|------|
| 21 | 分页参数验证不足 | `server/app/routes/announcements.js:9-11` | `pageSize` 无上限，可能 `pageSize=999999` |
| 22 | 版本号格式未验证 | `server/app/routes/version.js:34-42` | 未验证 semver 格式 |
| 23 | `probability` 无运行时验证 | `shared/types.ts:177` | 注释说 0-100，但无验证 |
| 24 | `updateSession` 参数过于宽松 | `shared/ipc-api.ts:75` | `Record<string, unknown>` 允许任意属性 |
| 25 | 登录剩余次数可能为负 | `server/app/routes/auth.js:69,76` | `rateInfo.remaining - 1` 可能为 -1 |

### 性能与体验

| # | 问题 | 文件 | 说明 |
|---|------|------|------|
| 26 | 重试延迟不检查 signal | `electron/services/ai.ts:100` | 取消请求后仍等待完整延迟 |
| 27 | 日志可能包含敏感信息 | `electron/utils/safeHandle.ts:20-22` | 错误堆栈可能包含 API Key |
| 28 | 缺少服务器错误处理中间件 | `server/app/server.js` | 无 404 和全局错误处理 |
| 29 | `shutdownAll` 使用 `Promise.all` | `electron/mcp/manager.ts:143` | 一个失败影响其他，应改用 `Promise.allSettled` |
| 30 | Markdown 导出未转义特殊字符 | `electron/ipc/chat.ts:501` | `msg.content` 直接嵌入 Markdown |
| 31 | `plain:` 前缀明文凭据 | `electron/services/safeStorage.ts:41-43` | 某些凭据以明文存储，应添加警告日志 |
| 32 | 世界书自动匹配性能 | `electron/services/charCard.ts:614-635` | 导入角色卡时对每个世界书全量读取 |

---

## ✅ 项目优点

### 安全性
- `tavern://` 自定义协议有路径穿越防护（正则白名单）
- Electron 使用 `contextIsolation: true` + `sandbox: true`
- 外部链接仅允许 `http/https`，用系统浏览器打开
- 正则表达式有长度限制（`MAX_PATTERN_LENGTH = 500`）防止 ReDoS
- 文本长度限制（`MAX_TEXT_LENGTH = 200,000`）防止性能问题

### 错误处理
- 全局 `unhandledRejection` 和 `uncaughtException` 捕获
- IPC 处理器统一异常捕获和日志记录（拦截 `ipcMain.handle`）
- 应用退出前有序清理 MCP server 和 TTS 进程

### 架构设计
- 模块化设计合理（Electron / React / Server / Shared 四层分离）
- 共享类型定义统一（`shared/types.ts`）
- 文件锁机制（`withFileLock`）防止并发写入冲突
- 原子写入模式（temp + rename）在 `storage.ts` 中已实现

### 功能丰富
- 多 AI Provider 支持（OpenAI / Claude / Gemini / Ollama）
- MCP 协议集成
- 世界书（Lorebook）语义搜索
- 群聊功能
- TTS / 图片生成
- 完善的测试覆盖

---

## 📋 修复优先级总览

| 优先级 | 问题编号 | 数量 | 建议时间 |
|--------|----------|------|----------|
| 🔴 严重 | 1-4 | 4 | 立即修复 |
| 🟠 中等 | 5-15 | 11 | 1-2 周内 |
| 🟡 低 | 16-32 | 17 | 迭代改进 |

---

## 🔧 具体修复建议

### 问题 1：定义 `withCharacterLock`

```typescript
// electron/ipc/character.ts 顶部添加
import { withFileLock } from '../services/storage'

function withCharacterLock<T>(id: string, fn: () => Promise<T>): Promise<T> {
  return withFileLock(`character-${id}`, fn)
}
```

### 问题 2：重定向 SSRF 校验

```typescript
// electron/services/charCard.ts _downloadOne 中
if (res.headers.location) {
  const redirectUrl = new URL(res.headers.location, url)
  if (!isSafeUrl(redirectUrl)) {
    throw new Error('重定向目标不安全: ' + redirectUrl.hostname)
  }
  return _downloadOne(redirectUrl.toString(), proxy, maxRedirects - 1)
}
```

### 问题 3：下载大小限制

```typescript
const MAX_DOWNLOAD_SIZE = 50 * 1024 * 1024 // 50MB
let totalSize = 0
res.on('data', (chunk) => {
  totalSize += chunk.length
  if (totalSize > MAX_DOWNLOAD_SIZE) {
    res.destroy()
    reject(new Error('文件过大'))
    return
  }
  chunks.push(chunk)
})
```

### 问题 5：JWT 算法锁定

```javascript
// server/app/middleware/auth.js
const decoded = jwt.verify(token, JWT_SECRET, { algorithms: ['HS256'] })
```

### 问题 6：CORS 修复

```javascript
// server/app/server.js
const origins = (process.env.ALLOWED_ORIGINS || '').split(',').filter(Boolean)
app.use(cors({ origin: origins.length ? origins : ['http://localhost:3000'] }))
```
